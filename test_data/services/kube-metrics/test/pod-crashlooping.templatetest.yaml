---
$schema: /app-interface/template-test-1.yml

labels:
  service: app-metadata-tests

name: pod-crashlooping
description: test for kube-metrics-pod-crashlooping prometheus rules

resourcePath: /observability/prometheusrules/kube-metrics-pod-crashlooping.prometheusrules.yaml.j2
expectedResult: /observability/prometheusrules/test/kube-metrics-pod-crashlooping-expected-results.prometheusrules.yaml
