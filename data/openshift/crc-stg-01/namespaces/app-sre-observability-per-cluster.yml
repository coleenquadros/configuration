---
$schema: /openshift/namespace-1.yml

labels: {}

name: app-sre-observability-per-cluster
description: namespace for the app-sre per-cluster observability stack

cluster:
  $ref: /openshift/crc-stg-01/cluster.yml

app:
  $ref: /services/observability/app.yml

environment:
  $ref: /products/app-sre/environments/stage.yml

openshiftResources:
### Nginx oauth proxy configuration
- provider: vault-secret
  path: app-interface/crc-stg-01/openshift-customer-monitoring/nginx-auth-proxy
  version: 1
  annotations:
    qontract.recycle: "true"
