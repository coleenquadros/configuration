---
$schema: /openshift/namespace-1.yml

labels: {}

name: openshift-monitoring
description: openshift-monitoring namespace

cluster:
  $ref: /openshift/hst01ue1/cluster.yml

app:
  $ref: /services/app-sre/app.yml

environment:
  $ref: /products/app-sre/environments/stage.yml

managedResourceTypes:
- ConfigMap

openshiftResources:
- provider: resource
  path: /observability/uwm/cluster-monitoring-config.configmap.yaml
