---
$schema: /openshift/namespace-1.yml

labels: {}

name: openshift-logging
description: openshift-logging namespace

cluster:
  $ref: /openshift/hivep04ew2/cluster.yml

app:
  $ref: /services/app-sre/app.yml

environment:
  $ref: /products/app-sre/environments/production.yml

managedResourceTypes:
#- Subscription
- ClusterLogging

openshiftResources:
# Disabling subscriptions till SREP enables logging, ClusterLogging installed manually as per OHSS-2944
#- provider: resource-template
#  path: /setup/clusterlogging/elasticsearch-operator.subscription.yaml
#- provider: resource-template
#  path: /setup/clusterlogging/cluster-logging.subscription.yaml
- provider: resource-template
  path: /setup/clusterlogging/instance-4.6.clusterlogging.yaml
  variables:
    memoryRequests: 8Gi
