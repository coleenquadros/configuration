---
$schema: /openshift/cluster-1.yml

labels:
  service: openshift.io

name: dev-cluster
description: Development cluster managed by the App-SRE. Best-effort SLA. Non-production.
serverUrl: "https://api.dsaas.openshift.com"
