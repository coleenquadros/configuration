---
$schema: /openshift/cluster-1.yml

labels:
  service: openshift.io

name: dsaas
description: Production cluster used by OpenShift.io
serverUrl: "https://api.dsaas.openshift.com"

auth:
  service: github-org
  org: rhdt-dev

managedGroups:
- dedicated-admins

subnets:
  vpc: 172.29.0.0/16
  service: 172.30.0.0/16
  pod: 10.128.0.0/14

automationToken:
  path: app-sre/creds/kube-configs/dsaas
  field: token

internal: false
