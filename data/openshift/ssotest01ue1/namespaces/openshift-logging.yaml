---
$schema: /openshift/namespace-1.yml

labels: {}

name: openshift-logging
description: ssotest01ue1 openshift-logging namespace

cluster:
  $ref: /openshift/ssotest01ue1/cluster.yml

app:
  $ref: /services/app-sre/app.yml

environment:
  $ref: /products/app-sre/environments/stage.yml

managedTerraformResources: true

terraformResources:
- provider: aws-iam-service-account
  account: app-sre
  identifier: log-forwarder-ssotest01ue1
  output_resource_name: instance
  user_policy:
    {
      "Version": "2012-10-17",
      "Statement": [
        {
          "Action": [
            "logs:CreateLogGroup",
            "logs:DescribeLogStreams",
            "logs:CreateLogStream",
            "logs:PutLogEvents"
          ],
          "Effect": "Allow",
          "Resource": "*"
        },
        {
          "Action": ["logs:DescribeLogGroups"],
          "Effect": "Allow",
          "Resource": "*"
        }
      ]
    }
