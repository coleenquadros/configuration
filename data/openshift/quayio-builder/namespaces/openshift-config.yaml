---
$schema: /openshift/namespace-1.yml

labels: {}

name: openshift-config
description: quayio-builder openshift-config namespace

cluster:
  $ref: /openshift/quayio-builder/cluster.yml

app:
  $ref: /services/app-sre/app.yml

environment:
  $ref: /products/app-sre/environments/production.yml

managedResourceTypes:
- Secret
- OAuth
- ClusterRoleBinding
- SecurityContextConstraints

openshiftResources:
- provider: resource-template
  path: /quay-builder/setup/github-secret.secret.yaml
- provider: resource-template
  path: /quay-builder/setup/cluster.oauth.yaml
- provider: resource
  path: /quay-builder/setup/quay-app-sre-cluster-admins.clusterrolebinding.yaml
- provider: resource
  path: /quay-builder/setup/quay-devs-cluster-admins.clusterrolebinding.yaml
- provider: resource
  path: /quay-builder/setup/quay-builder-scc.securitycontextconstraints.yaml
