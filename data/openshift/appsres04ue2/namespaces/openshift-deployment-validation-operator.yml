---
$schema: /openshift/namespace-1.yml

labels: {}

name: openshift-deployment-validation-operator
description: namespace for the OSD Deployment Validation Operator

cluster:
  $ref: /openshift/appsres04ue2/cluster.yml

app:
  $ref: /services/deployment-validation-operator/app.yml

environment:
  $ref: /products/app-sre/environments/production.yml

clusterAdmin: true

# # currently applied manually with https://github.com/app-sre/qontract-reconcile/pull/3087
# managedRoles: true

# # currently applied manually with https://github.com/app-sre/qontract-reconcile/pull/3086
# networkPoliciesAllow:
# - $ref: /services/observability/namespaces/openshift-customer-monitoring.appsres04ue2.yml
