---
$schema: /openshift/cluster-1.yml

labels:
  service: quayio

name: quays02ue1
description: Stage 4.3.25 cluster for Quay.io
consoleUrl: "https://console-openshift-console.apps.quays02ue1.s6d1.p1.openshiftapps.com/"
kibanaUrl: ""
prometheusUrl: "https://prometheus.quays02ue1.devshift.net"
serverUrl: "https://api.quays02ue1.s6d1.p1.openshiftapps.com:6443"

auth:
  service: github-org-team
  org: app-sre
  team: quays02ue1-cluster

ocm:
  $ref: /dependencies/ocm/production.yml

managedGroups:
- dedicated-admins

managedClusterRoles: true

spec:
  provider: aws
  region: us-east-1
  major_version: 4
  multi_az: false
  nodes: 12
  instance_type: m5.4xlarge
  storage: 1100
  load_balancers: 4

network:
  vpc: 10.106.0.0/16
  service: 172.30.0.0/16
  pod: 10.128.0.0/14

automationToken:
  path: app-sre/creds/kube-configs/quays02ue1
  field: token

disable:
  e2eTests:
  - create-namespace
  - dedicated-admin-rolebindings
  - default-network-policies
  - default-project-labels

internal: false

awsInfrastructureAccess:
- awsGroup:
    $ref: /aws/quayio-stage/groups/App-SRE-admin.yml
  accessLevel: read-only
- awsGroup:
    $ref: /aws/quayio-stage/groups/App-SRE-admin.yml
  accessLevel: network-mgmt
