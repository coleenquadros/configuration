---
$schema: /openshift/namespace-1.yml

labels: {}

name: dedicated-admin
description: hive-integration dedicated-admin namespace

cluster:
  $ref: /openshift/hive-integration/cluster.yml

app:
  $ref: /services/app-sre/app.yml

environment:
  $ref: /products/app-sre/environments/integration.yml

# when using `oc`, use the override as the kind instead of the resource
managedResourceTypeOverrides:
- resource: Template
  override: Template.template.openshift.io

managedResourceTypes:
- Template

openshiftResources:
- provider: resource
  path: /setup/project-request.template.yaml
