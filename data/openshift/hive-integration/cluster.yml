---
$schema: /openshift/cluster-1.yml

labels:
  service: hive

name: hive-integration
description: Integration cluster used by Hive
serverUrl: "https://api.hive-integration.openshift.com"

# Temporary due to https://jira.coreos.com/browse/SREP-1847
unManaged: true

automationToken:
  path: app-sre/creds/kube-configs/hive-integration
  field: token

managedGroups:
- hive-admins
- hive-readers
- dedicated-readers
