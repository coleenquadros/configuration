---
$schema: /openshift/namespace-1.yml

labels: {}

name: openshift-operators
description: hive-stage-01 openshift-operators namespace

cluster:
  $ref: /openshift/hive-stage-01/cluster.yml

app:
  $ref: /services/app-sre/app.yml

environment:
  $ref: /products/app-sre/environments/stage.yml

managedResourceTypes:
- Subscription

openshiftResources:
- provider: resource
  path: /cert-manager/openshift-cert-manager-operator-subscription.yaml
