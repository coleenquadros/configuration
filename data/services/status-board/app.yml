---
$schema: /app-sre/app-1.yml

labels:
  service: status-board

name: status-board
grafanaUrl: https://grafana.app-sre.devshift.net/ # to be fixed lager
sopsUrl: https://gitlab.cee.redhat.com/TODO # to be fixed later
architectureDocument: https://TODO # to be fixed later

description: |
  Status Board allows arbitrary services, integrations, test-suites and quality
  tools to report status for their scope.

onboardingStatus: InProgress

serviceOwners:
- name: SD OPS Dev
  email: sd-ops-services-dev@redhat.com

escalationPolicy:
  $ref: /teams/sd-ops-dev/escalation-policies/status-board.yml

dependencies:
- $ref: /dependencies/ci-int/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/gitlab/service.yml
- $ref: /dependencies/sso/service.yml
- $ref: /dependencies/quay/service.yml
