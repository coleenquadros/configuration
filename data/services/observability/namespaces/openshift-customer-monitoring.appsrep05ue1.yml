---
$schema: /openshift/namespace-1.yml

labels: {}

name: openshift-customer-monitoring
description: App-SRE OpenShift Application monitoring

cluster:
  $ref: /openshift/appsrep05ue1/cluster.yml

app:
  $ref: /services/observability/app.yml

environment:
  $ref: /products/app-sre/environments/production.yml

managedResourceTypes:
- Secret
- PrometheusRule
- ServiceMonitor

openshiftResources:
# Observability

## Secrets
### Prometheus additional alertmanager config
### Defines what alertmanager Prometheus fires alerts to
- provider: vault-secret
  path: app-interface/global/observability/prometheus-additional-alertmanager-config
  version: 1
### Prometheus additional scrape config
### Defines any additional jobs that don't fit into the servicemonitor model
- provider: resource-template
  type: extracurlyjinja2
  path: /observability/prometheus/prometheus-app-sre-additional-scrapeconfig-internal.secret.yaml
  variables:
    is_tekton_cluster: true
### Alertmanager configuration
- provider: resource-template
  type: extracurlyjinja2
  path: /observability/alertmanager/alertmanager-instance.secret.yaml
  validate_alertmanager_config: true
  enable_query_support: true
  variables:
    jiralert_host: jiralert.app-sre.devshift.net

## Servicemonitors
### All managed Prometheus Operators
- provider: resource
  path: /observability/servicemonitors/prometheus-operators-production.servicemonitor.yaml

## Prometheus Rules
### Federated Kubernetes metrics
- provider: prometheus-rule
  path: /observability/prometheusrules/kube-metrics.prometheusrules.yaml
  type: resource
### kube-metrics pod-crashlooping
- provider: prometheus-rule
  type: resource-template-extracurlyjinja2
  path: /observability/prometheusrules/kube-metrics-pod-crashlooping.prometheusrules.yaml.j2
  enable_query_support: true
### OCM metrics
- provider: prometheus-rule
  path: /observability/prometheusrules/ocm-metrics.prometheusrules.yaml
  type: resource
  tests:
  - /observability/prometheusrules/ocm-metrics-test.prometheusrules.yaml
### Prometheus alerts
- provider: prometheus-rule
  type: resource-template-extracurlyjinja2
  path: /observability/prometheusrules/prometheus-app-sre.prometheusrules.yaml.j2
  enable_query_support: true
### Prometheus mixin alerts
- provider: prometheus-rule
  path: /observability/prometheusrules/prometheus-mixin.prometheusrules.yaml
  type: resource
### OLM alerts
- provider: prometheus-rule
  path: /observability/olm/olm.prometheusrules.yaml
  type: resource
### App SRE prometheus PVC metrics
- provider: prometheus-rule
  path: /observability/prometheusrules/kube-metrics-pvc-observability.prometheusrules.yaml

################## Add  any cluster-specific content below this line ##################

## Prometheus Rules
### App-interface
  type: resource
- provider: prometheus-rule
  path: /observability/prometheusrules/app-interface-production-int.prometheusrules.yaml
  type: resource
### Qontract-reconcile SLOs
- provider: prometheus-rule
  path: /observability/prometheusrules/qontract-reconcile-time-to-reconcile-slo.prometheusrules.yaml
  type: resource-template-extracurlyjinja2
  variables:
    namespace: app-interface-production
    severity: medium
### ci.int.devshift.net
- provider: prometheus-rule
  path: /observability/prometheusrules/ci-int.prometheusrules.yaml
  type: resource
  tests:
  - /observability/prometheusrules/ci-int.prometheusrulestests.yaml
### Hive Capacity
## hive-production
- provider: prometheus-rule
  type: resource-template-extracurlyjinja2
  path: /observability/prometheusrules/hive-production-capacity.prometheusrules.yaml
  variables:
    appsre_env: production
    max_clusters_per_shard: 700
    grafana_datasource: appsrep05ue1-prometheus
  tests:
  - /observability/prometheusrules/hive-production-capacity.prometheusrules-test.yaml
## hive-stage
- provider: prometheus-rule
  type: resource-template-extracurlyjinja2
  path: /observability/prometheusrules/hive-stage-capacity.prometheusrules.yaml
  variables:
    appsre_env: staging
    max_clusters_per_shard: 700
    grafana_datasource: appsrep05ue1-prometheus
### App-SRE contract
- provider: prometheus-rule
  type: resource-template-extracurlyjinja2
  path: /app-sre/app-sre-contract/app-sre-contract.prometheusrules.yaml.j2
# Vault rules
- provider: prometheus-rule
  path: /observability/prometheusrules/vault-production.prometheusrules.yaml
  type: resource
  tests:
  - /observability/prometheusrules/vault-production.prometheusrulestest.yaml
### Container Security Operator rules
- provider: prometheus-rule
  path: /observability/prometheusrules/container-security-operator.prometheusrules.yaml
  type: resource
  tests:
  - /observability/prometheusrules/container-security-operator.prometheusrulestest.yaml
### Blackbox exporter checks
- provider: prometheus-rule
  path: /observability/prometheusrules/blackbox-exporter-int.prometheusrules.yaml
  type: resource

### gitlab merge queue
- provider: prometheus-rule
  path: /observability/prometheusrules/gitlab-merge-queue.prometheusrules.yaml
  type: resource
  tests:
  - /observability/prometheusrules/gitlab-merge-queue.prometheusrulestest.yaml

## ServiceMonitors
### App-SRE contract
- provider: resource-template
  type: jinja2
  path: /observability/servicemonitors/app-interface.servicemonitor.yaml
  variables:
    environment: production
    namespace: app-interface-production
### Qontract-reconcile
- provider: resource-template
  type: jinja2
  path: /observability/servicemonitors/qontract-reconcile.servicemonitor.yaml
  variables:
    environment: production
    namespace: app-interface-production
### MT-SRE Jenkins (ci-int) builds
- provider: prometheus-rule
  path: /observability/prometheusrules/mt-sre-ci-int.prometheusrules.yaml
  type: resource
  tests:
  - /observability/prometheusrules/mt-sre-ci-int.prometheusrulestests.yaml
### Vault
#### TODO: sunset the vault-exporter. This will require refactor of existing vault alerting rules
- provider: resource-template
  type: jinja2
  path: /observability/servicemonitors/vault.servicemonitor.yaml
  variables:
    environment: prod
    namespace: vault-prod
#### Targets primary container's /v1/sys/metrics endpoint
- provider: resource-template
  type: jinja2
  path: /observability/servicemonitors/vault-telemetry.servicemonitor.yaml
  variables:
    environment: prod
    namespace: vault-prod
### skupper - x509-certificate-exporter
- provider: resource-template
  type: jinja2
  path: /observability/servicemonitors/x509-certificate-exporter.servicemonitor.yaml
  variables:
    namespace: app-sre-observability-production
- provider: prometheus-rule
  path: /observability/prometheusrules/skupper-network-monitoring-prometheusrules.yaml
  type: resource
  tests:
  - /observability/prometheusrules/skupper-network-monitoring-prometheusrulestests.yaml

sharedResources:
- $ref: /services/observability/shared-resources/dvo-openshift.yml
- $ref: /services/observability/shared-resources/dvo-alerts.yml
