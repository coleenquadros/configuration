---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: observability

name: ci-ext-observability-views

description: ''

app:
  $ref: /services/observability/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: app-sre-observability
    description: 'app-sre-observability view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[(app-sre-observability|online-registration-exporter|aws-resource-exporter|zabbix-impersonator)\].*'
