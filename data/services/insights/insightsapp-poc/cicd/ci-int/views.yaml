---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insightsapp-poc

name: ci-int-insightsapp-poc

description: ''

app:
  $ref: /services/insights/insightsapp-poc/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: insightsapp-poc
    description: 'insightsapp-poc view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[(insightsapp-poc)\].*'
