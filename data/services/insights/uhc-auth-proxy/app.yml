---

$schema: /app-sre/app-1.yml

labels:
  service: uhc-auth-proxy
  platform: insights

name: uhc-auth-proxy

description: |
  This is an internal helper application that the 3Scale gateway uses to authenticate
  Openshift cluster telemetry applications that need to send data to cloud.redhat.com.

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Keith Walsh
  email: kwalsh@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/ci-int/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: uhc-auth-proxy
    public: false
    description: Repo for the uhc-auth-proxy Insights app

codeComponents:
- name: uhc-auth-proxy
  resource: upstream
  url: https://github.com/RedHatInsights/uhc-auth-proxy
