---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insights-legacy

name: insights-legacy-build-secret

description: 'secret to build with prodsec rules'

app:
  $ref: /services/insights/insights-legacy/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: secrets

config_path: /insights-stage/insights-legacy/secrets.yml
