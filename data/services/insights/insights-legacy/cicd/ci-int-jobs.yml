---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insights-legacy

name: ci-int-insights-legacy-jobs
description: insights-legacy ci-int jobs

app:
  $ref: /services/insights/insights-legacy/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: insights-legacy
    label: insights
    node: insights
    gl_group: insights-release-eng
    gl_project: insights-deployer
    quay_org: cloudservices
    jobs:
    - "insights-engine-gl-pr-check":
        display_name: insights-legacy pr-check
    - "insights-engine-gl-build-master":
        display_name: insights-legacy build-master
