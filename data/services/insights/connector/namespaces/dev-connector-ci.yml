---
$schema: /openshift/namespace-1.yml

labels:
  service: connector

name: connector-ci
description: Insights connector-ci on the DEV cluster under e2e-deploy/ocdeployer management

cluster:
  $ref: /openshift/insights/cluster-insights-dev.yml

app:
  $ref: /services/insights/connector/app.yml

environment:
  $ref: /products/insights/environments/ci.yml

managedRoles: true

openshiftResources:
- provider: vault-secret
  path: insights/quay/quay.io
  version: 3
  type: kubernetes.io/dockerconfigjson
  name: quay-cloudservices-pull
