---

$schema: /app-sre/app-1.yml

labels:
  service: insights-operator

name: insights-operator

description: insights-operator app on the Insights Platform

serviceOwners:
- name: CCX Team
  email: ccx@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: insights-operator-controller
    public: false
    description: Repo for the insights-operator Insights app

codeComponents:
- name: insights-operator-controller
  resource: upstream
  url: https://github.com/RedHatInsights/insights-operator-controller
