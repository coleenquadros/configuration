---
$schema: /app-interface/app-interface-sql-query-1.yml
labels: {}
name: 2020-10-26-check-removed-content-stage
namespace:
  $ref: /services/insights/automation-hub/namespaces/stage-automation-hub-stage.yml
identifier: automation-hub-stage
output: stdout
query: |
  SELECT namespace, name, version, certification
  FROM ansible_collectionversion
  WHERE content_ptr_id IN (
      SELECT content_id FROM core_repositorycontent WHERE version_removed_id IS NOT NULL);
