---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: hccm

name: ci-int-hccm-views

description: ''

app:
  $ref: /services/insights/hccm/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: hccm
    description: 'insights hccm view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[(hccm)\].*'
