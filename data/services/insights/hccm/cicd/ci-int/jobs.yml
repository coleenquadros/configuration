---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: hccm

name: ci-int-hccm-jobs

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: hccm-pg-post-update-actions
    label: hccm
    node: rhel7
    gl_group: insights-platform
    gl_project: pg-post-update-actions
    jobs:
    - 'gl-pr-check':
        display_name: hccm pg-post-update-actions run pr check
    - 'gl-build-master':
        display_name: hccm pg-post-update-actions build master branch

