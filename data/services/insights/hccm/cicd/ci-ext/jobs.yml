---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: ubi-presto

name: ci-ext-ubi-presto-jobs
description: ubi-presto ci-ext jobs

app:
  $ref: /services/insights/hccm/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: ubi-presto
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: ubi-presto
    quay_org: cloudservices
    jobs:
    - "gh-pr-check":
        display_name: ubi-presto pr-check
        branch: main
    - "gh-build-master":
        display_name: ubi-presto build-master
        branch: main
