---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: approval

name: ci-ext-approval-jobs
description: approval ci-ext jobs

app:
  $ref: /services/insights/approval/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: approval-api
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: approval-api
    quay_org: cloudservices
    jobs:
    - "insights-gh-pr-check":
        display_name: approval-api pr-check
    - "insights-gh-build-master":
        display_name: approval-api build-master
