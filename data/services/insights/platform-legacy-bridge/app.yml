---

$schema: /app-sre/app-1.yml

labels:
  service: platform-legacy-bridge
  platform: insights

name: platform-legacy-bridge

description: platform-legacy-bridge app on the Insights Platform

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Richard Brantley
  email: rbrantley@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/aws/service.yml
- $ref: /dependencies/ci-int/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: platform-legacy-bridge
    public: false
    description: Repo for the platform-legacy-bridge Insights app

codeComponents:
- name: platform-legacy-bridge
  resource: upstream
  url: https://github.com/RedHatInsights/platform-legacy-bridge
