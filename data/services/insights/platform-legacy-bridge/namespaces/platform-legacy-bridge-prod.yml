---
$schema: /openshift/namespace-1.yml

labels:
  service: platform-legacy-bridge

name: platform-legacy-bridge-prod
description: Insights platform-legacy-bridge-prod on the Prod cluster

cluster:
  $ref: /openshift/crcp01ue1/cluster.yml

quota:
  $ref: /services/insights/default-quota.yml

app:
  $ref: /services/insights/platform-legacy-bridge/app.yml

environment:
  $ref: /products/insights/environments/production.yml

networkPoliciesAllow:
- $ref: /services/observability/namespaces/openshift-customer-monitoring.crcp01ue1.yml

managedRoles: true


openshiftResources:
- provider: vault-secret
  path: insights/secrets/global/rh-registry-pull
  version: 2
  type: kubernetes.io/dockerconfigjson
- provider: vault-secret
  path: insights/quay/quay.io
  version: 3
  type: kubernetes.io/dockerconfigjson
  name: quay-cloudservices-pull
- provider: vault-secret
  path: insights/secrets/insights-prod/advisor-prod/platform-legacy-bridge
  version: 4


managedTerraformResources: true

terraformResources:
- provider: cloudwatch
  account: insights-prod
  identifier: platform-legacy-bridge-prod
  defaults: /terraform/resources/insights/production/cloudwatch/cloudwatch-1.yml
  output_resource_name: cloudwatch
  es_identifier: kibana-elasticsearch
