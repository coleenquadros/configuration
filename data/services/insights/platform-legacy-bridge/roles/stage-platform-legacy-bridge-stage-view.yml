---
$schema: /access/role-1.yml

labels:
  platform: insights
  service: platform-legacy-bridge

name: stage-platform-legacy-bridge-stage-view
description: View access to platform legacy bridge -STAGE namespace

permissions: []

access:
- namespace:
    $ref: /services/insights/platform-legacy-bridge/namespaces/stage-platform-legacy-bridge-stage.yml
  role: view
