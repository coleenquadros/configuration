---
$schema: /app-sre/saas-file-2.yml

labels:
  service: platform-legacy-bridge
  platform: insights

name: platform-legacy-bridge
description: platform-legacy-bridge app on the Insights Platform

app:
  $ref: /services/insights/platform-legacy-bridge/app.yml

pipelinesProvider:
  $ref: /services/insights/pipelines/tekton.crc-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: team-clouddot-info

managedResourceTypes:
- Deployment
- Service

imagePatterns:
- quay.io/cloudservices

authentication:
  image:
    path: insights/quay/cloudservices-push
    field: all

resourceTemplates:
- name: platform-legacy-bridge
  path: /templates/advisor/platform-legacy-bridge.yaml
  url: https://gitlab.cee.redhat.com/insights-platform/saas-templates
  targets:
  - namespace:
      $ref: /services/insights/platform-legacy-bridge/namespaces/stage-platform-legacy-bridge-stage.yml
    ref: 0a7f81f1d6fe8987d02cb04ec608e5cc2a3196d2
    parameters:
      CPU_LIMIT: '1'
      IMAGE_TAG: 0e964e0
      MEMORY_LIMIT: 1Gi
      MIN_REPLICAS: 1
      ENABLE_CLOUDWATCH: 'true'
      CW_LOG_GROUP: platform-legacy-bridge-stage
      CW_LOG_STREAM: platform-legacy-bridge-stage
      LOG_WRITE_DELAY: 500
  - namespace:
      $ref: /services/insights/platform-legacy-bridge/namespaces/platform-legacy-bridge-prod.yml
    ref: 0a7f81f1d6fe8987d02cb04ec608e5cc2a3196d2
    parameters:
      CPU_LIMIT: '1'
      IMAGE_TAG: 0e964e0
      MEMORY_LIMIT: 1Gi
      MIN_REPLICAS: 1
      ENABLE_CLOUDWATCH: 'true'
      CW_LOG_GROUP: platform-legacy-bridge-prod
      CW_LOG_STREAM: platform-legacy-bridge-prod
      LOG_WRITE_DELAY: 500
      LOG_ONLY: 'false'
      LEGACY_HEALTH_URL: 'https://access.redhat.com/r/insights'
