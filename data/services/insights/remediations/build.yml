---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: remediations

name: ci-ext-remediations-jobs
description: remediations ci-ext jobs

app:
  $ref: /services/insights/remediations/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: insights-remediations-frontend
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: insights-remediations-frontend
    quay_org: cloudservices
    jobs:
    - "insights-gh-pr-check":
        display_name: insights-remediations-frontend pr-check
    - "insights-gh-build-master":
        display_name: insights-remediations-frontend build-master