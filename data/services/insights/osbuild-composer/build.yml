---

$schema: /dependencies/jenkins-config-1.yml

# Labels are metadata useful for querying Qontract using GraphQL
labels:
  service: osbuild-composer
  platform: insights

name: ci-ext-insights-osbuild-composer

description: 'Job for building osbuild-composer'

app:
  $ref: /services/insights/osbuild-composer/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: osbuild-composer
    label: insights
    node: insights
    quay_org: cloudservices
    gh_org: osbuild
    gh_repo: osbuild-composer
    jobs:
      - gh-build-master:
          branch: main
          display_name: osbuild-composer build-main
          build_deploy_script_path: './tools/clouddot_build_deploy.sh'
