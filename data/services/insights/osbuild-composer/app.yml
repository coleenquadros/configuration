---
$schema: /app-sre/app-1.yml

labels:
  service: osbuild-composer
  platform: insights

name: osbuild-composer

description: osbuild composer app on the Insights Platform

architectureDocument: https://TODO
sopsUrl: https://gitlab.cee.redhat.com/TODO
grafanaUrl: https://TODO
onboardingStatus: BestEffort

parentApp:
  $ref: /services/insights/app.yml

escalationPolicy:
  $ref: /teams/placeholder-escalation.yml
serviceOwners:
- name: osbuilders
  email: osbuilders@redhat.com

dependencies:
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/aws/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: osbuild-composer
    public: true
    description: osbuild-composer on cloud.redhat.com

codeComponents:
- name: osbuild-composer
  resource: upstream
  url: https://github.com/osbuild/osbuild-composer
