---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insights-ingress

name: ci-int-insights-ingress

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: insights-ingress
    description: 'insights-ingress view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[(bsquizza-insights-ingress)\].*'  # for POC purposes
