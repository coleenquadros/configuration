---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: unleash-proxy

name: ci-int-unleash-proxy-jobs
description: unleash-proxy ci int jobs

app:
  $ref: /services/insights/unleash-proxy/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: unleash-proxy
    label: insights
    node: insights
    gl_group: automation-analytics
    gl_project: unleash-proxy
    quay_org: cloudservices
    jobs:
    - 'insights-gl-build-master':
        display_name: 'unleash-proxy gl-build-main'
        branch: main
    - 'insights-gl-pr-check':
        display_name: 'unleash-proxy gl-pr-check'
