---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: cloud-connector
  platform: insights

name: ci-ext-insights-cloud-connector

description: Build job for cloud-connector images

app:
  $ref: /services/insights/cloud-connector/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: cloud-connector
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: cloud-connector
    quay_org: cloudservices
    jobs:
    - "insights-gh-build-master":
        display_name: cloud-connector build-master
