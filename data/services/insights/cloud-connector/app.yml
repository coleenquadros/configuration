---

$schema: /app-sre/app-1.yml

labels:
  service: cloud-connector
  platform: insights

name: cloud-connector
description: |
  Cloud-Connector provides a secure network bridge between cloud.redhat.com and customer sites.
  This allows customers on cloud.redhat.com to manage Red Hat Products remotely from the cloud.

onboardingStatus: BestEffort

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Derek Horton
  email: dehort@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/aws/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: cloud-connector
    public: true
    description: Repo for the cloud-connector Insights app

codeComponents:
- name: cloud-connector
  resource: upstream
  url: https://github.com/RedHatInsights/cloud-connector
