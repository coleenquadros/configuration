---

$schema: /app-sre/app-1.yml

labels:
  service: system-baseline

name: system-baseline

description: system-baseline app on the Insights Platform

serviceOwners:
- name: Chris Duryee
  email: cduryee@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: system-baseline-backend
    public: false
    description: Repo for the system-baseline Insights app

codeComponents:
- name: system-baseline-backend
  resource: upstream
  url: https://github.com/RedHatInsights/system-baseline-backend
