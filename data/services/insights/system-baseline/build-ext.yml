---
$schema: /dependencies/jenkins-config-1.yml

# Labels are metadata useful for querying Qontract using GraphQL
labels:
  service: system-baseline
  platform: insights

name: ci-ext-insights-system-baseline
description: 'Job for building Insights system-baseline app'

app:
  $ref: /services/insights/system-baseline/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: system-baseline-backend
    label: insights
    node: insights
    quay_org: cloudservices
    gh_org: RedHatInsights
    gh_repo: system-baseline-backend
    jobs:
    - "insights-gh-pr-check":
        display_name: system-baseline-backend pr-check
    - "insights-gh-build-master":
        display_name: system-baseline-backend build-master
