---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: pes
  platform: insights

name: ci-ext-insights-pes

description: 'Job for buildin Package Evolution Service'

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: pes
    label: insights
    node: insights
    quay_org: cloudservices
    gl_group: tmeszaro
    gl_project: pes-cloud
    jobs:
    - gl-pr-check:
        display_name: pes pr-check
    - gl-build-master:
        display_name: pes build-master
