---
$schema: /app-sre/app-1.yml

labels:
  service: approval-api
  platform: insights

name: approval-api

description: |
  The first Insights Platform application migration POC.

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Insights Platform SRE
  email: gavin@redhat.com
- name: Brandon Squizzato
  email: bsquizza@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/aws/service.yml
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  # TODO: Remove the below repos, these are just in place for the POC so reconcile does not delete them.
  - name: kube-scripts
    description: ''
    public: true
  - name: miniop
    description: 'mini-operator for simple deployments'
    public: true

codeComponents:
- name: approval-api
  resource: upstream
  url: https://github.com/bsquizz/approval-api
- name: saas-approval
  resource: saasrepo
  url: https://github.com/bsquizz/saas-approval
