---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: catalog-tower-persister
  platform: insights

name: ci-ext-insights-catalog-tower-persister
description: catalog tower persister jobs

app:
  $ref: /services/insights/catalog-tower-persister/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: catalog-tower-persister
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: catalog_tower_persister
    quay_org: cloudservices
    jobs:
    - "insights-gh-pr-check":
        display_name: catalog-tower-persister pr-check
    - "insights-gh-build-master":
        display_name: catalog-tower-persister build-master
