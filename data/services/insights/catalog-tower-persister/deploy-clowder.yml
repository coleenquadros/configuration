---
$schema: /app-sre/saas-file-2.yml

labels:
  service: catalog-tower-persister
  platform: insights

name: catalog-tower-persister-clowder
description: catalog-tower-persister app on the Insights Platform

app:
  $ref: /services/insights/catalog-tower-persister/app.yml

pipelinesProvider:
  $ref: /services/insights/pipelines/tekton.crc-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/redhat-internal.yml
  channel: team-clouddot-info

managedResourceTypes:
  - ClowdApp

imagePatterns:
  - quay.io/cloudservices

authentication:
  image:
    path: insights/quay/cloudservices-push
    field: all
    version: 3

parameters:
  CLOWDER_ENABLED: "true"

resourceTemplates:
  - name: catalog_tower_persister
    path: /deploy/clowdapp.yaml
    url: https://github.com/RedHatInsights/catalog_tower_persister
    targets:
      - namespace:
          $ref: /services/insights/catalog-tower-persister/namespaces/catalog-tower-persister-prod.yml
        delete: true
        ref: a99c56e6c0a8c01aa7dfbcca553289620ad87c05
        parameters:
          ENV_NAME: prod
          MIN_REPLICAS: 3
      - namespace:
          $ref: /services/insights/catalog-tower-persister/namespaces/stage-catalog-tower-persister-stage.yml
        delete: true
        ref: main
        upstream:
          instance:
            $ref: /dependencies/ci-ext/ci-ext.yml
          name: RedHatInsights-catalog_tower_persister-gh-build-main
        parameters:
          ENV_NAME: stage
          MIN_REPLICAS: 3
      - namespace:
          $ref: /services/insights/ephemeral/namespaces/ephemeral-base.yml
        disable: true
        ref: internal
        parameters:
          LOG_LEVEL: WARN
          MIN_REPLICAS: 1
