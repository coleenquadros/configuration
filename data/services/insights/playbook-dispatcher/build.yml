---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: playbook-dispatcher
  platform: insights

name: ci-ext-insights-playbook-dispatcher

description: Build job for playbook-dispatcher images

app:
  $ref: /services/insights/playbook-dispatcher/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: playbook-artifact-validator
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: playbook-artifact-validator
    quay_org: cloudservices
    jobs:
    - gh-build-master:
        display_name: playbook-artifact-validator build-master
