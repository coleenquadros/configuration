---
$schema: /app-interface/app-interface-sql-query-1.yml
labels: {}
name: 2021-04-06-replication-slots
namespace:
  $ref: /services/insights/playbook-dispatcher/namespaces/stage.yml
identifier: playbook-dispatcher-stage
output: stdout
queries:
  - SELECT slot_name, plugin, slot_type, datoid, database, temporary, active, active_pid, xmin, catalog_xmin, restart_lsn, confirmed_flush_lsn FROM pg_replication_slots;
  - SELECT id, account, service, url FROM runs LIMIT 50;
