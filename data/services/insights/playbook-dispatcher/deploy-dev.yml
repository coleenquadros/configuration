---
$schema: /app-sre/saas-file-2.yml

labels:
  service: playbook-dispatcher
  platform: insights

name: playbook-dispatcher-dev
description: The Playbook Dispatcher service enables other services to run Ansible Playbooks on connected hosts

app:
  $ref: /services/insights/playbook-dispatcher/app.yml

pipelinesProvider:
  $ref: /services/insights/pipelines/tekton.crc-pipelines.app-sre-prod-01.yaml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: team-clouddot-info

managedResourceTypes:
- Deployment
- Service
- PersistentVolumeClaim
- CronJob

imagePatterns:
- quay.io/cloudservices
- quay.io/debezium

authentication:
  image:
    path: insights/secrets/global/rh-registry-pull
    field: all

resourceTemplates:
- name: playbook-dispatcher-dev
  path: /deploy/template.yaml
  url: https://github.com/RedHatInsights/playbook-dispatcher
  targets:
  - namespace:
      $ref: /services/insights/playbook-dispatcher/namespaces/ci.yml
    ref: master
    upstream:
      instance:
        $ref: /dependencies/ci-ext/ci-ext.yml
      name: RedHatInsights-playbook-dispatcher-gh-build-master
    parameters:
      CLOUD_CONNECTOR_HOST: cloud-connector.connector-ci.svc.cluster.local
      RETURN_URL: "https://ci.cloud.redhat.com/api/ingress/v1/upload"
      RBAC_IMPL: mock

  - namespace:
      $ref: /services/insights/playbook-dispatcher/namespaces/qa.yml
    ref: master
    upstream:
      instance:
        $ref: /dependencies/ci-ext/ci-ext.yml
      name: RedHatInsights-playbook-dispatcher-gh-build-master
    parameters:
      CLOUD_CONNECTOR_HOST: cloud-connector.connector-qa.svc.cluster.local
      RETURN_URL: "https://qa.cloud.redhat.com/api/ingress/v1/upload"

- name: playbook-dispatcher-connect-dev
  path: /deploy/connect-dev.yaml
  url: https://github.com/RedHatInsights/playbook-dispatcher
  targets:
  - namespace:
      $ref: /services/insights/playbook-dispatcher/namespaces/ci.yml
    ref: master
    upstream:
      instance:
        $ref: /dependencies/ci-ext/ci-ext.yml
      name: RedHatInsights-playbook-dispatcher-gh-build-master
  - namespace:
      $ref: /services/insights/playbook-dispatcher/namespaces/qa.yml
    ref: master
    upstream:
      instance:
        $ref: /dependencies/ci-ext/ci-ext.yml
      name: RedHatInsights-playbook-dispatcher-gh-build-master
