---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: rhsm
  platform: insights

name: ci-int-insights-rhsm-splunk-forwarder

description: 'rhsm ci-int jobs'

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: splunk-forwarder
    label: insights
    node: insights
    gl_group: rhsm
    gl_project: splunk-forwarder
    quay_org: cloudservices
    jobs:
    - gl-pr-check:
        display_name: rhsm splunk-forwarder pr-check
    - gl-build-master:
        display_name: rhsm splunk-forwarder build-master
- project:
    name: rhsm-subscriptions
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: rhsm-subscriptions
    quay_org: cloudservices
    jobs:
    - "gh-build-master":
        display_name: rhsm-subscriptions build-master
- project:
    name: rhsm-subscriptions-egress
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: rhsm-subscriptions-egress
    quay_org: cloudservices
    jobs:
    - "gh-build-master":
        display_name: rhsm-subscriptions-egress build-master
- project:
    name: rhsm-conduit
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: rhsm-conduit
    quay_org: cloudservices
    jobs:
    - "gh-build-master":
        display_name: rhsm-conduit build-master
