---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: rhsm
  platform: insights

name: ci-int-insights-rhsm-splunk-forwarder

description: 'splunk forwarder sidecar for rhsm'

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: splunk-forwarder
    label: insights
    node: insights
    gl_group: rhsm
    gl_project: splunk-forwarder
    quay_org: cloudservices
    jobs:
    - gl-pr-check:
        display_name: rhsm splunk-forwarder pr-check
    - gl-build-master:
        display_name: rhsm splunk-forwarder build-master
