---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: subscription-inventory-ui

name: ci-ext-subscription-inventory-ui-jobs
description: subscription-inventory-ui ci-ext jobs

app:
  $ref: /services/insights/rhsm/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: subscription-inventory-ui
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: subscription-inventory-ui
    quay_org: cloudservices
    jobs:
    - "insights-gh-pr-check":
        display_name: subscription-inventory-ui pr-check
    - "insights-gh-build-master":
        display_name: subscription-inventory-ui build-master
