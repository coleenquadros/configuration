---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insights-core-kafka

name: ci-int-insights-core-kafka-jobs

description: 'insights-core-kafka ci-int jobs'

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: insights-core-kafka
    label: insights-core-kafka
    node: insights
    gl_group: insights-platform
    gl_project: insights-core-kafka
    quay_org: cloudservices
    kube_cluster: insights
    jobs:
    - gl-pr-check:
        display_name: pr-check
    - gl-build-master:
        display_name: build-master
