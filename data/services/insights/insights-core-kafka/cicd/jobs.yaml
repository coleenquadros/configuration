---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insights-core-kafka

name: ci-ext-insights-core-kafka-jobs

description: 'insights-core-kafka ci-ext jobs'

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: insights-core-kafka
    label: insights-core-kafka
    node: app-sre
    gh_org: redhatinsights
    gh_repo: insights-core-kafka
    quay_org: cloudservices
    kube_cluster: insights
    jobs:
    - 'gh-pr-check':
        display_name: pr-check
    - 'gh-build-master':
        display_name: build-master
