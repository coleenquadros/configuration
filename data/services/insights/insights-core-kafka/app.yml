---
$schema: /app-sre/app-1.yml

labels:
  service: insights-core-kafka

name: insights-core-kafka

description: |
  Frontend for insights-core that consumes archives from a topic and produces results to another topic

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Kyle Lape
  email: klape@redhat.com
- name: Jesse Jaggars
  email: jjaggars@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/ci-int/service.yml
- $ref: /dependencies/gitlab/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: insights-core-kafka
    description: 'Frontend for insights-core that consumes archives from a topic and produces results to another topic'
    public: false

codeComponents:
- name: gitlab
  resource: upstream
  url: https://gitlab.cee.redhat.com/insights-platform/insights-core-kafka
