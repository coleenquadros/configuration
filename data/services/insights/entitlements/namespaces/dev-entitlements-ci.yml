---
$schema: /openshift/namespace-1.yml

labels:
  service: entitlements

name: entitlements-ci
description: Insights Entitlements CI on DEV cluster under e2e-deploy management

cluster:
  $ref: /openshift/insights/cluster-insights-dev.yml

app:
  $ref: /services/insights/entitlements/app.yml

environment:
  $ref: /products/insights/environments/ci.yml

managedRoles: true
