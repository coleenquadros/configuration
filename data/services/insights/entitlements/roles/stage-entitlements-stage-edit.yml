---
$schema: /access/role-1.yml

labels:
  platform: insights
  service: entitlements

name: stage-entitlements-stage-edit
description: Edit access to entitlements -STAGE namespace

permissions: []

access:
- namespace:
    $ref: /services/insights/entitlements/namespaces/stage-entitlements-stage.yml
  role: edit
