---
$schema: /access/role-1.yml

labels:
  platform: insights
  service: entitlements

name: dev-entitlements-ci-edit
description: Edit access to entitlements -CI namespace

permissions: []

access:
- namespace:
    $ref: /services/insights/entitlements/namespaces/dev-entitlements-ci.yml
  role: edit
