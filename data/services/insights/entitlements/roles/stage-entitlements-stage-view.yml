---
$schema: /access/role-1.yml

labels:
  platform: insights
  service: entitlements

name: stage-entitlements-stage-view
description: View access to entitlements -STAGE namespace

permissions: []

access:
- namespace:
    $ref: /services/insights/entitlements/namespaces/stage18-entitlements-stage.yml
  role: view
