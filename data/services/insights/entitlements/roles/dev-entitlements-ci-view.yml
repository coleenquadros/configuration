---
$schema: /access/role-1.yml

labels:
  platform: insights
  service: entitlements

name: dev-entitlements-ci-view
description: View access to entitlements -CI namespace

permissions: []

access:
- namespace:
    $ref: /services/insights/entitlements/namespaces/dev-entitlements-ci.yml
  role: view
