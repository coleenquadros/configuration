---
$schema: /access/role-1.yml

labels:
  platform: insights
  service: entitlements

name: prod-entitlements-prod-view
description: View access to entitlements -PROD namespace

permissions: []

access:
- namespace:
    $ref: /services/insights/entitlements/namespaces/prod-entitlements-prod.yml
  role: view
