---
$schema: /app-sre/app-1.yml

labels:
  service: entitlements
  platform: insights
  type: core

name: entitlements

description: |
  This is a helper service used to query and process entitlements
  that a given tenant account has in the Red HAT subscription service.

architectureDocument: https://TODO
sopsUrl: https://gitlab.cee.redhat.com/service/app-interface/-/tree/master/docs/console.redhat.com/app-sops/entitlements
grafanaUrl: https://grafana.app-sre.devshift.net/d/0fmN7EWGz/platform-health?orgId=1
onboardingStatus: OnBoarded

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Chris Moore
  email: cmoore@redhat.com
- name: Keith Walsh
  email: kwalsh@redhat.com

escalationPolicy:
  $ref: /teams/insights/escalation-policies/crc-entitlements-escalations.yml

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/aws/service.yml
- $ref: /dependencies/ci-int/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: entitlements-api-go
    public: false
    description: Repo for the entitlements Insights app
  - name: entitlements-config-cronjob
    public: false
    description: Repo for the entitlements Insights app

codeComponents:
- name: entitlements-api-go
  resource: upstream
  url: https://github.com/RedHatInsights/entitlements-api-go

sentryProjects:
- team:
    $ref: /dependencies/sentry/teams/insights-entitlements-stage.yml
  projects:
  - name: insights-entitlements-stage
    description: "insights entitlements service in stage"
    email_prefix: "[insights-entitlements stage]"
    platform: go
- team:
    $ref: /dependencies/sentry/teams/insights-entitlements-production.yml
  projects:
  - name: insights-entitlements-prod
    description: "insights entitlements service in production"
    email_prefix: "[insights-entitlements production]"
    platform: go
