---
$schema: /app-sre/app-1.yml

labels:
  service: entitlements
  platform: insights
  type: core

name: entitlements

description: |
  This is a helper service used to query and process entitlements
  that a given tenant account has in the Red Hat subscription service.

architectureDocument: https://TODO
sopsUrl: https://gitlab.cee.redhat.com/service/app-interface/-/tree/master/docs/console.redhat.com/app-sops/entitlements
grafanaUrls:
- title: Platform Health
  url: https://grafana.app-sre.devshift.net/d/0fmN7EWGz/platform-health?orgId=1
onboardingStatus: OnBoarded

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Access & Management team
  email: platform-accessmanagement@redhat.com

escalationPolicy:
  $ref: /teams/insights/escalation-policies/crc-entitlements-escalations.yml

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/aws/service.yml
- $ref: /dependencies/ci-ext/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: entitlements-api-go
    public: false
    description: Repo for the entitlements Insights app
  - name: entitlements-config-cronjob
    public: false
    description: Repo for the entitlements Insights app

codeComponents:
- name: entitlements-api-go
  resource: upstream
  url: https://github.com/RedHatInsights/entitlements-api-go
- name: entitlements-config
  resource: upstream
  url: https://github.com/RedHatInsights/entitlements-config
