---

$schema: /app-sre/app-1.yml

labels:
  service: slo-report 
  platform: insights

name: slo-report

description: slo-report app on the Insights Platform

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Blake Holifield
  email: bholifie@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/ci-int/service.yml
- $ref: /dependencies/gitlab/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: slo-report
    public: false
    description: SLO report email that launches each Monday 

codeComponents:
- name: slo-report
  resource: upstream
  url: https://gitlab.cee.redhat.com/insights-platform/devprod-ops

