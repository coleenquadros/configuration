---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: slo-report
  platform: insights

name: ci-int-slo-report

description: 'Builds the container for the weekly SLO report'

app:
  $ref: /services/insights/slo-report/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: slo-report
    label: insights
    node: insights
    gl_group: insights-platform
    gl_project: devprod-ops
    build_deploy_script_path: ./apps/slo-report/build_deploy.sh
    quay_org: cloudservices
    jobs:
    - "insights-gl-build-master":
        display_name: slo-report build-master

