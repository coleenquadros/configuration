---

$schema: /app-sre/app-1.yml

labels:
  service: haberdasher
  platform: insights

name: haberdasher

description: A PID1 wrapper command for Insights Platform constainers

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Chris Mitchell
  email: cmitchel@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml

quayRepos:
  - org:
      $ref: /dependencies/quay/cloudservices.yml
    items:
    - name: logstash
      public: false
      description: Repo for platform log aggregator

codeComponents:
- name: haberdasher
  resource: upstream
  url: https://github.com/RedHatInsights/haberdasher
