---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: haberdasher
  platform: insights

name: ci-ext-insights-haberdasher

description: 'Haberdasher build'

app:
  $ref: /services/insights/haberdasher/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: haberdasher
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: haberdasher
    quay_org: cloudservices
    jobs:
    - gh-build-master:
        display_name: haberdasher build-master
