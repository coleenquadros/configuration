---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: strimzi
  platform: insights

name: ci-int-insights-kafka-connect-jobs

description: Build job for kafka-connect images

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: kafka-connect
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: platform-mq
    quay_org: cloudservices
    jobs:
    - gh-build-master:
        display_name: kafka-connect build-master
