---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insights

name: insights-strimzi-views

description: ''

app:
  $ref: /services/insights/strimzi/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: insights-strimzi
    description: 'insights strimzi view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[prometheus-kafka-consumer-group-exporter\].*'
