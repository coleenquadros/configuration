---
$schema: /access/role-1.yml

labels:
  platform: insights
  service: kibana

name: dev-kibana-view
description: View access to kibana  namespace

permissions: []

access:
- namespace:
    $ref: /services/insights/kibana/namespaces/dev-kibana.yml
  role: view
