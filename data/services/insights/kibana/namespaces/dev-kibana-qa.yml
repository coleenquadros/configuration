---
$schema: /openshift/namespace-1.yml

labels:
  service: kibana

name: kibana-qa
description: Insights Kibana QA on the DEV cluster

cluster:
  $ref: /openshift/insights/cluster-insights-dev.yml

app:
  $ref: /services/insights/kibana/app.yml

environment:
  $ref: /products/insights/environments/qa.yml

managedRoles: true

