---
$schema: /openshift/namespace-1.yml

labels:
  service: kibana

name: kibana
description: Insights Kibana on the Stage cluster

cluster:
  $ref: /openshift/insights/cluster-insights-stage.yml

app:
  $ref: /services/insights/kibana/app.yml

managedRoles: true

