---
$schema: /openshift/namespace-1.yml

labels:
  service: kibana

name: kibana-dev
description: Insights Kibana on the DEV cluster

cluster:
  $ref: /openshift/insights/cluster-insights-dev.yml

app:
  $ref: /services/insights/kibana/app.yml

environment:
  $ref: /products/insights/environments/dev.yml

managedRoles: true

openshiftResources:
- provider: vault-secret
  path: insights/secrets/insights-dev/kibana/es-cleanup-secret 
  version: 1
