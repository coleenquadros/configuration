---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: kibana

name: ci-int-kibana-jobs
description: kibana ci-int jobs

app:
  $ref: /services/insights/kibana/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: es-cleanup
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: es-cleanup
    quay_org: cloudservices
    jobs:
    - "insights-gh-build-master":
        display_name: es-cleanup build-main
    - "insights-gh-pr-check":
        display_name: es-cleanup pr-check
