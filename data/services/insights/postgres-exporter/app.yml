---

$schema: /app-sre/app-1.yml

labels:
  service: postgres-exporter

name: postgres-exporter

description: postgres-exporter app on the Insights Platform

serviceOwners:
- name: Kyle Lape
  email: klape@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/aws/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: postgresql-exporter
    public: false
    description: Repo for the postgres-exporter Insights app

codeComponents:
- name: koku
  resource: upstream
  url: https://github.com/project-koku/koku
