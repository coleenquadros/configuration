---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: apicast-test-services

name: ci-ext-insights-engine-jobs

description: Build job for apicast-test-services

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: apicast-test-services
    label: apicast-test-services
    node: insights
    gh_group: RedHatInsights
    gh_project: apicast-test-services
    jobs:
    - gh-pr-check:
        display_name: pr-check
    - gh-build-master:
        display_name: build-master
