---
$schema: /openshift/namespace-1.yml

labels:
  service: gateway

name: apicast-test-services
description: Basic web service used to test 3scale gateway functionality

cluster:
  $ref: /openshift/insights/crc-stage-01.yml

app:
  $ref: /services/insights/gateway/app.yml

environment:
  $ref: /products/insights/environments/stage.yml

networkPoliciesAllow:
- $ref: /services/observability/namespaces/openshift-customer-monitoring.crc-stage-01.yml

managedRoles: true

managedResourceTypes:
- Secret

openshiftResources:
- provider: vault-secret
  path: insights/quay/quay.io
  version: 3
  type: kubernetes.io/dockerconfigjson
  name: quay-cloudservices-pull
