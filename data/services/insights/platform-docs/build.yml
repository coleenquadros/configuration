---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: platform-docs
  platform: insights

name: ci-int-insights-platform-docs

description: 'Platform docs build'

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: platform-docs
    label: insights
    node: insights
    gl_group: insights-plaform
    gl_project: platform-docs-build
    quay_org: cloudservices
    jobs:
    - gl-build-master:
        display_name: platform-docs build-master
- project:
    name: aletheia
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: aletheia
    quay_org: cloudservices
    jobs:
    - gh-pr-check:
        display_name: aletheia pr-check
    - gh-build-master:
        display_name: aletheia build-master

