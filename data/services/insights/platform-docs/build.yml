---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: platform-docs
  platform: insights

name: ci-int-insights-platform-docs

description: 'Platform docs build'

app:
  $ref: /services/insights/platform-docs/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: platform-docs-gh
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: platform-docs
    quay_org: cloudservices
    jobs:
    - gh-build-master:
        display_name: platform-docs-gh build-master
- project:
    name: aletheia
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: aletheia
    quay_org: cloudservices
    jobs:
    - gh-pr-check:
        display_name: aletheia pr-check
    - gh-build-master:
        display_name: aletheia build-master
