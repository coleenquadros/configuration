---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insights-engine

name: ci-int-insights-engine-jobs

description: ''

app:
  $ref: /services/insights/engine/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: insights-engine-ci
    label: insights
    node: insights
    gl_group: insights-platform
    gl_project: insights-engine
    quay_org: cloudservices
    jobs:
    - gl-pr-check:
        display_name: insights-engine-ci pr-check
    - gl-build-master:
        display_name: insights-engine-ci build-master
