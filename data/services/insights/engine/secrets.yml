---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insights-engine

name: insights-engine-build-secret

description: 'secret to build with prodsec rules'

app:
  $ref: /services/insights/engine/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: secrets

config_path: /insights-stage/engine-stage/secrets.yml
