---
$schema: /access/role-1.yml

labels:
  platform: insights
  service: engine

name: dev-engine-ci-view
description: View access to engine -CI namespace

permissions: []

access:
- namespace:
    $ref: /services/insights/engine/namespaces/dev-engine-ci.yml
  role: view
