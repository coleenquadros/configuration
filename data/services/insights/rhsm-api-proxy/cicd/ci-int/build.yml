---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: rhsm-api-proxy
  platform: insights

name: ci-int-insights-rhsm-splunk-forwarder

description: 'rhsm ci-int jobs'

app:
  $ref: /services/insights/rhsm-api-proxy/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: rhsm-auto-registration-listener
    label: insights
    node: insights
    gl_group: rhsm
    gl_project: automatic-registration
    quay_org: cloudservices
    jobs:
    - gl-build-master:
        display_name: rhsm auto-registration-listener build-main
        branch: master
