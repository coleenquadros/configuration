---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: rhsm-api-proxy
  platform: insights

name: ci-int-insights-rhsm-api-proxy

description: 'rhsm-api-proxy ci-int jobs'

app:
  $ref: services/insights/rhsm-api-proxy/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: rhsm-api-proxy
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: rhsm-api-proxy
    quay_org: cloudservices
    jobs:
    - "insights-gh-build-master":
        display_name: rhsm-api-proxy caddy build-main
        branch: main
    - "insights-gh-pr-check":
        display_name: rhsm-api-proxy caddy pr-check
