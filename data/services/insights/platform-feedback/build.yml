---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: platform-feedback

name: ci-int-platform-feedback-jobs

description: 'image build for platform-feedback'

app:
  $ref: /services/insights/platform-feedback/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: platform-feedback
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: hermod
    quay_org: cloudservices
    jobs:
    - gh-build-master:
        display_name: platform-feedback build-master

