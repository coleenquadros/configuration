---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: pes-app
  platform: insights

name: ci-ext-insights-pes-app

description: 'Job for buildin Package Evolution Service'

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: pes-app
    label: insights
    node: insights
    quay_org: cloudservices
    gl_group: insights-platform
    gl_project: pes-app
    jobs:
    - gl-pr-check:
        display_name: pes-app pr-check
    - gl-build-master:
        display_name: pes-app build-master
