---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: platform-logging

name: ci-int-insights-platform-logging-jobs

description: Build job for platform-logging images

app:
  $ref: /services/insights/platform-logging/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: platform-logging
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: platform-logging
    quay_org: cloudservices
    jobs:
    - "insights-gh-build-master":
        display_name: platform-logging build-master
- project:
    name: platform-logger
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: platform-logger
    quay_org: cloudservices
    jobs:
    - "insights-gh-build-master":
        display_name: platform-logger build-master
