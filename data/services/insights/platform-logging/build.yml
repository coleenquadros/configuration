---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: platform-logging
  platform: insights

name: ci-ext-insights-platform-logging

description: 'platform-logging build'

app:
  $ref: /services/insights/haberdasher/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: platform-logging
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: platform-logging
    quay_org: cloudservices
    jobs:
    - gh-build-master:
        display_name: platform-logging build-master
