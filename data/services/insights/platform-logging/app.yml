---

$schema: /app-sre/app-1.yml

labels:
  service: platform-logging
  platform: insights

name: platform-logging

description: A log aggregator service for Platform logs

onboardingStatus: BestEffort

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Chris Mitchell
  email: cmitchel@redhat.com

dependencies:
- $ref: /dependencies/ci-int/service.yml
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/aws/service.yml
- $ref: /dependencies/quay/service.yml

quayRepos:
  - org:
      $ref: /dependencies/quay/cloudservices.yml
    items:
    - name: logstash
      public: false
      description: Repo for platform log aggregator


codeComponents:
- name: platform-logging
  resource: upstream
  url: https://github.com/RedHatInsights/platform-logging
