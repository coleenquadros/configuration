---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: advisor

name: ci-int-advisor-jobs

description: 'advisor ci-int jobs'

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: advisor-api
    label: advisor-api
    node: insights
    gl_group: insights-platform
    gl_project: advisor-api
    quay_org: cloudservices
    kube_cluster: insights
    kube_namespace: advisor-sre-ci
    saasherder_context: advisor
    saasherder_services: api
    jobs:
    - gl-pr-check:
        display_name: pr-check
    - gl-build-master:
        display_name: build-master
- project:
    name: advisor-service
    label: advisor-service
    node: insights
    gl_group: insights-platform
    gl_project: advisor-service
    quay_org: cloudservices
    kube_cluster: insights
    kube_namespace: advisor-sre-ci
    saasherder_context: advisor
    saasherder_services: service
    jobs:
    - gl-pr-check:
        display_name: pr-check
    - gl-build-master:
        display_name: build-master
