---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insights-core-engine

name: ci-int-insights-core-engine-jobs

description: 'insights-core-engine ci-int jobs'

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: insights-core-engine
    label: insights-core-engine
    node: app-sre
    gl_org: insights-platform
    gl_repo: insights-core-engine
    quay_org: cloudservices
    jobs:
    - 'gh-pr-check':
        display_name: pr-check
    - 'gh-build-master':
        display_name: build-master
