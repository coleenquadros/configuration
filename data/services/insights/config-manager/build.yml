---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: config-manager
  platform: insights

name: ci-ext-insights-config-manager

description: Build job for config-manager images

app:
  $ref: /services/insights/config-manager/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: config-manager
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: config-manager
    quay_org: cloudservices
    jobs:
    - gh-build-master:
        display_name: config-manager build-master
    - insights-gh-pr-check:
        display_name: config-manager pr-check
