---
$schema: /app-sre/app-1.yml

labels:
  service: config-manager
  platform: insights

name: config-manager
description: The Config Manager service allows users to set a service configuration
  for all hosts connected through Red Hat Connector

architectureDocument: https://TODO
sopsUrl: https://gitlab.cee.redhat.com/TODO
grafanaUrls:
- title: TODOREPLACE
  url: https://TODO
onboardingStatus: BestEffort

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Link Dupont
  email: ldupont@redhat.com

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/aws/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: config-manager
    public: true
    description: The Config Manager service allows users to set a service configuration
      for all hosts connected through Red Hat Connector

codeComponents:
- name: config-manager
  resource: upstream
  url: https://github.com/RedHatInsights/config-manager

escalationPolicy:
  $ref: /teams/insights/escalation-policies/crc-config-manager-escalations.yml
