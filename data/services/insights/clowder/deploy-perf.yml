---
$schema: /app-sre/saas-file-2.yml

labels:
  service: clowder
  platform: insights

name: clowder-perf
description: Deploy clowder OLM resources

app:
  $ref: /services/insights/clowder/app.yml

pipelinesProvider:
  $ref: /services/insights/pipelines/tekton.crc-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: team-clouddot-info

managedResourceTypes:
- CustomResourceDefinition
- Deployment
- Service
- ClusterRole
- Role
- ConfigMap
- ServiceAccount
- RoleBinding
- ClusterRoleBinding
- ValidatingWebhookConfiguration
- ConsolePlugin

authentication:
  image:
    path: insights/quay/cloudservices-push
    field: all
    version: 3
  code:
    path: app-sre/creds/github-app-sre-bot
    field: openshift-saas-deploy

imagePatterns:
- quay.io/cloudservices/clowder

resourceTemplates:
- name: clowder-non-olm
  path: /deploy.yml
  url: https://github.com/RedHatInsights/clowder
  targets:
  - namespace:
      $ref: /services/insights/clowder/namespaces/perf-clowder-perf.yml
    ref: master
    upstream:
      instance:
        $ref: /dependencies/ci-ext/ci-ext.yml
      name: RedHatInsights-clowder-gh-build-catalog-master-upstream-RedHatInsights-clowder-gh-build-master
    parameters:
      WATCH_STRIMZI_RESOURCES: "false"
      CREATE_SERVICE_MONITORS: "true"
      TARGET_NAMESPACE: "clowder-system"

deployResources:
  requests:
    cpu: "1"
    memory: 2000Mi
  limits:
    cpu: "1"
    memory: 2000Mi

clusterAdmin: true
compare: false
