---
$schema: /openshift/namespace-1.yml

labels:
  service: commit-tracker
  platform: insights

name: commit-tracker-ci
description: commit-tracker namespace on insights ci cluster

cluster:
  $ref: /openshift/insights/cluster-insights-dev.yml

app:
  $ref: /services/insights/commit-tracker/app.yml

environment:
  $ref: /products/insights/environments/ci.yml

managedRoles: true

openshiftResources:
- provider: vault-secret
  path: insights/quay/quay.io
  version: 3
  type: kubernetes.io/dockerconfigjson
  name: quay-cloudservices-pull
