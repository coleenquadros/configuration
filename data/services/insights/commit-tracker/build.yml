---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: commit-tracker
  platform: insights

name: ci-int-insights-commit-tracker

description: ''

app:
  $ref: /services/insights/commit-tracker/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: commit-tracker
    label: insights
    node: insights
    gl_group: insights-platform
    gl_project: commit-tracker-go
    quay_org: cloudservices
    jobs:
    - gl-build-master:
        display_name: commit-tracker build-master
- project:
    name: commit-tracker-gh
    label: insights
    node: insights
    gl_group: insights-platform
    gl_project: commit-tracker-gh
    quay_org: cloudservices
    jobs:
    - gl-build-master:
        display_name: commit-tracker-gh build-master
