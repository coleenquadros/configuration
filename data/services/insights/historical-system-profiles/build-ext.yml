---
$schema: /dependencies/jenkins-config-1.yml

# Labels are metadata useful for querying Qontract using GraphQL
labels:
  service: historical-system-profiles
  platform: insights

name: ci-ext-insights-historical-system-profiles
description: 'Job for building Insights historical-system-profiles app'

app:
  $ref: /services/insights/historical-system-profiles/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: historical-system-profiles-backend
    label: rhel8
    node: jenkins-slave-09-rhel8
    quay_org: cloudservices
    gh_org: RedHatInsights
    gh_repo: historical-system-profiles-backend
    jobs:
    - "insights-gh-pr-check":
        display_name: historical-system-profiles--backend pr-check
    - "insights-gh-build-master":
        display_name: historical-system-profiles-backend build-master
