---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: idp-configs

name: ci-ext-idp-configs-jobs
description: idp-configs ci-ext jobs

app:
  $ref: /services/insights/idp-configs/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: idp-configs-api
    label: insights
    node: insights
    gh_org: identitatem
    gh_repo: idp-configs-api
    quay_org: cloudservices
    jobs:
    - "insights-gh-build-master":
        branch: main
        display_name: idp-config-api build-main
    - "insights-gh-pr-check":
        branch: main
        display_name: idp-config-api pr-check
