---

$schema: /app-sre/app-1.yml

labels:
  service: rbac
  platform: insights
  type: core

name: rbac

description: |
  The RBAC service allows customers to manage and apply role based controls for applications running on cloud.redhat.com.
  It provides -
  * UI and API for creating groups and roles and assigning them to users
  * API for internal applications to query access permissions assigned to given user at runtime


parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Christopher Moore
  email: cmoore@redhat.com
- name: Keith Walsh
  email: kwalsh@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/aws/service.yml
- $ref: /dependencies/ci-int/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: rbac
    public: false
    description: Repo for the rbac Insights app
  - name: rbac-config-cronjob
    public: false
    description: Repo for the rbac Insights app
  - name: rbac-scheduler
    public: false
    description: Repo for the rbac Insights app
  - name: rbac-worker
    public: false
    description: Repo for the rbac Insights app

codeComponents:
- name: insights-rbac
  resource: upstream
  url: https://github.com/RedHatInsights/insights-rbac
