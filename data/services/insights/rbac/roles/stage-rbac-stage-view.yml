---
$schema: /access/role-1.yml

labels:
  platform: insights
  service: rbac

name: stage-rbac-stage-view
description: View access to rbac -STAGE namespace

permissions: []

access:
- namespace:
    $ref: /services/insights/rbac/namespaces/stage-rbac-stage.yml
  role: view
