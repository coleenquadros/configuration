---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: catalog-inventory
  platform: insights

name: ci-ext-insights-catalog-inventory
description: catalog inventory jobs

app:
  $ref: /services/insights/catalog-inventory/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: catalog-inventory
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: catalog_inventory-api
    quay_org: cloudservices
    jobs:
    - "insights-gh-pr-check":
        display_name: catalog-inventory pr-check
    - "insights-gh-build-master":
        display_name: catalog-inventory build-master
