---
$schema: /app-sre/saas-file-1.yml

labels:
  service: turnpike
  platform: insights

name: turnpike-redis
description: Turnpike API Gateway (Redis deploy)

app:
  $ref: /services/insights/turnpike/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: team-clouddot-info

managedResourceTypes:
- Deployment
- PersistentVolumeClaim
- Service

imagePatterns:
- registry.redhat.io/rhel8

authentication:
  image:
    path: insights/secrets/global/rh-registry-pull
    field: all

resourceTemplates:
- name: redis
  path: /templates/redis.yml
  url: https://github.com/RedHatInsights/turnpike
  targets:
  - namespace:
      $ref: /services/insights/turnpike/namespaces/stage-turnpike-stage.yml
    ref: master
    parameters:
      IMAGE_TAG: 1-91
  - namespace:
      $ref: /services/insights/turnpike/namespaces/prod-turnpike-prod.yml
    ref: master
    parameters:
      IMAGE_TAG: 1-91
