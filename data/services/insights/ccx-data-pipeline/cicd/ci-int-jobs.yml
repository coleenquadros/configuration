---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: ccx-data-pipeline

name: ci-int-ccx-jobs
description: ccx ci-int jobs

app:
  $ref: /services/insights/ccx-data-pipeline/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: ccx-data-pipeline
    label: insights
    node: insights
    gl_group: ccx
    gl_project: ccx-data-pipeline
    quay_org: cloudservices
    jobs:
    - "insights-gl-pr-check":
        display_name: ccx pr-check
    - "insights-gl-build-master":
        display_name: ccx build-master
