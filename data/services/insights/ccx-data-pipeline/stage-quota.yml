---
"$schema": /openshift/quota-1.yml

labels:
  platform: insights

name: ccx-data-pipeline-stage-quota

description: Quota definition for ccx-data-pipeline-stage namespace

quotas:
- name: compute-resources-non-terminating
  resources:
    limits:
      cpu: "24"
      memory: 24Gi
    requests:
      cpu: "12"
      memory: 12Gi
  scopes:
  - NotTerminating
- name: compute-resources-terminating  
  resources:
    limits:
      cpu: "6"
      memory: 24Gi
    requests:
      cpu: "3"
      memory: 12Gi
  scopes:
  - Terminating
