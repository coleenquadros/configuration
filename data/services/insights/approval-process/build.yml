---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: approval-process
  platform: insights

name: ci-ext-insights-approval-process
description: approval process jobs

app:
  $ref: /services/insights/approval-process/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: approval-process
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: approval-process
    quay_org: cloudservices
    jobs:
    - "insights-gh-build-master":
        display_name: approval-process build-master
