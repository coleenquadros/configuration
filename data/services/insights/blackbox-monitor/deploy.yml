---
$schema: /app-sre/saas-file-2.yml

labels:
  service: blackbox-monitor 
  platform: insights

name: blackbox-monitor 
description: Blackbox Monitoring for c.rh.c

app:
  $ref: /services/insights/blackbox-monitor/app.yml

pipelinesProvider:
  $ref: /services/insights/pipelines/tekton.crc-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: team-clouddot-info

managedResourceTypes:
- DeploymentConfig
- ServiceAccount

imagePatterns:
- quay.io/cloudservices
- quay.io/openshift

authentication:
  code:
    path: app-sre/creds/github-app-sre-bot
    field: openshift-saas-deploy
  image:
    path: insights/quay/cloudservices-push
    field: all

parameters:
  CLOUD_WATCH_LOG_GROUP: blackbox-monitor-stage

resourceTemplates:
- name: blackbox-monitor
  path: /template/blackbox-monitor.yaml
  url: https://gitlab.cee.redhat.com/insights-platform/blackbox-monitor
  parameters:
    IMAGE: quay.io/cloudservices/blackbox-monitor
  targets:
  - namespace:
      $ref: /services/insights/blackbox-monitor/namespaces/stage-blackbox-monitor.yml
    ref: master
    parameters:
      REDEPLOY: true
