---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: blackbox-monitor

name: ci-int-blackbox-jobs
description: catalog ci-int jobs

app:
  $ref: /services/insights/blackbox-monitor/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: blackbox-monitor
    label: insights
    node: insights
    gl_group: insights-platform
    gl_project: blackbox-monitor
    quay_org: cloudservices
    jobs:
    - "insights-gl-build-master":
        display_name: blackbox-monitor build-master
