---

$schema: /app-sre/app-1.yml

labels:
  service: insights-operator-controller
  platform: insights

name: insights-operator-controller

description: insights-operator-controller app on the Insights Platform

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Jesse Jaggars
  email: jjaggars@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/aws/service.yml
- $ref: /dependencies/ci-int/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: insights-operator-controller
    public: false
    description: Repo for the insights-operator-controller Insights app

codeComponents:
- name: insights-operator-controller
  resource: upstream
  url: https://github.com/RedHatInsights/insights-operator-controller
