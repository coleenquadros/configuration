---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insights

name: insights-views

description: ''

app:
  $ref: /services/insights/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: insights
    description: 'insights view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[(insights|advisor|automation-hub|catalog|compliance|drift|engine|entitlements|gateway|host-inventory|ingress|insights-mnm|insights-mnm-grafana|module-update-router|patchman|rbac|rbac-exporter|recepter|remediations|rule|nomination-api|sources|strimzi|system-baseline|tower-analytics|uhc-auth-proxy|vulnerability|subscriptions)\].*'
