---

$schema: /app-sre/app-1.yml

labels:
  service: resources
  platform: insights

name: resources

description: Resource optimization app on the Insights Platform

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Suraj Patil
  email: supatil@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/aws/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: resource-optimization
    public: false
    description: Repo for the resource optimization Insights app

# Below github url needs to be replaced by offical repo later. 
codeComponents:
- name: resource-optimization
  resource: upstream
  url: https://github.com/patilsuraj767/resource-optimization-test
