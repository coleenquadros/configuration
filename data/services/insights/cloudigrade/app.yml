---

$schema: /app-sre/app-1.yml

labels:
  service: cloudigrade
  platform: insights

name: cloudigrade

description: Cloudigrade app on the Insights Platform

onboardingStatus: BestEffort

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Brad Smith
  email: brasmith@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/aws/service.yml
- $ref: /dependencies/ci-int/service.yml
- $ref: /dependencies/ci-ext/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: cloudigrade
    public: true
    description: cloudigrade app
  - name: cloudinginx
    public: true
    description: cloudigrade nginx proxy
  - name: houndigrade
    public: true
    description: cloudigrade inspection pod
  - name: shiftigrade
    public: true
    description: cloudigrade deployment pod

codeComponents:
- name: cloudigrade
  resource: upstream
  url: https://github.com/cloudigrade/cloudigrade
- name: houndigrade
  resource: upstream
  url: https://github.com/cloudigrade/houndigrade
- name: shiftigrade
  resource: upstream
  url: https://github.com/cloudigrade/shiftigrade
