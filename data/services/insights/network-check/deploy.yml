---
$schema: /app-sre/saas-file-2.yml

labels:
  service: network-check
  platform: insights

name: network-check
description: Checks basic network stbility

app:
  $ref: /services/insights/network-check/app.yml

pipelinesProvider:
  $ref: /services/insights/pipelines/tekton.crc-pipelines.app-sre-prod-01.yaml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: team-clouddot-info

managedResourceTypes:
- Deployment
- Service

imagePatterns:
- quay.io/cloudservices

resourceTemplates:
- name: network-check
  path: /template.yml
  url: https://github.com/RedHatInsights/network-check
  parameters:
    HTTP_TIMEOUT: "2"
    GETENT_TIMEOUT: "2"
  targets:
  - namespace:
      $ref: /services/insights/network-check/namespaces/stage.yml
    ref: master
    parameters:
      GETENT_HOST: koku-stage.cm3mmfyi8atf.us-east-1.rds.amazonaws.com
      HTTP_URL: http://entitlements-api-go.entitlements-stage.svc:3000/
  - namespace:
      $ref: /services/insights/network-check/namespaces/fedramp-stage.yml
    ref: master
    parameters:
      GETENT_HOST: policies-ui-backend-stage.cocewxopjp2m.us-east-1.rds.amazonaws.com
      HTTP_URL: http://entitlements-api-go.entitlements-stage.svc:3000/
  - namespace:
      $ref: /services/insights/network-check/namespaces/prod.yml
    ref: 1f8af1d7091787f9718370dc84e60d10105aed22
    parameters:
      GETENT_HOST: cost-management-prod.ckxxru2ayexw.us-east-1.rds.amazonaws.com
      HTTP_URL: http://entitlements-api-go.entitlements-prod.svc:3000/
