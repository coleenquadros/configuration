---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: network-check
  platform: insights

name: ci-int-insights-network-check

description: ''

app:
  $ref: /services/insights/network-check/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: network-check
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: network-check
    quay_org: cloudservices
    jobs:
    - gh-pr-check:
        display_name: network-check pr-check
    - gh-build-master:
        display_name: network-check build-master
