---
$schema: /app-sre/saas-file-2.yml

labels:
  service: service-mesh
  platform: insights

name: service-mesh
description: Deploy control plane resources for Istio Service Mesh on console.redhat.com

app:
  $ref: /services/insights/app.yml

pipelinesProvider:
  $ref: /services/insights/pipelines/tekton.crc-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: team-clouddot-info

managedResourceTypes:
- ServiceMeshControlPlane
- ServiceMeshMemberRoll

imagePatterns:
- quay.io/cloudservices

authentication:
  image:
    path: insights/quay/cloudservices-push
    field: all
  code:
    path: app-sre/creds/github-app-sre-bot
    field: openshift-saas-deploy

resourceTemplates:
- name: control-plane
  path: /control-plane.yml
  url: https://gitlab.cee.redhat.com/insights-platform/service-mesh-config
  targets:
  - namespace:
      $ref: /services/insights/service-mesh/namespaces/stage.yml
    ref: master
    parameters:
      ENV_NAME: "stage"
