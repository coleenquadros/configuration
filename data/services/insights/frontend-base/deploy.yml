---
$schema: /app-sre/saas-file-2.yml

labels:
  service: ephemeral
  platform: insights

name: consoledot-ephemeral-fe
description: apps used for testing in ephemeral environments

app:
  $ref: /services/insights/ephemeral/app.yml

pipelinesProvider:
  # TEMPORARY: use the tekton poc namespace for APPSRE-3420 testing
  $ref: /services/insights/pipelines/tekton.crc-poc-pipelines.appsres03ue1.yaml

managedResourceTypes:
- CustomResourceDefinition
- Deployment
- DeploymentConfig
- Service
- ClusterRole
- Role
- ConfigMap
- ServiceAccount
- RoleBinding
- ClusterRoleBinding
- Frontend
- FrontendEnvironment
- Bundle

imagePatterns:
- quay.io/cloudservices

clusterAdmin: true
compare: false

slack:
  workspace:
    $ref: /dependencies/slack/redhat-internal.yml
  channel: team-consoledot-devprod

authentication:
  code:
    path: app-sre/creds/github-app-sre-bot
    field: openshift-saas-deploy
  image:
    path: insights/quay/cloudservices-push
    field: all
    version: 3

deployResources:
  requests:
    cpu: 400m
    memory: 1500Mi
  limits:
    cpu: 400m
    memory: 1500Mi

resourceTemplates:

# Deploys Chrome
- name: insights-chrome
  path: /frontend.yml
  url: https://github.com/RedHatInsights/insights-chrome
  targets:
  # fetest frontend test environment deployment
  - namespace:
      $ref: /services/insights/ephemeral/namespaces/consoledot-frontend-perf.yml
    ref: master
    parameters:
      ENV_NAME: "env-perf"
      IMAGE: "quay.io/cloudservices/insights-chrome-frontend"
      SSO_URL: 'https://sso.stage.redhat.com/auth/'
  # Stage Deployments
  # Stage Beta
  - namespace:
      $ref: /services/insights/frontend-operator/namespaces/stage-beta-frontends.yml
    ref: master
    parameters:
      ENV_NAME: "frontends-beta"
      IMAGE: "quay.io/cloudservices/insights-chrome-frontend"
      SSO_URL: 'https://sso.stage.redhat.com/auth/'
  # Stage Stable
  - namespace:
      $ref: /services/insights/frontend-operator/namespaces/stage-frontends.yml
    ref: 956f6fd6bce9ee88fe07fbbf89134eff67a6ab57
    parameters:
      ENV_NAME: "frontends"
      IMAGE: "quay.io/cloudservices/insights-chrome-frontend"
      SSO_URL: 'https://sso.stage.redhat.com/auth/'



# Landing page
- name: landing-page-frontend
  path: /frontend.yml
  url: https://github.com/RedHatInsights/landing-page-frontend
  targets:
  # Stage Deployments
  # Stage Beta
  - namespace:
      $ref: /services/insights/frontend-operator/namespaces/stage-beta-frontends.yml
    ref: master
    parameters:
      ENV_NAME: "frontends-beta"
      IMAGE: "quay.io/cloudservices/landing-page-frontend"
  # Stage Stable
  - namespace:
      $ref: /services/insights/frontend-operator/namespaces/stage-frontends.yml
    ref: fcf17a7509eee75ded9b00fa19af4077ac547d6a
    parameters:
      ENV_NAME: "frontends"
      IMAGE: "quay.io/cloudservices/landing-page-frontend"

# Dashboard
- name: insights-dashboard
  path: /frontend.yml
  url: https://github.com/RedHatInsights/insights-dashboard
  targets:
  # Stage Deployment
  # Stage Beta
  - namespace:
      $ref: /services/insights/frontend-operator/namespaces/stage-beta-frontends.yml
    ref: master
    parameters:
      ENV_NAME: "frontends-beta"
      IMAGE: "quay.io/cloudservices/insights-dashboard-frontend"
  # Stage Stable
  - namespace:
      $ref: /services/insights/frontend-operator/namespaces/stage-frontends.yml
    ref: b996d0ae6182de3c43175e4f900ea079a9040155
    parameters:
      ENV_NAME: "frontends"
      IMAGE: "quay.io/cloudservices/insights-dashboard-frontend"      

# Deploys the FrontendEnvironment resources from gitlab
- name: frontend-env
  path: /env.yml
  url: https://gitlab.cee.redhat.com/insights-platform/frontend-environments
  targets:
  # This is the "fetest" stage testing environment that will be removed after rollout
  - namespace:
      $ref: /services/insights/ephemeral/namespaces/consoledot-frontend-stage.yml
    ref: main
    parameters:
      ENV_NAME: env-stage
      SSO_SOURCE: 'https://sso.stage.redhat.com/auth/'
  # This is the beta frontend stage environment
  - namespace:
      $ref: /services/insights/frontend-operator/namespaces/stage-beta-frontends.yml
    ref: main
    parameters:
      ENV_NAME: frontends-beta
      SSO_SOURCE: 'https://sso.stage.redhat.com/auth/'
  # This is the stable frontend stage environment
  - namespace:
      $ref: /services/insights/frontend-operator/namespaces/stage-frontends.yml
    ref: main
    parameters:
      ENV_NAME: frontends
      SSO_SOURCE: 'https://sso.stage.redhat.com/auth/'

- name: frontend-configs
  path: /deploy/deploy.yaml
  url: https://gitlab.cee.redhat.com/insights-platform/frontend-configs
  targets:
  # "fetest" this can be removed in future
  - namespace:
      $ref: /services/insights/ephemeral/namespaces/consoledot-frontend-stage.yml
    ref: main
    parameters:
      ENV_NAME: env-stage
  # ephemeral for bonfire
  - namespace:
      $ref: /services/insights/ephemeral/namespaces/ephemeral-base.yml
    disable: true  # do not create an app-sre deploy job for ephemeral namespace
    ref: internal  # populated by bonfire
  # frontend-configs is not deployed to stage or prod

- name: api-frontend
  path: /deploy/frontend.yml
  url: https://github.com/RedHatInsights/api-frontend
  targets:
  - namespace:
      $ref: /services/insights/ephemeral/namespaces/ephemeral-base.yml
    disable: true  # do not create an app-sre deploy job for ephemeral namespace
    ref: internal  # populated by bonfire
  # "fetest" stage deployment. Will be removed in future.
  - namespace:
      $ref: /services/insights/ephemeral/namespaces/consoledot-frontend-stage.yml
    ref: 9454d7d34627d46b1783eda5ad0752a654303774
    parameters:
      ENV_NAME: "env-stage"
  # Stage Beta Deployment
  - namespace:
      $ref: /services/insights/frontend-operator/namespaces/stage-beta-frontends.yml
    ref: master
    parameters:
      ENV_NAME: "frontends-beta"
      IMAGE: "quay.io/cloudservices/api-frontend"
  # Stage Stable Deployment
  - namespace:
      $ref: /services/insights/frontend-operator/namespaces/stage-frontends.yml
    ref: 49f93dba5238c61b3d6467038c19a5a372abd65f
    parameters:
      ENV_NAME: "frontends"
      IMAGE: "quay.io/cloudservices/api-frontend"

