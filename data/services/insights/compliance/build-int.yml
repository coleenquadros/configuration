---
$schema: /dependencies/jenkins-config-1.yml

# Labels are metadata useful for querying Qontract using GraphQL
labels:
  service: compliance
  platform: insights

name: ci-int-insights-compliance
description: 'Job for building Insights Compliance'

app:
  $ref: /services/insights/compliance/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: compliance-ssg
    label: insights
    node: insights
    quay_org: cloudservices
    gh_org: RedHatInsights
    gh_repo: compliance-ssg
    jobs:
    - "insights-gh-build-master":
        display_name: compliance-ssg build-master
        branch: "main"
