---

$schema: /app-sre/app-1.yml

labels:
  service: rds-exporter
  platform: insights

name: rds-exporter

description: rds-exporter for enhanced metrics for AWS RDS instances

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Kyle Lape
  email: klape@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/aws/service.yml

## TODO - This repo is deprecated
quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: rdsexporter
    public: false
    description: Repo for the rds-exporter Insights app
