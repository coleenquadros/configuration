---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: marketplace

name: ci-ext-marketplace-jobs
description: marketplace ci-ext jobs

app:
  $ref: /services/insights/marketplace/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: marketplace
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: marketplace-processor
    quay_org: cloudservices
    jobs:
    - "insights-gh-pr-check":
        display_name: marketplace pr-check
    - "insights-gh-build-master":
        display_name: marketplace build-master
