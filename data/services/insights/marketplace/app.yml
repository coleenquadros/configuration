---

$schema: /app-sre/app-1.yml

labels:
  service: marketplace
  platform: insights

name: marketplace

description: marketplace app on the Insights Platform

parentApp:
  $ref: /services/insights/app.yml

serviceOwners:
- name: Chris Hambridge
  email: chambrid@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/aws/service.yml
- $ref: /dependencies/ci-int/service.yml
- $ref: /dependencies/ci-ext/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/cloudservices.yml
  items:
  - name: marketplace
    public: false
    description: Repo for the marketplace Insights app
  - name: pendo-prom-emitter
    public: false
    description: Repo for the Pendo prometheus emitter app

codeComponents:
- name: marketplace-processor
  resource: upstream
  url: https://github.com/RedHatInsights/marketplace-processor
- name: pendo-prom-emitter
  resource: upstream
  url: https://github.com/RedHatInsights/pendo-prom-emitter
