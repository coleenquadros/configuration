---
$schema: /app-sre/saas-file-2.yml

labels:
  service: marketplace
  platform: insights

name: marketplace-clowder
description: marketplace app on the Insights Platform

app:
  $ref: /services/insights/marketplace/app.yml

pipelinesProvider:
  $ref: /services/insights/pipelines/tekton.crc-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/redhat-internal.yml
  channel: team-clouddot-info

managedResourceTypes:
- ClowdApp
- PersistentVolumeClaim

imagePatterns:
- quay.io/cloudservices

authentication:
  image:
    path: insights/quay/cloudservices-push
    field: all
    version: 3

resourceTemplates:
- name: marketplace
  path: /deploy/clowdapp.yaml
  url: https://github.com/RedHatInsights/marketplace-processor
  targets:
  - namespace:
      $ref: /services/insights/ephemeral/namespaces/ephemeral-base.yml
    disable: true  # do not create an app-sre deploy job for ephemeral namespace
    ref: internal  # populated by bonfire
    parameters:
      MIN_REPLICAS: 1
  - namespace:
      $ref: /services/insights/marketplace/namespaces/stage-marketplace-stage.yml
    ref: master
    upstream:
      instance:
        $ref: /dependencies/ci-ext/ci-ext.yml
      name: RedHatInsights-marketplace-processor-gh-build-master
    delete: true
    parameters:
      MIN_REPLICAS: 3
      ENV_NAME: stage
      MARKETPLACE_BUCKET: open-marketplace-stage
      ENABLE_CLOUDWATCH_LOGGING: "True"
  - namespace:
      $ref: /services/insights/marketplace/namespaces/marketplace-prod.yml
    ref: d1c9425c951ddadb73a98ab2b5a9a0c5589ed61e
    delete: true
    parameters:
      MIN_REPLICAS: 6
      ENV_NAME: prod
      MARKETPLACE_BUCKET: open-marketplace-prod
      ENABLE_CLOUDWATCH_LOGGING: "True"
