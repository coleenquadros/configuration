---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: catalog

name: ci-ext-catalog-jobs
description: catalog ci-ext jobs

app:
  $ref: /services/insights/catalog/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: catalog-api
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: catalog-api
    quay_org: cloudservices
    jobs:
    - "insights-gh-pr-check":
        display_name: catalog-api pr-check
    - "insights-gh-build-master":
        display_name: catalog-api build-master
