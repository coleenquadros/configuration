---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: ephemeral

name: ci-ext-ephemeral-jobs
description: ephemeral ci-ext jobs

app:
  $ref: /services/insights/ephemeral/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: ephemeral-namespace-operator
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: ephemeral-namespace-operator
    quay_org: cloudservices
    jobs:
    - "insights-gh-build-master":
        display_name: ephemeral-namespace-operator build-master
        branch: main
