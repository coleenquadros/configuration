---
$schema: /app-sre/app-1.yml

labels:
  service: managed-scripts

name: managed-scripts

description: |
  https://github.com/openshift/managed-scripts - container for managed scripts

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/ci-ext/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/app-sre.yml
  items:
  - name: managed-scripts
    description: managed-scripts container image
    public: true

codeComponents:
- name: managed-scripts
  resource: upstream
  url: https://github.com/openshift/managed-scripts/backplane
