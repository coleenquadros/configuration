---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: cincinnati

name: ci-int-cincinnati-views

description: ''

app:
  $ref: /services/cincinnati/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: cincinnati
    description: 'cincinnati view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[cincinnati|Cincinnati\].*'
