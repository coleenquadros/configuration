---
$schema: /app-sre/saas-file-1.yml

labels:
  service: hive

name: saas-hive-config
description: SaaS tracking file for HiveConfig

app:
  $ref: /services/hive/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: team-hive-info

managedResourceTypes:
- HiveConfig

imagePatterns: []

resourceTemplates:
- name: hive
  url: https://github.com/openshift/hive
  path: /hack/olm-registry/olm-artifacts-template.yaml
  parameters:
    REGISTRY_IMG: quay.io/app-sre/hive-registry
    ADDITIONAL_CERTIFICATE_AUTHORITY: letsencrypt-ca
  targets:
  - namespace:
      $ref: /services/osd-operators/namespaces/hive-integration-cluster-scope.yml
    ref: master
    upstream: openshift-hive-gh-build-master
    parameters:
      GLOBAL_PULL_SECRET: hive-ci-global-pull-secret
      MANAGED_DOMAINS:
        - aws:
            credentialsSecretRef:
              name: external-dns-route53-admin
          domains:
          - i1.devshift.org
        - gcp:
            credentialsSecretRef:
              name: google-cloud-dns-creds
          domains:
          - i2.devshift.org
      DISABLED_CONTROLLERS:
        - "remoteingress"
  - namespace:
      $ref: /services/osd-operators/namespaces/hive-stage-cluster-scope.yml
    ref: master
    upstream: openshift-hive-gh-build-master
    parameters:
      MANAGED_DOMAINS:
        - aws:
            credentialsSecretRef:
              name: external-dns-route53-admin
          domains:
          - s1.devshift.org
        - gcp:
            credentialsSecretRef:
              name: google-cloud-dns-creds
          domains:
          - s2.devshift.org
      DISABLED_CONTROLLERS:
        - "remoteingress"
  - namespace:
      $ref: /services/osd-operators/namespaces/hive-stage-01/hive-stage-01-cluster-scope.yml
    ref: master
    upstream: openshift-hive-gh-build-master
    parameters:
      MANAGED_DOMAINS:
        - aws:
            credentialsSecretRef:
              name: external-dns-route53-admin
          domains:
          - s1.devshift.org
        - gcp:
            credentialsSecretRef:
              name: google-cloud-dns-creds
          domains:
          - s2.devshift.org
      DISABLED_CONTROLLERS:
        - "remoteingress"
  - namespace:
      $ref: /services/osd-operators/namespaces/hive-production-cluster-scope.yml
    ref: 3261cec3ba098e1ca4019db3d1b91a2db86f95c9
    parameters:
      MANAGED_DOMAINS:
        - aws:
            credentialsSecretRef:
              name: external-dns-route53-admin
          domains:
          - p1.openshiftapps.com
        - gcp:
            credentialsSecretRef:
              name: google-cloud-dns-creds
          domains:
          - p2.openshiftapps.com
      DISABLED_CONTROLLERS:
        - "remoteingress"
  - namespace:
      $ref: /services/osd-operators/namespaces/hivep01ue1/hivep01ue1-cluster-scope.yml
    ref: e9039a4eea4493f37bc13c3d02bebf09b197daa4
    parameters:
      MANAGED_DOMAINS:
        - aws:
            credentialsSecretRef:
              name: external-dns-route53-admin
          domains:
          - p1.openshiftapps.com
        - gcp:
            credentialsSecretRef:
              name: google-cloud-dns-creds
          domains:
          - p2.openshiftapps.com
      DISABLED_CONTROLLERS:
        - "remoteingress"
