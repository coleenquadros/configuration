---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: hive

name: ci-ext-hive-jobs

description: ''

app:
  $ref: /services/hive/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: hive
    label: hive
    node: hive
    gh_org: openshift
    gh_repo: hive
    pr_check_script_path: './hack/app_sre_pr_check.sh'
    jobs:
    - 'gh-pr-check':
        display_name: pr-check

- project:
    name: external-dns
    label: hive
    node: hive
    gh_org: openshift
    gh_repo: external-dns
    branch: 'openshift-4.0'
    quay_org: app-sre
    build_deploy_script_path: './hack/app_sre_build_deploy.sh'
    pr_check_script_path: './hack/app_sre_pr_check.sh'
    jobs:
    - 'gh-build-master':
        display_name: external-dns build master
