---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: hive

name: ci-ext-hive-views

description: ''

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: hive
    description: 'hive view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[hive\].*'
