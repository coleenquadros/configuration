---
$schema: /openshift/namespace-1.yml

labels: {}

name: gcp-project-operator
description: integration credentials for gcp-project-operator

cluster:
  $ref: /openshift/hive-integration/cluster.yml

app:
  $ref: /services/hive/app.yml

networkPoliciesAllow:
- $ref: /services/hive/namespaces/olm-integration.yml

managedRoles: true

managedResourceTypes:
- Secret
- ConfigMap

openshiftResources:
- provider: vault-secret
  path: osd-sre/hive-integration/gcp-project-operator/gcp-project-operator-credentials
  version: 2
- provider: resource
  path: /hive-integration/gcp-project-operator/gcp-project-operator.configmap.yaml
