---
$schema: /openshift/namespace-1.yml

labels: {}

name: gcp-project-operator
description: production credentials for gcp-project-operator

cluster:
  $ref: /openshift/hive-production/cluster.yml

app:
  $ref: /services/hive/app.yml

networkPoliciesAllow:
- $ref: /services/hive/namespaces/olm-production.yml

managedRoles: true

managedResourceTypes:
- Secret

openshiftResources:
- provider: vault-secret
  path: osd-sre/hive-production/gcp-project-operator/gcp-project-operator-credentials
  version: 1
