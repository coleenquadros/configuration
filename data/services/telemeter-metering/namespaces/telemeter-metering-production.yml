---
$schema: /openshift/namespace-1.yml

labels: {}

name: telemeter-metering-production
description: production namespace for telemeter-metering

cluster:
  $ref: /openshift/app-sre/cluster.yml

app:
  $ref: /services/telemeter-metering/app.yml

limitRanges:
  $ref: /dependencies/openshift/limitranges/no-resource-limits.yml

networkPoliciesAllow:
- $ref: /services/observability/namespaces/app-sre-observability-production.yml

managedRoles: true

managedResourceTypes:
- Secret

openshiftResources:
# the `token` key belongs to the telemeter-cache-production/telemeter-metering-access SA
- provider: vault-secret
  path: app-interface/app-sre/telemeter-metering-production/telemeter-access
  version: 1
