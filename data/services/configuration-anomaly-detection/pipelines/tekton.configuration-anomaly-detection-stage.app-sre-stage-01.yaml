---
$schema: /app-sre/pipelines-provider-1.yml

labels: {}

name: tekton-configuration-anomaly-detection-stage-app-sre-stage-01
description: tekton provider in the configuration-anomaly-detection-stage namespace in the app-sre-stage-01 cluster

provider: tekton
namespace:
  $ref: /services/configuration-anomaly-detection/namespaces/configuration-anomaly-detection-stage.yaml

defaults:
  "$ref": /pipelines/tekton-provider-global-defaults.yaml
