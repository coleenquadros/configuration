---
$schema: /app-sre/saas-file-2.yml

labels:
  service: configuration-anomaly-detection

name: saas-configuration-anomaly-detection-db
description: 'SaaS tracking file for configuration-anomaly-detection observability'

app:
  $ref: /services/configuration-anomaly-detection/app.yml

pipelinesProvider:
  $ref: /services/configuration-anomaly-detection/pipelines/tekton.configuration-anomaly-detection-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: team-srep-info

managedResourceTypes:
- ConfigMap

imagePatterns: []

resourceTemplates:
  - name: configuration-anomaly-detection-dashboards
    url: https://github.com/openshift/configuration-anomaly-detection
    path: /dashboards
    provider: directory
    targets:
      - namespace:
          $ref: /services/observability/namespaces/app-sre-observability-stage.yml
        ref: main
