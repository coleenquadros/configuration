---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: configuration-anomaly-detection

name: ci-int-configuration-anomaly-detection-jobs

description: ''

app:
  $ref: /services/configuration-anomaly-detection/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
  - project:
      name: configuration-anomaly-detection
      label: configuration-anomaly-detection
      node: app-sre
      gh_org: openshift
      gh_repo: configuration-anomaly-detection
      quay_org: app-sre
      build_deploy_script_path: make osd-container-image-build-push
      jobs:
        - 'gh-build-master':
            display_name: configuration-anomaly-detection build master
            branch: main
