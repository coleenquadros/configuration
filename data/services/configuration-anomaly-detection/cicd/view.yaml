---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: configuration-anomaly-detection

name: ci-int-configuration-anomaly-detection-views

description: ''

app:
  $ref: /services/configuration-anomaly-detection/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
  - view:
      name: configuration-anomaly-detection
      description: 'configuration-anomaly-detection view'
      view-type: list
      job-filters:
        regex-job:
          regex-name: DESCRIPTION
          regex: '^\[configuration-anomaly-detection\].*'
