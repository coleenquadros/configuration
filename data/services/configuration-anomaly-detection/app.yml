---
$schema: /app-sre/app-1.yml

labels:
  service: osdv4

name: configuration-anomaly-detection

description: Configuration anomaly detection for OSD clusters

onboardingStatus: InProgress

serviceOwners:
- name: SRE-Platform (Team Orange)
  email: sd-sre-platform+pagerduty-configuration-anomaly-detection-agent@redhat.com

dependencies:
- $ref: /dependencies/ci-ext/service.yml
  $ref: /dependencies/aws/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/app-sre.yml
  items:
  - name: configuration-anomaly-detection
    description: Configuration anomaly detection for OSD clusters
    public: true
