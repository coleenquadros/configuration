---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: container-security-operator

name: ci-ext-container-security-operator-views

description: ''

app:
  $ref: /services/container-security-operator/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: container-security-operator
    description: 'container-security-operator view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[container-security-operator\].*'
