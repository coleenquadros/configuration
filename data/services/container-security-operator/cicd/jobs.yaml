---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: container-security-operator

name: ci-ext-container-security-operator-jobs

description: ''

app:
  $ref: /services/container-security-operator/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: container-security-operator
    label: container-security-operator
    node: app-sre
    gh_org: app-sre
    gh_repo: container-security-operator
    quay_org: app-sre
    jobs:
    - 'gh-pr-check':
        display_name: container-security-operator pr-check
