---
$schema: /openshift/namespace-1.yml

labels:
  service: quayio

name: cloudwatch-exporter
description: staging namespace for cloudwatch-exporter

cluster:
  $ref: /openshift/quayio-stage/cluster.yml

app:
  $ref: /services/quayio/app.yml

environment:
  $ref: /products/app-sre/environments/stage.yml

networkPoliciesAllow:
- $ref: /services/observability/namespaces/openshift-customer-monitoring.quayio-stage.yml

managedRoles: true

managedResourceTypes:
- ConfigMap

openshiftResources:
- provider: vault-secret
  path: app-interface/quayio-stage/cloudwatch-exporter/iam-creds
  version: 2
  labels:
    version: v2
- provider: resource
  path: /quayio-stage/cloudwatch-exporter/cloudwatch-exporter.configmap.yaml
