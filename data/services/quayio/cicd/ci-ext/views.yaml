---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: quayio

name: ci-ext-quayio-views

description: ''

app:
  $ref: /services/quayio/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: quayio
    description: 'quayio view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[(quayio|syslog-cloudwatch-bridge)\].*'
