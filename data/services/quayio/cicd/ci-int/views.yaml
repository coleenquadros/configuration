---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: quayio

name: ci-int-quayio-views

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: quayio
    description: 'quayio view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[quayio\].*'
