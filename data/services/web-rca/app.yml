---
$schema: /app-sre/app-1.yml

labels:
  service: web-rca

name: web-rca
grafanaUrl: https://grafana.app-sre.devshift.net/FIXME
sopsUrl: https://gitlab.cee.redhat.com/FIXME
architectureDocument: https://gitlab.cee.redhat.com/service/web-rca/-/blob/main/README.md#the-architecture

description: |
  Web-RCA is a service that allows anyone at Red Hat to create RCAs (root cause
  analysis) / incident reports.

onboardingStatus: InProgress

serviceOwners:
- name: SD OPS Dev
  email: sd-ops-services-dev@redhat.com

escalationPolicy:
  $ref: /teams/sd-ops-dev/escalation-policies/web-rca.yml

dependencies:
- $ref: /dependencies/ci-int/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/gitlab/service.yml
- $ref: /dependencies/sso/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/aws/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/app-sre.yml
  teams:
  - permissions:
    - $ref: /dependencies/quay/permissions/quay-membership-sd-ops-dev.yml
    role: read
  items:
  - name: web-rca
    description: Web-RCA is a service that allows anyone at Red Hat to create RCAs (root cause analysis) / incident reports.
    public: false

codeComponents:
- name: web-rca
  resource: upstream
  url: https://gitlab.cee.redhat.com/service/web-rca

sentryProjects:
- team:
    $ref: /dependencies/sentry/teams/sd-ops-dev-stage.yml
  projects:
  - name: web-rca-stage
    description: Web RCA API
    email_prefix: sd-ops-dev
    platform: go
    auto_resolve_age: 168
    allowed_domains:
    - '*'
