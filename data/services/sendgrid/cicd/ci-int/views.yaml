---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: sendgrid

name: ci-int-sendgrid-views

description: ''

app:
  $ref: /services/sendgrid/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: sendgrid
    description: 'sendgrid view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[sendgrid\].*'
