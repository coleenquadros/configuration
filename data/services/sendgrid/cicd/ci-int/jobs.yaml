
---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: sendgrid

name: ci-int-sendgrid-jobs

description: ''

app:
  $ref: /services/sendgrid/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: ocm-sendgrid-service
    label: sendgrid
    node: sd-uhc
    gl_group: service
    gl_project: ocm-sendgrid-service
    jobs:
    - 'gl-pr-check':
        display_name: OCM sendgrid service check merge request
    - 'gl-build-master':
        display_name: OCM sendgrid service build master
