---
$schema: /app-sre/gabi-instance-1.yml

labels:
  service: gabi

name: gabi-stage

description: |
 For GABI service use only.

signoffManagers:
- $ref: /teams/app-sre/users/jbeakley.yml

# Nobody should need to access this GABI instance.
users: []

instances:
- account: app-sre-stage
  identifier: gabi-stage
  namespace:
    $ref: /services/gabi/namespaces/gabi-stage.yml

expirationDate: '1970-01-01'
