---
$schema: /app-sre/gabi-instance-1.yml

labels:
  service: gabi

name: gabi-cloud-connector
description: |
 I would like to be able to get db access for cloud-connector
 I acknowledge there is no sensitive data or there is sensitive data, but agrees to have a specific engineer accessing it

signoffManagers:
- $ref: /teams/insights/users/gmccullo.yml

users:
- $ref: /teams/insights/users/dhorton.yml

instances:
- account: insights-stage
  identifier: cloud-connector-stage
  namespace:
    $ref: /services/insights/cloud-connector/namespaces/stage.yml
- account: insights-prod
  identifier: cloud-connector-prod
  namespace:
    $ref: /services/insights/cloud-connector/namespaces/prod.yml

expirationDate: '2022-07-12'
