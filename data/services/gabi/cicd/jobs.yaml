---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: gabi

name: ci-ext-gabi-jobs

description: ''

app:
  $ref: /services/gabi/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: gabi
    label: app-sre
    node: app-sre
    gh_org: app-sre
    gh_repo: gabi
    quay_org: app-sre
    jobs:
    - 'gh-pr-check':
        display_name: pr-check gabi
        branch: main
        pr_check_script_path: |
          echo -e '\U1f64a \U1f648 \U1f649'
    - 'gh-build-master':
        display_name: build and push image gabi
        branch: main
