---
$schema: /access/role-1.yml

labels:
  service: gabi

name: gabi-cost-management-signoff-manager
description: allows a manager to approve requests related to a gabi instance

permissions: []

self_service:
- change_type:
    $ref: /app-interface/changetype/gabi-signoff-manager.yml
  datafiles:
  - $ref: /services/gabi/gabi-instances/gabi-cost-management.yml
