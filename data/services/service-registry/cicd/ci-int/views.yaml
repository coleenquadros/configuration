---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: service-registry

name: ci-int-service-registry-view

description: ''

app:
  $ref: /services/service-registry/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: service-registry
    description: 'Jenkins View For Service Registry Jobs'
    view-type: list
    job-filters:
        regex-job:
          regex-name: DESCRIPTION
          # https://gitlab.cee.redhat.com/service/app-interface/-/blob/fc57d25f013f36d9126475c55a1b4bb876cb8503/resources/jenkins/global/defaults.yaml#L6 
          # matches against your job's [{label}] {display_name}
          regex: '^\[service-registry\].*'
