---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: try-openshift-com

name: ci-int-try-openshift-com-views

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: try-openshift-com
    description: 'try-openshift-com view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[try-openshift-com\].*'
