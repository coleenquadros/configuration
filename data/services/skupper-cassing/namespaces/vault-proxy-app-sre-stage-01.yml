---
$schema: /openshift/namespace-1.yml

labels:
  service: skupper-cassing

name: skupper-vault-proxy

description: Proxy to vault via skupper

cluster:
  $ref: /openshift/app-sre-stage-01/cluster.yml

app:
  $ref: /services/skupper-cassing/app.yml

environment:
  $ref: /products/app-sre/environments/stage.yml

networkPoliciesAllow:
- $ref: /services/skupper-cassing/namespaces/vault-user-app-sre-stage-01.yml

openshiftResources: []
# - provider: vault-secret
#   path: app-interface/status-board/production/runtime
#   version: 1
# - provider: route
#   path: /status-board/production/status-board.route.yaml
