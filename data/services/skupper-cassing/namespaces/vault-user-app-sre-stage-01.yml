---
$schema: /openshift/namespace-1.yml

labels:
  service: skupper-cassing

name: skupper-vault-user

description: Vault usage example (e.g. q-r integration)

cluster:
  $ref: /openshift/app-sre-stage-01/cluster.yml

app:
  $ref: /services/skupper-cassing/app.yml

environment:
  $ref: /products/app-sre/environments/stage.yml

managedResourceTypes:
- ServiceAccount
- Deployment

sharedResources:
- $ref: /services/skupper-cassing/shared-resources/test-client.yml

openshiftResources:
- provider: resource-template
  path: /skupper-cassing/test-client/deployment.yml
  variables:
    test_urls:
    - http://vault.skupper-vault-net.svc.cluster.local:8080
delete: true
