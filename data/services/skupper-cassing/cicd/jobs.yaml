---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: skupper-cassing

name: ci-ext-skuppet-cassing-jobs

description: ''

app:
  $ref: /services/skupper-cassing/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: http-stub
    label: app-sre
    node: app-sre
    gh_org: chassing
    gh_repo: http-stub
    quay_org: app-sre
    build_deploy_script_path: "./build_deploy.sh"
    jobs:
    - 'gh-build-master':
        display_name: http-stub build master
