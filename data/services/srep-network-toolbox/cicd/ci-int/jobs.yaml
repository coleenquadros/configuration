---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: srep-network-toolbox

name: srep-network-toolbox

description: 'Builds the srep-network-toolbox image'

app:
  $ref: /services/srep-network-toolbox/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: srep-network-toolbox
    label: srep-network-toolbox
    node: app-sre
    gl_group: service
    gl_project: srep-network-toolbox
    quay_org: app-sre
    jobs:
    - 'gl-build-master':
        display_name: srep-network-toolbox build master
    - 'gl-pr-check':
        display_name: srep-network-toolbox pr-check
