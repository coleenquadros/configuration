---
$schema: access/role.yml

labels:
    service: openshift.io

name: base

permissions:
- service: github-org
  org: rhdt-dev

# members:
#   synthetic field queryable through GraphQL
#   contains all users that have a reference this role
