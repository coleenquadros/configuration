---
$schema: /access/role-1.yml

labels:
  service: openshift.io

name: devtools-ci-che

permissions:
- $ref: /dependencies/vault/permissions/devtools-osio.yml

oidc_permissions:
- $ref: /dependencies/vault/permissions/oidc/prod/devtools-osio.yml
