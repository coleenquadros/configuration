---
$schema: /access/role-1.yml

labels:
  service: openshift.io

name: bot
description: for bots that need to belong to 'rhdt-dev' gh org

permissions:
- $ref: /dependencies/github/permissions/rhdt-dev.yml
