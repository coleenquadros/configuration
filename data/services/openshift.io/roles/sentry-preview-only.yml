---
$schema: /access/role-1.yml

labels:
  service: openshift.io

name: sentry-preview-only

permissions: []

access:
- namespace:
    $ref: /services/openshift.io/namespaces/sentry-preview.yml
  role: view
