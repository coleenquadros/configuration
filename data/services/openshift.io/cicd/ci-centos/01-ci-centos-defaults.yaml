---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: jenkins

name: ci-centos-defaults-jenkins-configs

description: ''

app:
  $ref: /services/openshift.io/app.yml

instance:
  $ref: /dependencies/ci-centos/ci-centos.yml

type: defaults

config_path: /jenkins/ci-centos/defaults.yaml
