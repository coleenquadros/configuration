---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: app-sre

name: ci-int-rhel-images-views

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: rhel-images
    description: 'rhel-images view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^rhel-.*'
