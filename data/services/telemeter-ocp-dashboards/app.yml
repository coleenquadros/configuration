---
$schema: /app-sre/app-1.yml

labels:
  service: datahub

name: telemeter-ocp-dashboards

description: |
  A Grafana instance deployed via CICD to talk to Datahub and show OCP Global dashboards

serviceOwners:
- name: Datahub Team
  email: mhild@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/ci-int/service.yml

quayRepos: []
