---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: telemeter-ocp-dashboards

name: ci-int-telemeter-ocp-dashboards-views

description: ''

app:
  $ref: /services/telemeter-ocp-dashboards/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: datahub
    description: 'Telemeter OCP dashboards et al'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[telemeter-ocp-dashboards\].*'
