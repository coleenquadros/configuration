---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: uhc

name: ci-ext-uhc-views

description: ''

app:
  $ref: /services/ocm/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: sd-uhc
    description: 'sd-uhc view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[(sd-uhc|uhc)\].*'
