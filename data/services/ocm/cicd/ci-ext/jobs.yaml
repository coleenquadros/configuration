---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: uhc

name: ci-ext-uhc-jobs

description: ''

app:
  $ref: /services/ocm/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: ocm-cli
    label: sd-uhc
    node: sd-uhc
    gh_org: openshift-online
    gh_repo: ocm-cli
    jobs:
    - 'gh-pr-check':
        display_name: OCM CLI check merge request
    - 'gh-build-tag':
        display_name: OCM CLI build release

- project:
    name: ocm-api-model
    label: sd-uhc
    node: sd-uhc
    gh_org: openshift-online
    gh_repo: ocm-api-model
    jobs:
    - 'gh-pr-check':
        display_name: OCM API model check merge request
    - 'gh-build-tag':
        display_name: OCM API model build release
