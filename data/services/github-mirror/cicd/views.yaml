---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: github-mirror

name: ci-ext-github-mirror-views

description: ''

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: github-mirror
    description: 'github-mirror view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[github-mirror\].*'
