---
$schema: /app-sre/app-1.yml

labels:
  service: github-mirror

name: github-mirror

description: |
  The GitHub Mirror is an API mirror that implements conditional requests.

onboardingStatus: OnBoarded

grafanaUrl: https://grafana.app-sre.devshift.net/d/DE29QJlWk/github-mirror

serviceOwners:
- name: App-SRE Team
  email: sd-app-sre@redhat.com

dependencies:
- $ref: /dependencies/aws/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/ci-ext/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/app-sre.yml
  items:
  - name: github-mirror
    description: GitHub API mirror with conditional requests
    public: true

codeComponents:
- name: gihub-mirror
  resource: upstream
  url: https://github.com/app-sre/github-mirror
