---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: api-designer

name: ci-int-api-designer-views

description: ''

app:
  $ref: /services/api-designer/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: api-designer
    description: 'api-designer view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[api-designer\].*'
