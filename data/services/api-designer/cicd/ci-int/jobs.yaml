---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: api-designer

name: ci-int-api-designer-jobs

description: ''

app:
  $ref: /services/api-designer/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
  - project:
      name: ads-ui-midstream
      label: api-designer
      node: managed-services
      gl_group: api-designer
      gl_project: ads-ui
      jobs:
        - 'gl-pr-check':
            display_name: ads-ui pr-check
            concurrent_build: true
        - 'gl-build-master-managed-services':
            branch: main
            display_name: ads-ui build-main


  - project:
      name: ads-editors-midstream
      label: api-designer
      node: managed-services
      gl_group: api-designer
      gl_project: ads-editors
      jobs:
        - 'gl-pr-check':
            display_name: ads-editors pr-check
            concurrent_build: true
        - 'gl-build-master-managed-services':
            branch: main
            display_name: ads-editors build-main
