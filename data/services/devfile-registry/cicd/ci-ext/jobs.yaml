---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: devfile-registry

name: ci-ext-devfile-registry-jobs

description: ''

app:
  $ref: /services/devfile-registry/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: devfile-registry
    label: devfile-registry
    node: app-sre
    gh_org: devfile
    gh_repo: registry
    pr_check_script_path: "./.ci/build.sh"
