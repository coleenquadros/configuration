---
$schema: /app-sre/saas-file-2.yml

labels:
  service: devfile-registry

name: saas-devfile-registry-rh-post-deploy
description: SaaS tracking file for devfile-registry-redhat post deployment tests

app:
  $ref: /services/devfile-registry/app-redhat.yml

pipelinesProvider:
  $ref: /services/devfile-registry/pipelines/tekton.devfile-registry-pipelines.app-sre-prod-01.yaml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: team-devfile-registry-alert

managedResourceTypes:
- Job
- ServiceAccount

publishJobLogs: true

imagePatterns:
- quay.io/devfile/devfile-registry-integration

resourceTemplates:
- name: devfile-registry
  url: https://github.com/redhat-developer/devfile-registry
  path: /.ci/acceptance-tests/devfile-registry-acceptance.yaml
  targets:
  - namespace:
      $ref: /services/devfile-registry/namespaces/devfile-registry-stage.yml
    ref: b65dfce089ff116d1044ed5221470646ab33c07e
    parameters:
      REGISTRY: https://devfile-registry.stage.redhat.com
      IMAGE_TAG: next
    promotion:
      publish:
      - devfile-registry-redhat-stage-post-deploy-tests-success-channel
