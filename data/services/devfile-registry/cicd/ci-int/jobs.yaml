---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: devfile-registry

name: ci-int-devfile-registry-jobs

description: ''

app:
  $ref: /services/devfile-registry/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: devfile-registry
    label: devfile-registry
    node: app-sre
    gh_org: devfile
    gh_repo: registry
    quay_org: app-sre
    build_deploy_script_path: "./.ci/build_and_deploy.sh"
    jobs:
    - 'gh-build-master':
        branch: main
        display_name: build main

- project:
    name: redhat-devfile-registry
    label: redhat-devfile-registry
    node: app-sre
    gh_org: redhat-developer
    gh_repo: devfile-registry
    quay_org: app-sre
    build_deploy_script_path: "./.ci/build_and_deploy.sh"
    jobs:
    - 'gh-build-master':
        branch: main
        display_name: build main
