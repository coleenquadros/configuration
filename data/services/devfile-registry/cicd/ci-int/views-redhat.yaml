---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: devfile-registry

name: ci-int-redhat-devfile-registry-views

description: ''

app:
  $ref: /services/devfile-registry/app-redhat.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: redhat-devfile-registry
    description: 'redhat-devfile-registry view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[redhat-devfile-registry\].*'
