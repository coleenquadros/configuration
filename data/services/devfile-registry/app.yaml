---
$schema: /app-sre/app-1.yml

labels:
  service: devfile-registry

name: devfile-registry

description: |
  Hosted Devfile Registry Service

onboardingStatus: Proposed

serviceOwners:
- name: Devfile Services
  email: team-devfile@redhat.com

dependencies:
- $ref: /dependencies/github/service.yml

performanceParameters:
  SLO: 90

codeComponents:
- name: Devfile Registry support repository
  resource: upstream
  url: https://github.com/devfile/registry-support
- name: Devfile Registry stack repository
  resource: upstream
  url: https://github.com/devfile/registry
