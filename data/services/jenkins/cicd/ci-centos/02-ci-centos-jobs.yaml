---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: jenkins

name: ci-centos-defaults-jenkins-jobs

description: ''

app:
  $ref: /services/openshift.io/app.yml

instance:
  $ref: /dependencies/ci-centos/ci-centos.yml

type: jobs

config_path: /jenkins/ci-centos/jobs.yaml
