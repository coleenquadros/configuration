---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: backplane

name: ci-int-backplane-view

description: ''

app:
  $ref: /services/backplane/app.yaml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: backplane
    description: backplane
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[backplane\].*'
