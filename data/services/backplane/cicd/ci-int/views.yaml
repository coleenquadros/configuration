---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: backplane

name: ci-int-ims-backplane-views

description: ''

app:
  $ref: /services/backplane/app.yaml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: ims-backplane
    description: 'ims-backplane'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[ims-backplane\].*'
