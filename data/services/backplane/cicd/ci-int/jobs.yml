---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: backplane

name: ci-int-ims-backplane-jobs

description: ''

app:
  $ref: /services/backplane/app.yaml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: ims-backplane
    label: backplane
    node: backplane
    gl_group: service
    gl_project: backplane-api
    quay_org: app-sre
    pr_check_script_path: './hack/app_sre_pr_check.sh'
    build_deploy_script_path: './hack/app_sre_build_deploy.sh'
    jobs:
    - 'gl-pr-check':
        display_name: ims-backplane MR check
    - 'gl-build-master':
        display_name: ims-backplane build master branch
