---
$schema: /dependencies/jenkins-config-1.yml
app:
  $ref: /services/backplane/app.yaml
config:
  -
    view: 
      description: "backplane managed-scripts"
      job-filters:
        regex-job:
          regex: "^\\[managed-scripts\\].*"
          regex-name: DESCRIPTION
      name: managed-scripts
      view-type: list
description: ""
instance:
  $ref: /dependencies/ci-ext/ci-ext.yml
labels:
  service: managed-scripts
name: ci-int-managed-scripts-view
type: views
