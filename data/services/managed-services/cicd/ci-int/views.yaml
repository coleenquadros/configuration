---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: managed-services

name: ci-int-managed-services-views

description: ''

app:
  $ref: /services/managed-services/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: managed-services
    description: 'managed-services view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[managed-services\].*'

- view:
    name: kas-fleet-manager
    description: 'kas-fleet-manager view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[kas-fleet-manager\].*'

- view:
    name: ciam
    description: 'ciam view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[ciam\].*'

