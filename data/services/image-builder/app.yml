---
$schema: /app-sre/app-1.yml

labels:
  service: image-builder

name: image-builder

description: |
  The Image Builder application.

architectureDocument: |
  https://osbuild.pages.redhat.com/internal-guides/image-builder/image-builder.html
sopsUrl: https://gitlab.cee.redhat.com/TODO
grafanaUrl: https://TODO
onboardingStatus: InProgress

escalationPolicy:
  $ref: /teams/image-builder/escalation-policies/image-builder.yml

serviceOwners:
- name: osbuilders
  email: osbuilders@redhat.com
- name: Sanne Raymaekers
  email: sraymaek@redhat.com
- name: Tom Gundersen
  email: tgunders@redhat.com

dependencies:
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/app-sre.yml
  teams:
  - permissions:
    - $ref: /dependencies/quay/permissions/quay-membership-app-sre-prodsec.yml
    role: read
  items:
  - name: composer
    public: true
    description: composer component of the image-builder service

codeComponents:
- name: osbuild-composer
  resource: upstream
  url: https://github.com/osbuild/osbuild-composer
