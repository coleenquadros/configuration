---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: image-builder

name: ci-ext-image-builder-job-templates

description: ''

app:
  $ref: /services/image-builder/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: job-templates

config_path: /jenkins/image-builder/jobs-templates.yml
