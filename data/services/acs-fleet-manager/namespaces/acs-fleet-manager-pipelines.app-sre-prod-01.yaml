---
$schema: /openshift/namespace-1.yml

labels:
  provider: tekton

name: acs-fleet-manager-pipelines
description: acs-fleet-manager pipelines namespace

cluster:
  $ref: /openshift/app-sre-prod-01/cluster.yml

app:
  $ref: /services/acs-fleet-manager/app.yml

environment:
  $ref: /products/app-sre/environments/production.yml

managedRoles: true

managedResourceTypes:
- Secret
- ClusterRole

sharedResources:
- $ref: /services/app-interface/shared-resources/app-sre-pipelines.yml

