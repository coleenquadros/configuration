---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: acs-fleet-manager

name: ci-ext-acs-fleet-manager-views

description: 'View configuration file for acs-fleet-manager'

app:
  $ref: /services/acs-fleet-manager/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
  - view:
      name: acs-fleet-manager
      description: 'acs-fleet-manager view'
      view-type: list
      job-filters:
        regex-job:
          regex-name: DESCRIPTION
          regex: '^\[acs-fleet-manager\].*'
