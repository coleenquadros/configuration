---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: deployment-validation-operator

name: ci-int-deployment-validation-operator-views

description: ''

app:
  $ref: /services/deployment-validation-operator/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: deployment-validation-operator
    description: 'deployment-validation-operator view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[deployment-validation-operator\].*'
