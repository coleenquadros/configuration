---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: addons

name: ci-int-addons-views

description: ''

app:
  $ref: /services/addons/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: addons
    description: 'addons view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[addons\].*'
