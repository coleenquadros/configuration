---
$schema: /openshift/namespace-1.yml

labels: {}

name: mtsre-workloads
description: managed tenants workloads namespace for stage env

cluster:
  $ref: /openshift/app-sre-stage-01/cluster.yml

app:
  $ref: /services/addons/app.yml

environment:
  $ref: /products/osdv4/environments/stage.yml

openshiftResources:
- provider: vault-secret
  name: mtsre-clusters-checker-org-ids
  path: mt-sre/creds/org/sd-mt-sre
  version: 1
  annotations:
    qontract.recycle: "true"
- provider: vault-secret
  name: mtsre-clusters-checker-slack-creds
  path: mt-sre/creds/slack/apps/mtsre-clusters-checker
  version: 2
  annotations:
    qontract.recycle: "true"
- provider: vault-secret
  name: mtsre-clusters-checker-ocm-creds
  path: mt-sre/creds/ocm/sd-mt-sre
  version: 2
  annotations:
    qontract.recycle: "true"

managedRoles: true

