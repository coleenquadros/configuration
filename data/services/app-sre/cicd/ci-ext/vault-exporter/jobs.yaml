---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vault-exporter

name: ci-ext-vault-exporter-jobs

description: ''

app:
  $ref: /services/app-sre/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: vault-exporter
    label: vault-exporter
    node: app-sre
    gh_org: app-sre
    gh_repo: vault-exporter
    quay_org: app-sre
    build_deploy_script_path: "./build_deploy.sh"
    jobs:
    - 'gh-build-master':
        display_name: build master
