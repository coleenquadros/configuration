---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vault-exporter

name: ci-ext-vault-exporter-views

description: ''

app:
  $ref: /services/app-sre/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: vault-exporter
    description: 'vault-exporter view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[vault-exporter\].*'
