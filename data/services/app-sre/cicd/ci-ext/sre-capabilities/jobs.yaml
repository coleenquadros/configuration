---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: sre-capabilities

name: ci-ext-sre-capabilities-jobs

description: ''

app:
  $ref: /services/app-sre/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: aus-cli
    label: aus-cli
    node: app-sre
    gh_org: app-sre
    gh_repo: aus-cli
    quay_org: app-sre
    jobs:
    - 'gh-pr-check':
        display_name: pr check
    - 'gh-build-tag':
        display_name: build tag
