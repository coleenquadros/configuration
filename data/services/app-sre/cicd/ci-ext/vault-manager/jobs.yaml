---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vault-manager

name: ci-ext-vault-manager-jobs

description: ''

app:
  $ref: /services/app-sre/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: vault-manager
    label: vault-manager
    node: app-sre
    gh_org: app-sre
    gh_repo: vault-manager
    quay_org: app-sre
    build_deploy_script_path: "./build_deploy.sh"
    pr_check_script_path: "./pr_check.sh"
    jobs:
    - 'gh-build-master-vault-manager':
        display_name: build master
    - 'gh-pr-check-vault-manager':
        display_name: pr-check
