---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: app-sre

name: ci-stage-app-sre-jobs

description: ''

app:
  $ref: /services/app-sre/app.yml

instance:
  $ref: /dependencies/ci-stage/ci-stage.yml

type: jobs

config:
- project:
    name: ci-stage
    label: app-sre-stage
    node: app-sre-stage
    jobs:
    - 'jenkins-workers-cleanup':
        display_name: jenkins-workers-cleanup-stage
        gl_group: app-sre
        gl_project: infra

