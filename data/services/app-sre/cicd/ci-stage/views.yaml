---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: app-sre

name: ci-stage-app-sre-views

description: ''

app:
  $ref: /services/app-sre/app.yml

instance:
  $ref: /dependencies/ci-stage/ci-stage.yml

type: views

config:
- view:
    name: app-sre
    description: 'app-sre view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[(App-SRE|app-sre)\].*'
