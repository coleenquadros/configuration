---
$schema: /app-sre/app-1.yml

labels:
  service: addon-operator

name: addon-operator

description: |
    Coordinates the lifecycle of Addons in Managed OpenShift

onboardingStatus: OnBoarded

serviceOwners:
- name: Managed Tenants SRE Team
  email: mt-sre@redhat.com

escalationPolicy:
  $ref: /teams/mt-sre/escalation-policies/general.yaml

sopsUrl: https://gitlab.cee.redhat.com/TODO
architectureDocument: https://TODO

grafanaUrls:
- title: Nonexistent folder
  url: https://TODO

serviceNotifications:
- name: Red Hat Outage List
  email: outage-list@redhat.com

dependencies:
- $ref: /dependencies/openshift/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/ci-int/service.yml
- $ref: /dependencies/quay/service.yml

parentApp:
  $ref: /services/osd-operators/app.yml

quayRepos:
- org:
    $ref: /dependencies/quay/app-sre.yml
  items:
  - name: addon-operator-bundle
    description: addon-operator-bundle for the olm index
    public: true
  - name: addon-operator-index
    description: addon-operator-index for olm catalog
    public: true
  - name: addon-operator-package
    description: addon-operator package for package-operator
    public: true

codeComponents:
- name: addon-operator
  resource: upstream
  url: https://github.com/openshift/addon-operator
