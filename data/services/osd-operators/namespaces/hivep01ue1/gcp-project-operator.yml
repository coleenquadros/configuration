---
$schema: /openshift/namespace-1.yml

labels: {}

name: gcp-project-operator
description: staging credentials for gcp-project-operator

cluster:
  $ref: /openshift/hivep01ue1/cluster.yml

app:
  $ref: /services/osd-operators/app.yml

environment:
  $ref: /products/osdv4/environments/production-hivep01ue1.yml

networkPoliciesAllow:
- $ref: /services/osd-operators/namespaces/hivep01ue1/openshift-operator-lifecycle-manager.yml

managedRoles: true

# managedResourceTypes:
# - ConfigMap

# openshiftResources:
# - provider: vault-secret
#   path: osd-sre/hive-stage/gcp-project-operator/gcp-project-operator-credentials
#   version: 4
# - provider: resource
#   path: /hive-stage/gcp-project-operator/gcp-project-operator.configmap.yaml
