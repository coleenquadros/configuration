---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: srep-operators

name: ci-ext-srep-operators-views

description: ''

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: srep-operators
    description: 'srep-operators'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[(srep-operators|osd-operators)\].*'
- view:
    name: sre-ssh-proxy
    description: 'sre-ssh-proxy'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[sre-ssh-proxy\].*'
