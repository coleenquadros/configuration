---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-gcp-project-operator-jobs

description: ''

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: gcp-project-operator
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: gcp-project-operator
    quay_org: app-sre
    build_deploy_script_path: './hack/app_sre_build_deploy.sh'
    backup_ecr_push_creds_account_region: 'app-sre/us-east-1'
    jobs:
    - 'gh-build-master':
        display_name: gcp-project-operator build master
