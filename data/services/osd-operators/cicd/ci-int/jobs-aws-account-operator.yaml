---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-aws-account-operator-jobs

description: ''

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: aws-account-operator
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: aws-account-operator
    quay_org: app-sre
    build_deploy_script_path: './hack/app_sre_build_deploy.sh'
    jobs:
    - 'gh-build-master':
        display_name: aws-account-operator build master
