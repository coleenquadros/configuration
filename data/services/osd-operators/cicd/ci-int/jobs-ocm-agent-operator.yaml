---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-ocm-agent-operator-jobs

description: ''

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: ocm-agent-operator
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: ocm-agent-operator
    quay_org: app-sre
    build_deploy_script_path: 'SKIP_SAAS_FILE_CHECKS=true make build-push'
    jobs:
    - 'gh-build-master':
        display_name: ocm-agent-operator build master
