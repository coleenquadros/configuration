---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-private-cluster-rhapi-apischeme-updater-jobs

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: private-cluster-rhapi-apischeme-updater
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: private-cluster-rhapi-apischeme-updater
    quay_org: app-sre
    build_deploy_script_path: './hack/app_sre_build_deploy.sh'
    jobs:
    - 'gh-build-master':
        display_name: private-cluster-rhapi-apischeme-updater build master
