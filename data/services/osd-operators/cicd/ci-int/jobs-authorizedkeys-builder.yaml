---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-authorizedkeys-builder-jobs

description: 'Deploy SSH keys for OSD support teams'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: authorizedkeys-builder
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: authorizedkeys-builder
    pr_check_script_path: './pr_check.sh'
    build_deploy_script_path: './run.sh'
    jobs:
    # This job template uses a git publisher to automatically
    # push new commits upon successful job completion.
    - 'clusterimageset-run':
        job_name: authorizedkeys-builder-run
        display_name: authorizedkeys-builder build master
        cron_expression: 'H * * * *'
    - 'gl-pr-check':
        display_name: authorizedkeys-builder pr check
