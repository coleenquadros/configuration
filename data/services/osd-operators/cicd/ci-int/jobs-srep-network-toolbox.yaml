---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-srep-network-toolbox-jobs

description: 'srep-network-toolbox container build'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: srep-network-toolbox
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: srep-network-toolbox
    jobs:
    - 'gl-build-master':
        display_name: srep-network-toolbox build master
    - 'gl-pr-check':
        display_name: srep-network-toolbox pr-check
