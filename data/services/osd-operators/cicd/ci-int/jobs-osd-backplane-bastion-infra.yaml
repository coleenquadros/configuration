---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-backplane-bastion-infra

description: 'backplane bastion builder'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: osd-backplane-bastion-infra
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: osd-backplane-bastion-infra
    jobs:
    - 'gl-build-master-osd-backplane-bastion-infra-packer':
        display_name: osd-backplane-bastion-infra-packer build master nightly
        branch: master
        cron_expression: '0 0 * * *'
