---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-ocm-agent-jobs

description: 'ocm-agent build process'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: ocm-agent
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: ocm-agent
    quay_org: app-sre
    build_deploy_script_path: './build/build_deploy.sh'
    jobs:
    - 'gh-build-master':
        display_name: ocm-agent build master
