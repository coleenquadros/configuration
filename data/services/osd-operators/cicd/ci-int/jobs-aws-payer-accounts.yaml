---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-payer-accounts-terraform-jobs

description: 'Aws Payer accounts setup with Terraform'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: aws-payer-accounts
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: aws-payer-accounts-terraform
    jobs:
    - 'gl-build-master-aws-payer-accounts-terraform':
        display_name: aws-payer-accounts-terraform build master
        branch: main
    - 'gl-pr-check-aws-payer-accounts-terraform':
        display_name: aws-payer-accounts-terraform pr-check
