---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-srep-pr-checks

description: 'Common job for repos only needing PR checks'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: terraform-aws-vpc-private
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: terraform-aws-vpc-private
    jobs:
    - 'gl-pr-check':
        display_name: terraform-aws-vpc-private pr-check

- project:
    name: terraform-aws-s3-bootstrap
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: terraform-aws-s3-bootstrap
    jobs:
    - 'gl-pr-check':
        display_name: terraform-aws-s3-bootstrap pr-check

- project:
    name: osd-aws-privatelink-terraform-hypershift
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: osd-aws-privatelink-terraform-hypershift
    jobs:
    - 'gl-pr-check':
        display_name: osd-aws-privatelink-terraform-hypershift pr-check

- project:
    name: osd-aws-privatelink-terraform-rosa
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: osd-aws-privatelink-terraform-rosa
    jobs:
    - 'gl-pr-check':
        display_name: osd-aws-privatelink-terraform-rosa pr-check
