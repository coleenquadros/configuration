---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-cloud-ingress-operator-jobs

description: ''

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: cloud-ingress-operator
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: cloud-ingress-operator
    quay_org: app-sre
    build_deploy_script_path: make build-push
    jobs:
    - 'gh-build-master':
        display_name: cloud-ingress-operator build master
