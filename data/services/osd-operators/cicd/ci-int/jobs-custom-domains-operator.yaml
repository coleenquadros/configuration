---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-custom-domains-operator-jobs

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: custom-domains-operator
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: custom-domains-operator
    quay_org: app-sre
    build_deploy_script_path: make build-push
    jobs:
    - 'gh-build-master':
        display_name: custom-domains-operator build master
