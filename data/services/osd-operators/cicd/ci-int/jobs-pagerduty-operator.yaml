---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-pagerduty-operator-jobs

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: pagerduty-operator
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: pagerduty-operator
    quay_org: app-sre
    build_deploy_script_path: './hack/app_sre_build_deploy.sh'
    jobs:
    - 'gh-build-master':
        display_name: pagerduty-operator build master

- project:
    name: pagerduty-operator-saas
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: saas-osd-operators
    kube_cluster: hive-production
    kube_namespace: pagerduty-operator
    saasherder_context: pagerduty-operator
    image_pattern: '^quay\.io/app-sre/'
    quay_org: app-sre
    ssh_ci_ext: yes
    disable_saasherder_delete: true
    disable_saasherder_validate: true
    jobs:
    - 'saas-deploy':
        display_name: pagerduty-operator saas deploy
    - 'saas-pr-check':
        display_name: pagerduty-operator saas pr-check
