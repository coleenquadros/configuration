---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-osd-cluster-ready-jobs

description: 'osd-cluster-ready image build process'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: osd-cluster-ready
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: osd-cluster-ready
    quay_org: app-sre
    build_deploy_script_path: make osd-container-image-build-push
    jobs:
    - 'gh-build-master':
        display_name: osd-cluster-ready build master
        branch: master
