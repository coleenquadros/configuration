---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-ids-images-jobs

description: 'ids-images container build'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: ids-images
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: ids-images
    quay_org: app-sre
    build_deploy_script_path: 'make build-push'
    pr_check_script_path: 'make build'
    backup_ecr_push_creds_account_region: 'app-sre/us-east-1'
    jobs:
    - 'gl-build-master':
        display_name: ids-images build main
        branch: main
    - 'gl-pr-check':
        display_name: ids-images pr-check
