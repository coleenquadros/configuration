
---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-osd-status-checker-jobs

description: 'Periodic Operator Version Status Checker'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: osd-operator-status
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: osd-operator-status
    jobs:
    - 'osd-operator-status-periodic-check':
        display_name: osd-operator-status periodic check 
        branch: test
        job_type: periodic-integrations
        ci_cmd: make run
        cron_expression: 'H * * * *'
