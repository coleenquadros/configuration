---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-clamav-images-jobs

description: 'clamav-images container build'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: clamav-images
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: clamav-images
    quay_org: app-sre
    build_deploy_script_path: 'make build-push'
    pr_check_script_path: 'make build'
    backup_ecr_push_creds_account_region: 'app-sre/us-east-1'
    jobs:
    - 'gl-build-master':
        display_name: clamav-images build main
        branch: main
    - 'gl-pr-check':
        display_name: clamav-images pr-check
