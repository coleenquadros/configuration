---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-boilerplate-jobs

description: ''

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: boilerplate
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: boilerplate
    quay_org: app-sre
    build_deploy_script_path: 'make build-push'
    jobs:
    - 'gh-build-master':
        display_name: boilerplate build master
