---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-osd-network-verifier-jobs

description: 'osd-network-validator image build process'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: osd-network-verifier
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: osd-network-verifier
    quay_org: app-sre
    build_deploy_script_path: make osd-container-image-build-push
    jobs:
    - 'gh-build-master':
        display_name: osd-network-verifier build main
        branch: main
