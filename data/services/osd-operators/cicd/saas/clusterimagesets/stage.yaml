---
$schema: /app-sre/saas-file-1.yml

labels:
  service: osd-operators

name: saas-clusterimagesets-stage
description: SaaS tracking file for clusterimagesets

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

slack:
  output: events
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: sd-srep-releases-stage

managedResourceTypes:
- ClusterImageSet

takeover: true
compare: true

imagePatterns: []

resourceTemplates:
- name: clusterimagesets
  url: https://gitlab.cee.redhat.com/service/clusterimagesets
  path: /stage
  provider: directory
  targets:
  - namespace:
      $ref: /services/osd-operators/namespaces/hive-stage-cluster-scope.yml
    ref: master
  - namespace:
      $ref: /services/osd-operators/namespaces/hive-stage-01/cluster-scope.yml
    ref: master
  - namespace:
      $ref: /services/osd-operators/namespaces/hives02ue1/cluster-scope.yml
    ref: master
  - namespace:
      $ref: /services/osd-operators/namespaces/ssotest01ue1/cluster-scope.yml
    ref: master
