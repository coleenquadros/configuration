---
$schema: /app-sre/saas-file-2.yml

labels:
  service: osd-operators

name: saas-cis-int
description: SaaS tracking file for clusterimagesets

app:
  $ref: /services/osd-operators/app.yml

pipelinesProvider:
  $ref: /services/osd-operators/pipelines/tekton.cis-pipelines.appsrep05ue1.yaml

slack:
  output: events
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: sd-srep-releases-int

managedResourceTypes:
- ClusterImageSet

takeover: true

imagePatterns: []

resourceTemplates:
- name: clusterimagesets
  url: https://gitlab.cee.redhat.com/service/clusterimagesets
  path: /integration
  provider: directory
  targets:
  - namespace:
      $ref: /services/osd-operators/namespaces/hivei01ue1/cluster-scope.yml
    ref: master
