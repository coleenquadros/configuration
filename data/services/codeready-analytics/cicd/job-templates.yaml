---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: codeready-analytics

name: ci-ext-codeready-analytics-job-templates

description: ''

app:
  $ref: /services/codeready-analytics/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: job-templates

config_path: /jenkins/codeready-analytics/ci-ext/job-templates.yaml
