---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: codeready-analytics

name: ci-ext-codeready-analytics-views

description: ''

app:
  $ref: /services/codeready-analytics/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: codeready-analytics
    description: 'codeready-analytics view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[(codeready-analytics|CodeReady-Analytics)\].*'
