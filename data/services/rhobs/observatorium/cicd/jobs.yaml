---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: observatorium

name: ci-int-observatorium-jobs

description: ''

app:
  $ref: /services/rhobs/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
#e2e test images
- project:
    name: observatorium-integration-tests
    label: observatorium
    node: app-sre
    gh_org: observatorium
    quay_org: app-sre
    build_deploy_script_path: ./tests/integration_tests/build_deploy.sh
    jobs:
    -  'gh-build-master':
         gh_repo: configuration
         display_name: observatorium-e2e build-push main
