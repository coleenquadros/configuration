---
$schema: /access/role-1.yml

labels:
  service: observatorium

name: observatorium-thanos-receieve-controller-stage
description: openshift ServiceAccount observatorium-thanos-receieve-controller permissions to observatorium stage namespace

permissions: []

access:
- namespace:
    $ref: /services/rhobs/observatorium/observatorium-metrics/namespaces/observatorium-metrics-stage.yml
  role: edit

# TODO(kakkoyun): Do we still need this? Consider removing after testing it.
