---
$schema: /access/role-1.yml

labels:
  service: observatorium

name: thanos-receieve-controller-stage
description: openshift ServiceAccount thanos-receieve-controller permissions to observatorium stage namespace

permissions: []

access:
- namespace:
    $ref: /services/rhobs/observatorium/namespaces/observatorium-metrics-stage.yml
  role: edit

# TODO(kakkoyun): Do we still need this? Consider removing after testing it.
