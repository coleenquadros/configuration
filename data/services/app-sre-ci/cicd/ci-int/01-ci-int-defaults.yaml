---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: jenkins

name: ci-int-defaults-jenkins-configs

description: ''

app:
  $ref: /services/app-sre/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: defaults

config_path: /jenkins/ci-int/defaults.yaml
