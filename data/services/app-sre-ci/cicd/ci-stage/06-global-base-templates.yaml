---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: jenkins

name: ci-stage-global-base-templates-jenkins-configs

description: ''

app:
  $ref: /services/app-sre/app.yml

instance:
  $ref: /dependencies/ci-stage/ci-stage.yml

type: global-base-templates

config_path: /jenkins/global/base-templates.yaml
