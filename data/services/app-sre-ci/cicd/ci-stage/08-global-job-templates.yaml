---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: jenkins

name: ci-stage-global-job-templates-jenkins-configs

description: ''

app:
  $ref: /services/app-sre/app.yml

instance:
  $ref: /dependencies/ci-stage/ci-stage.yml

type: job-templates

config_path: /jenkins/global/templates.yaml
