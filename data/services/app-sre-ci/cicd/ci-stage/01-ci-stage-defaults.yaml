---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: jenkins

name: ci-stage-defaults-jenkins-configs

description: ''

app:
  $ref: /services/app-sre/app.yml

instance:
  $ref: /dependencies/ci-stage/ci-stage.yml

type: defaults

config_path: /jenkins/ci-stage/defaults.yaml
