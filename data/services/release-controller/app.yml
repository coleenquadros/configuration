---
$schema: /app-sre/app-1.yml

labels:
  service: release-controller

name: release-controller

description: |
  https://github.com/openshift/release-controller - The release controller assembles OpenShift releases from CI inputs

serviceOwner:
  name: Clayton Coleman
  email: ccoleman@redhat.com

performanceParameters:
  SLO: 90

dependencies:
- $ref: /dependencies/quay/service.yml

quayRepos:
- org:
    $ref: /dependencies/quay/app-sre.yml
  items:
  - name: release-controller
    description: 'release-controller container'
    public: true
