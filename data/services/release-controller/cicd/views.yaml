---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: release-controller

name: ci-ext-release-controller-views

description: ''

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: release-controller
    description: 'release-controller view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[release-controller\].*'
