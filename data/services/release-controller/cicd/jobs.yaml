---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: release-controller

name: ci-ext-release-controller-jobs

description: ''

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: release-controller
    label: release-controller
    node: hive
    gh_org: openshift
    gh_repo: release-controller
    quay_org: app-sre
    kube_cluster: app-sre-stage
    kube_namespace: release-controller-stage
    saasherder_context: release-controller
    saasherder_services: release-controller
    jobs:
    - 'gh-build-master-release-controller':
        display_name: build master
