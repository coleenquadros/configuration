---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: services-dashboard

name: ci-int-services-dashboard-jobs

description: 'Build the Services Dashboad "dashload" image'

app:
  $ref: /services/services-dashboard/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: services-dashboard
    label: services-dashboard
    node: rhel8
    gl_group: service
    gl_project: dashboard
    quay_org: app-sre
    jobs:
    - 'gl-build-master':
        display_name: Services Dashboard build main
        branch: main
