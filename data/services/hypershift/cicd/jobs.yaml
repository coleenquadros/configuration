---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: hypershift

name: ci-int-hypershift-jobs

description: ''

app:
  $ref: /services/hypershift/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: hypershift
    label: hypershift
    node: rhel8
    gh_org: openshift
    gh_repo: hypershift
    quay_org: app-sre
    build_deploy_script_path: './hack/app-sre/build_push.sh'
    jobs:
    - 'gh-build-master':
        display_name: hypershift build main
        branch: main
