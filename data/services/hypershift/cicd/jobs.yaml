---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: hypershift

name: ci-ext-hypershift-jobs

description: ''

app:
  $ref: /services/hypershift/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: hypershift
    label: hypershift
    node: rhel8
    gh_org: geoberle
    gh_repo: hypershift
    quay_org: app-sre
    build_deploy_script_path: './hack/app-sre/build_push.sh'
    jobs:
    - 'gh-build-master':
        display_name: hypershift build internal
        branch: internal
