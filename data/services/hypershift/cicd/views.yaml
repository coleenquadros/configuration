---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: hypershift

name: ci-ext-hypershift-views

description: ''

app:
  $ref: /services/hypershift/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: hypershift
    description: 'hypershift view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[hypershift\].*'
