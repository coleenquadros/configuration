---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: sre-ssh-proxy

name: ci-ext-sre-ssh-proxy-views

description: ''

app:
  $ref: /services/srep-sshd/app.yaml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: sre-ssh-proxy
    description: 'sre-ssh-proxy'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[sre-ssh-proxy\].*'
