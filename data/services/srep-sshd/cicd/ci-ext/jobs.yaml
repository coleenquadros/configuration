---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: openshift-sre-sshd

name: openshift-sre-sshd

description: ''

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: srep-sshd 
    label: srep-sshd 
    node: app-sre 
    gh_org: openshift
    gh_repo: openshift-sre-sshd-container
    quay_org: app-sre
    pr_check_script_path: './hack/app_sre_pr_check.sh'
    build_deploy_script_path: './hack/app_sre_build_deploy.sh'
    jobs:
      - 'gh-build-master':
          display_name: openshift-sre-sshd build master
      - 'gh-pr-check':
          display_name: openshift-sre-sshd pr-check
