
---
$schema: /app-sre/saas-file-2.yml

labels:
  service: saas-glitchtip

name: saas-glitchtip
description: SaaS tracking file for Glichtip

app:
  $ref: /services/glitchtip/app.yml

pipelinesProvider:
  $ref: /services/glitchtip/pipelines/tekton.glitchtip-pipelines.app-sre-prod-01.yml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: managed-by-cssre

authentication:
  image:
    path: cs-sre/creds/quay/pull
    field: all
    version: 3

managedResourceTypes:
- Deployment
- Service
- CronJob
- ServiceAccount
- ConfigMap
- Job

imagePatterns:
- quay.io/cs-sre/glitchtip

resourceTemplates:
- name: glitchtip
  url: https://github.com/rh-cssre/glitchtip-frontend
  path: /openshift/template.yaml
  targets:
  - namespace:
      $ref: /services/glitchtip/namespaces/glitchtip-stage.yml
    ref: master
    upstream:
      instance:
        $ref: /dependencies/ci-ext/ci-ext.yml
      name: rh-cssre-glitchtip-frontend-gh-build-master

- name: glitchtip-stage
  url: https://github.com/rh-cssre/glitchtip-frontend
  path: /openshift/grafana
  provider: directory
  targets:
  - namespace:
      $ref: /services/observability/namespaces/app-sre-observability-stage.yml
    ref: master
