---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: glitchtip

name: ci-ext-github-mirror-jobs

description: 'Glitchtip Error Tracking Integration Job'

app:
  $ref: /services/glitchtip/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: glitchtip
    label: glitchtip
    node: app-sre
    gh_org: rh-cssre
    gh_repo: glitchtip-frontend
    quay_org: cs-sre
    jobs:
    - 'gh-build-master':
        display_name: glitchtip build master
    - 'gh-pr-check':
        display_name: glitchtip pr-check
- project:
    name: glitchtip-app-sre
    label: glitchtip
    node: app-sre
    gh_org: app-sre
    gh_repo: glitchtip
    quay_org: app-sre
    jobs:
    - 'gh-build-master':
        branch: main
        display_name: glitchtip build main
    - 'gh-pr-check':
        branch: main
        display_name: glitchtip pr-check
