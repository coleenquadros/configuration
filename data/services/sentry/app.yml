---
$schema: /app-sre/app-1.yml


labels:
  service: sentry

name: Sentry

description: |
  Sentry is an error tracking tool.

serviceOwners:
- name: App-SRE Team
  email: sd-app-sre@redhat.com

dependencies:
- $ref: /dependencies/ci-ext/service.yml
- $ref: /dependencies/ci-int/service.yml
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/gitlab/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/openshift/service.yml

performanceParameters:
  SLO: 90

quayRepos:
- org:
    $ref: /dependencies/quay/app-sre.yml
  items:
  - name: sentry
    description: Error tracking tool
    public: true

codeComponents:
- name: sentry
  resource: upstream
  url: https://github.com/app-sre/sentry
- name: saas-sentry
  resource: saasrepo
  url: https://gitlab.cee.redhat.com/service/saas-sentry
