---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: sentry

name: ci-ext-sentry-views

description: ''

app:
  $ref: /services/sentry/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: sentry
    description: 'sentry view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[(sentry|Sentry)\].*'
