---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: sentry

name: ci-int-sentry-views

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: sentry
    description: 'sentry view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[(sentry|Sentry)\].*'
