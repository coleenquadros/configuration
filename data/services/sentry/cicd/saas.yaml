---
$schema: /app-sre/saas-file-2.yml

labels:
  service: sentry

name: saas-sentry
description: SaaS tracking file for Sentry

app:
  $ref: /services/sentry/app.yml

pipelinesProvider:
  $ref: /services/app-interface/pipelines/tekton.app-sre-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/redhat-internal.yml
  channel: sd-app-sre-info

authentication:
  image:
    path: app-sre/quay/app-sre-pull
    field: all

managedResourceTypes:
- Deployment
- Service
- CronJob
- ServiceAccount
- ConfigMap

imagePatterns:
- quay.io/app-sre/sentry
- quay.io/app-sre/redis-5

resourceTemplates:
- name: sentry
  url: https://github.com/app-sre/sentry
  path: /sentry.yaml
  parameters:
    SENTRY_SINGLE_ORGANIZATION: "true"
    REPLICAS_WEB: "0"
    REPLICAS_WORKER: "0"
  targets:
  - namespace:
      $ref: /services/sentry/namespaces/sentry-stage.yaml
    ref: master
    upstream:
      instance:
        $ref: /dependencies/ci-ext/ci-ext.yml
      name: app-sre-sentry-gh-build-master
    parameters:
      CLEANUP_DAYS: 7
      IMAGE_TAG: 415f18f
  - namespace:
      $ref: /services/sentry/namespaces/sentry-production.yaml
    ref: 068b92ad3bee99ce8a999399f37fcfdab295be08
    parameters:
      CLEANUP_DAYS: 14
      IMAGE_TAG: 415f18f
