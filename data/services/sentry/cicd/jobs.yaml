---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: sentry

name: ci-ext-sentry-jobs

description: ''

app:
  $ref: /services/sentry/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: sentry
    label: sentry
    node: app-sre
    gh_org: app-sre
    gh_repo: sentry
    quay_org: app-sre
    jobs:
    - 'gh-build-master':
        display_name: build master
