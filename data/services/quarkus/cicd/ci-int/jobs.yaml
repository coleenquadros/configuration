---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: launcher

name: ci-int-launcher-jobs

description: ''

app:
  $ref: /services/quarkus/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: code-quarkus-redhat-int-build-ci
    label: code-quarkus-redhat-int-build
    node: app-sre
    gh_org: redhat-developer
    gh_repo: code.quarkus.redhat.com
    quay_org: app-sre
    jobs:
    - 'gh-build-master':
          branch: internal
          build_deploy_script_path: ./build_deploy_int.sh
          environment: stage
          display_name: code.quarkus.redhat.com for internal builds master
