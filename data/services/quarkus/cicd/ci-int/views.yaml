---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: quarkus

name: ci-int-quarkus-views

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: quarkus
    description: 'Quarkus view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '.*(q|Q)uarkus.*'
