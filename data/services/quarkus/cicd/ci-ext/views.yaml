---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: quarkus

name: ci-ext-code-quarkus-views

description: ''

app:
  $ref: /services/quarkus/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: quarkus
    description: 'Quarkus view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[[^]]*(q|Q)uarkus[^]]*\].*'
