---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: registry-quarkus-redhat

name: ci-int-registry-quarkus-jobs

description: "Build for registry.quarkus.redhat.com"

app:
  $ref: /services/quarkus/registry/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
  - project:
      name: registry-quarkus-redhat
      label: registry-quarkus-redhat
      node: quarkus
      gl_group: quarkus
      gl_project: registry.quarkus.redhat.com
      quay_org: app-sre
      jobs:
        - "gl-build-master":
            branch: main
            display_name: registry.quarkus.redhat.com build main
        - "gl-pr-check":
            display_name: registry.quarkus.redhat.com pr-check
