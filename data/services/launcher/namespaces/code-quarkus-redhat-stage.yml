---
$schema: /openshift/namespace-1.yml

labels:
  service: launcher

name: code-quarkus-redhat-stage
description: Staging namespace for code.quarkus.redhat.com

cluster:
  $ref: /openshift/app-sre-stage-01/cluster.yml

app:
  $ref: /services/launcher/app.yml

environment:
  $ref: /products/quarkus/environments/stage.yml

networkPoliciesAllow:
- $ref: /services/observability/namespaces/openshift-customer-monitoring.app-sre-stage-01.yml

openshiftAcme:
  config:
    $ref: /dependencies/openshift/acme/v4.yml
  accountSecret:
    provider: vault-secret
    path: app-interface/app-sre-stage/code-quarkus-stage/acme-account
    version: 1

managedRoles: true

managedResourceTypes:
- Route
- Secret

openshiftResources:
- provider: route
  path: /app-sre-stage/code-quarkus-redhat-stage/code-quarkus-redhat.route.yaml
- provider: vault-secret
  path: app-interface/app-sre-stage/code-quarkus-redhat-stage/secrets
  version: 5
