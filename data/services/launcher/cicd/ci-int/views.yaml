---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: launcher

name: ci-int-code-quarkus-views

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: code-quarkus-redhat
    description: 'code.quarkus.redhat.com view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[code-quarkus-redhat\].*'

