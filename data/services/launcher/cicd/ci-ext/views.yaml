---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: launcher

name: ci-ext-code-quarkus-views

description: ''

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: code-quarkus
    description: 'code.quarkus.io view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[code-quarkus\].*'
- view:
    name: code-quarkus-redhat
    description: 'code.quarkus.redhat.com view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[code-quarkus-redhat\].*'

