---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: launcher

name: ci-ext-launcher-quarkus-views

description: ''

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: launcher-quarkus
    description: 'launcher quarkus view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[launcher-quarkus\].*'
