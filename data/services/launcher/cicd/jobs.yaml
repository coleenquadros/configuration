---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: launcher

name: ci-ext-launcher-jobs

description: ''

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: launcher-quarkus-ci
    label: launcher-quarkus
    node: app-sre
    gh_org: launcher
    gh_repo: launcher-quarkus
    quay_org: launcher
    jobs:
    - 'gh-build-master-launcher-quarkus':
        display_name: launcher-quarkus build master
