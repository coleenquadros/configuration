---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: mas-sso

name: ci-int-mas-sso-views

description: ''

app:
  $ref: /services/mas-sso/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: mas-sso
    description: 'mas-sso view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[mas-sso\].*'
