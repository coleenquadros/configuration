
---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: mas-sso

name: ci-int-mas-sso-jobs

description: ''

app:
  $ref: /services/mas-sso/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: ocm-mas-sso-service
    label: mas-sso
    node: managed-services
    gl_group: service
    gl_project: saas-mas-sso
    quay_org: app-sre
    jobs:
      - 'gl-pr-check-mas-sso':
          display_name: saas-mas-sso pr-check
          pr_check_script_path: './pr_check.sh'
      - mas-sso-build-template:
          display_name: mas-sso service build master
          branch: master
          build_deploy_script_path: "./build_deploy.sh"
