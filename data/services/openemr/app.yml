---
$schema: /app-sre/app-1.yml

labels:
  service: OpenEMR

name: OpenEMR

description: |
  Container app for OpenEMR related content

serviceOwners:
- name: Jeff Pullen
  email: jpullen@redhat.com

dependencies:
- $ref: /dependencies/github/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/ci-ext/service.yml

performanceParameters:
  SLO: 90

codeComponents:
- name: openemr-kube
  resource: upstream
  url: https://github.com/RedHatOfficial/openemr-kube
