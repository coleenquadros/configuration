---
$schema: /access/role-1.yml

labels:
  service: telemeter

name: thanos-receieve-controller-stage
description: openshift ServiceAccount thanos-receieve-controller permissions to telemeter stage namespace

permissions: []

access:
- namespace:
    $ref: /services/telemeter/namespaces/telemeter-stage.yml
  role: edit
