---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: telemeter

name: ci-int-telemeter-jobs

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: telemeter-ci
    label: telemeter
    node: app-sre
    gh_org: openshift
    gh_repo: telemeter
    quay_org: app-sre
    kube_cluster: app-sre-stage
    kube_namespace: telemeter-stage
    saasherder_context: telemeter
    saasherder_services: prometheus-operator prometheus-telemeter
    saasherder_object_blacklist: 'Route,Secret,ConfigMap,ClusterRole,ClusterRoleBinding' # Default: Route,Secret,ConfigMap,ServiceAccount
    saas_git: https://gitlab.cee.redhat.com/service/saas-telemeter.git
    jobs:
    - 'gh-build-master':
        display_name: telemeter build master

- project:
    name: observatorium-ci
    label: telemeter
    node: app-sre
    gh_org: observatorium
    gh_repo: configuration
    quay_org: app-sre
    kube_cluster: app-sre-stage
    kube_namespace: telemeter-stage
    saasherder_context: telemeter
    saasherder_services: observatorium observatorium-jaeger openshift-acme
    saasherder_object_blacklist: 'Route,Secret,ConfigMap,ClusterRole,ClusterRoleBinding' # Default: Route,Secret,ConfigMap,ServiceAccount
    saas_git: https://gitlab.cee.redhat.com/service/saas-telemeter.git
    jobs:
    - 'gh-build-master':
        display_name: configuration build master

- project:
    name: observatorium-nginx
    label: telemeter
    node: app-sre
    gh_org: observatorium
    gh_repo: nginx
    quay_org: app-sre
    kube_cluster: app-sre-stage
    kube_namespace: telemeter-stage
    build_deploy_script_path: "./build_deploy.sh"
    jobs:
    - 'gh-build-master':
        display_name: nginx build master

- project:
    name: telemeter-saas
    label: telemeter
    node: app-sre
    gl_group: service
    gl_project: saas-telemeter
    kube_cluster: app-sre
    kube_namespace: telemeter-production
    saasherder_context: telemeter
    saasherder_services: prometheus-operator prometheus-telemeter telemeter-server
    saasherder_object_blacklist: 'Route,Secret,ConfigMap,ClusterRole,ClusterRoleBinding' # Default: Route,Secret,ConfigMap,ServiceAccount
    image_pattern: '^(quay\.io/(thanos/thanos|cortexproject/cortex|observatorium/thanos-receive-controller|app-sre/jaegertracing-all-in-one|app-sre/jaegertracing-jaeger-agent|app-sre/telemeter|app-sre/observatorium-receive-proxy|app-sre/openshift-oauth-proxy))'
    quay_org: app-sre
    jobs:
    - 'saas-deploy':
        display_name: saas deploy
    - 'saas-pr-check':
        display_name: saas pr check
