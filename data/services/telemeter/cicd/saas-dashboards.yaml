---
$schema: /app-sre/saas-file-1.yml

labels:
  service: telemeter

name: saas-telemeter-grafana-dashboards
description: SaaS tracking file for Telemeter Grafana dashboards

app:
  $ref: /services/telemeter/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: team-monitoring-info

managedResourceTypes:
- ConfigMap

imagePatterns: []

resourceTemplates:
- name: dashboards
  url: https://gitlab.cee.redhat.com/observatorium/configuration
  path: /manifests/production/grafana
  provider: directory
  targets:
  - namespace:
      $ref: /services/observability/namespaces/app-sre-observability-stage.yml
    ref: master
