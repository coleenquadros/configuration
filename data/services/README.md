# App SRE Managed Services

Each subdirectory represents a top level service being managed by the App SRE team. Children of those directories may represent microservices that compose the top level service.