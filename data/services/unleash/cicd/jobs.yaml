---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: unleash

name: ci-ext-unleash-jobs

description: ''

app:
  $ref: /services/unleash/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: unleash
    label: unleash
    node: app-sre
    gh_org: app-sre
    gh_repo: unleash
    quay_org: app-sre
    jobs:
    - 'gh-build-master':
        display_name: unleash build master
    - 'gh-pr-check':
        display_name: unleash pr-check
