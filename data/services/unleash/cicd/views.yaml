---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: unleash

name: ci-ext-unleash-views

description: ''

app:
  $ref: /services/unleash/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: unleash
    description: 'unleash view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[unleash\].*'
