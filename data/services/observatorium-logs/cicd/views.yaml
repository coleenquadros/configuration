---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: observatorium-logs

name: ci-int-observatorium-logs-views

description: ''

app:
  $ref: /services/observatorium-logs/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: observatorium-logs
    description: 'observatorium logs view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[observatorium-logs.*\].*'
