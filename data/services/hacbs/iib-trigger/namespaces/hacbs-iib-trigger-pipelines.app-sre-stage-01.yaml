---
$schema: /openshift/namespace-1.yml

labels:
  provider: tekton

name: hacbs-iib-trigger-pipelines
description: hacbs-iib-trigger pipelines namespace

cluster:
  $ref: /openshift/app-sre-stage-01/cluster.yml

app:
  $ref: /services/app-sre/app.yml

environment:
  $ref: /products/app-sre/environments/production.yml

managedRoles: true

managedResourceTypes:
- Secret
- ClusterRole

sharedResources:
- $ref: /services/app-interface/shared-resources/app-sre-pipelines.yml

openshiftResources:
- provider: vault-secret
  name: registry.redhat.io-creds
  path: hacbs/hacbs-iib-trigger-pipelines/staging/registry.redhat.io-creds
  version: 1
  type: kubernetes.io/dockerconfigjson
  annotations:
    qontract.recycle: "true"
