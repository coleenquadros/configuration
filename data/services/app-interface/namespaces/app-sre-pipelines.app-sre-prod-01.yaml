---
$schema: /openshift/namespace-1.yml

labels:
  provider: tekton

name: app-sre-pipelines
description: app-sre pipelines namespace

cluster:
  $ref: /openshift/app-sre-prod-01/cluster.yml

app:
  $ref: /services/app-sre/app.yml

environment:
  $ref: /products/app-sre/environments/production.yml

managedRoles: true

managedResourceTypes:
- Secret
- Task
- Pipeline
- ClusterRole

sharedResources:
- $ref: /services/app-interface/shared-resources/app-sre-pipelines.yml

openshiftResources:
- provider: vault-secret
  path: app-sre/creds/splunk/tekton-int
  version: 1
  name: splunk-int
- provider: vault-secret
  path: app-sre/creds/splunk/tekton-ext
  version: 1
  name: splunk-ext
