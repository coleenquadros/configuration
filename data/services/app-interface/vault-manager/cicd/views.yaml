---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vault-manager

name: ci-ext-vault-manager-views

description: ''

app:
  $ref: /services/app-interface/vault-manager/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: vault-manager
    description: 'vault-manager view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[vault-manager\].*'
