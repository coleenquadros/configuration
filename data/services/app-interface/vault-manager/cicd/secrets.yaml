---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vault-manager

name: ci-ext-vault-manager-secrets

description: ''

app:
  $ref: /services/app-interface/vault-manager/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: secrets

config_path: /jenkins/app-sre/vault-manager/secrets.yaml
