---
$schema: /app-sre/saas-file-2.yml

labels:
  service: app-interface

name: saas-git-partition-sync-producer
description: 'SaaS tracking file for git-partition-sync-producer'

app:
  $ref: /services/app-interface/git-partition-sync/app.yml

pipelinesProvider:
  $ref: /services/app-interface/pipelines/tekton.app-sre-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: sd-app-sre-info

managedResourceTypes:
- ServiceAccount
- Deployment

imagePatterns:
- quay.io/app-sre/git-partition-sync-producer

resourceTemplates:
- name: git-partition-sync-producer
  url: https://github.com/app-sre/git-partition-sync-producer
  path: /openshift/producer.yaml
  targets:
  - namespace:
      $ref: /services/app-interface/namespaces/app-interface-stage-int.yml
    ref: master
    upstream:
      instance:
        $ref: /dependencies/ci-ext/ci-ext.yml
      name: app-sre-git-partition-sync-producer-gh-build-master
    parameters:
      DRY_RUN: true
      RUN_ONCE: false
      VAULT_SECRET_NAME: git-partition-sync-producer
      RECONCILE_SLEEP_TIME: 15m
      GQL_SECRET_NAME: gql-creds
      GRAPHQL_QUERY_FILE: /query.graphql
