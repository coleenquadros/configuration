---
$schema: /app-sre/saas-file-2.yml

labels:
  service: app-interface

name: saas-git-partition-sync-producer
description: 'SaaS tracking file for git-partition-sync-producer'

app:
  $ref: /services/app-interface/git-partition-sync/app.yml

pipelinesProvider:
  $ref: /services/app-interface/pipelines/tekton.app-sre-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: sd-app-sre-info

managedResourceTypes:
- ServiceAccount
- Deployment

imagePatterns:
- quay.io/app-sre/git-partition-sync-producer

resourceTemplates: []
