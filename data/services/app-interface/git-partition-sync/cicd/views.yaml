---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: git-partition-sync

name: ci-ext-git-partition-sync-views

description: ''

app:
  $ref: /services/app-interface/git-partition-sync/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: git-partition-sync
    description: 'git-partition-sync view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[git-partition-sync\].*'
