---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: app-interface

name: ci-ext-go-qontract-reconcile-views

description: ''

app:
  $ref: /services/app-interface/go-qontract-reconcile/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: go-qontract-reconcile
    description: 'go-qontract-reconcile view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[go-qontract-reconcile\].*'
