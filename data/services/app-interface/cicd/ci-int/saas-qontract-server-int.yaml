---
$schema: /app-sre/saas-file-2.yml

labels:
  service: app-interface

name: saas-qontract-server-int
description: 'SaaS tracking file for App-interface'

app:
  $ref: /services/app-interface/app.yml

pipelinesProvider:
  $ref: /services/app-interface/pipelines/tekton.app-sre-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/redhat-internal.yml
  channel: sd-app-sre-info

managedResourceTypes:
- Deployment
- Service
- ServiceAccount
- PodDisruptionBudget

imagePatterns:
- quay.io/app-sre/qontract-server
- quay.io/app-sre/nginx-gate
- quay.io/app-sre/s3-reload
- quay.io/app-sre/busybox

resourceTemplates:
- name: qontract-server
  url: https://github.com/app-sre/qontract-server
  path: /openshift/app-interface.yaml
  parameters:
    IMAGE_GATE_TAG: 49a928c
    REPLICAS: 3
    MAX_OLD_SPACE_SIZE: "800"
    APP_INTERFACE_RESOURCES:
      requests:
        cpu: 900m
        memory: 512Mi
      limits:
        memory: 1Gi
    GATE_RESOURCES:
      requests:
        cpu: 100m
        memory: 100Mi
      limits:
        memory: 100Mi
    S3RELOADER_RESOURCES:
      requests:
        cpu: 30m
        memory: 20Mi
      limits:
        memory: 40Mi
    IMAGE_GATE_AUTH_DISABLE: "true"
  targets:
  - namespace:
      $ref: /services/app-interface/namespaces/app-interface-dev-int.yml
    ref: master
    parameters:
      REPLICAS: 2
      APP_INTERFACE_RESOURCES:
        requests:
          cpu: 50m
          memory: 512Mi
        limits:
          cpu: 500m
          memory: 512Mi
      IMAGE_GATE_AUTH_DISABLE: "true"
  - namespace:
      $ref: /services/app-interface/namespaces/app-interface-production-int.yml
    ref: b04f3df8d90a9180197e4025b562431bd5a25dfc
