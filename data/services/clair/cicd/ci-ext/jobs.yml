---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: clair

name: ci-ext-clair-jobs

description: ''

app:
  $ref: /services/clair/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: clair
    label: clair
    node: app-sre
    gh_org: quay
    gh_repo: clair
    quay_org: app-sre
    build_deploy_script_path: ./contrib/openshift/build_and_deploy.sh
    pr_check_script_path: ./contrib/openshift/pr_check.sh
    jobs:
    - 'gh-build-master':
        display_name: clair build master
    - 'gh-pr-check':
        display_name: clair pr-check
