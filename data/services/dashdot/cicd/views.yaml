---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: dashdot

name: ci-ext-dashdot-views

description: ''

app:
  $ref: /services/dashdot/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: dashdotdb
    description: 'dashdotdb view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[dashdotdb\].*'
