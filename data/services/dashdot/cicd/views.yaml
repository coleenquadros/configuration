---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: dashdotdb

name: ci-ext-dashdotdb-views

description: ''

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: dashdotdb
    description: 'dashdotdb view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[dashdotdb\].*'
