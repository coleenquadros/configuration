---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: assisted-installer

name: ci-ext-assisted-installer-views

description: ''

app:
  $ref: /services/assisted-installer/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: assisted-installer
    description: 'assisted-installer view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[assisted-installer\].*'
- view:
    name: assisted-service
    description: 'assisted-service view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[assisted-service\].*'
