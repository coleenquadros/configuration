---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osde2e

name: ci-int-osde2e-jobs

description: ''

app:
  $ref: /services/sd-cicd/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: osde2e-parameterized-job
    label: osde2e
    node: osde2e
    gh_org: openshift
    gh_repo: osde2e
    jobs:
      # ---- run script
    - 'osde2e-parameterized-job':
        branch: main
        display_name: "osde2e parameterized job"
        ci_cmd: './scripts/parameterized-job.sh'
