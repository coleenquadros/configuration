---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osde2e

name: ci-int-osde2e-views

description: ''

app:
  $ref: /services/sd-cicd/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: osde2e
    description: 'osde2e view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[osde2e\].*'
