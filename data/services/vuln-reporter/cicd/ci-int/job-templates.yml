---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: app-sre

name: ci-int-vuln-reporter-job-templates

description: 'vuln-reporter Jenkins job templates'

app:
  $ref: /services/vuln-reporter/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: job-templates

config_path: /jenkins/app-sre/vuln-reporter/job-templates.yml
