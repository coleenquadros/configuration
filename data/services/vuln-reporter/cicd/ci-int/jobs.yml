---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vuln-reporter

name: ci-int-vuln-reporter-jobs

description: 'build job for vuln-reporter'

app:
  $ref: /services/vuln-reporter/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: vuln-reporter
    label: vuln-reporter
    node: app-sre
    gl_group: app-sre
    gl_project: vuln-reporter
    quay_org: app-sre
    jobs:
    - 'gl-build-master':
        display_name: vuln-reporter build master
