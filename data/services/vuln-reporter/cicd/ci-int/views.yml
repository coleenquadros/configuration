---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vuln-reporter

name: ci-int-vuln-reporter-views

description: ''

app:
  $ref: /services/vuln-reporter/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: vuln-reporter
    description: 'vuln-reporter view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[vuln-reporter\].*'
