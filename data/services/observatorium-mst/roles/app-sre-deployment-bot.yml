---
$schema: /access/role-1.yml

labels:
  service: observatorium-mst

name: app-sre-deployment-bot-observatorium-mst
description: openshift ServiceAccount app-sre-deployment-bot permissions to observatorium-mst

permissions: []

access:
- namespace:
    $ref: /services/observatorium-mst/namespaces/observatorium-mst-stage.yml
  role: admin
- namespace:
    $ref: /services/observatorium-mst/namespaces/observatorium-mst-stage.yml
  role: view
# TODO(kakkoyun): Enable with production MR.
# - namespace:
#     $ref: /services/observatorium-mst/namespaces/observatorium-mst-production.yml
#   role: admin
# - namespace:
#     $ref: /services/observatorium-mst/namespaces/observatorium-mst-production.yml
#   role: view
