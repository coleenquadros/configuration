---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: sd-scale

name: ci-ext-sd-scale-jobs

description: ''

app:
  $ref: /services/sd-scale/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: sd-scale
    label: sd-scale
    node: app-sre
    jobs:
    - 'sd-scale-test':
        display_name: Scale test runner

