---
$schema: /access/role-1.yml

labels:
  service: vault

name: app-sre-deployment-bot-vault
description: openshift ServiceAccount app-sre-deployment-bot permissions to the app-sre vault service

permissions: []

access:
- namespace:
    $ref: /services/vault.devshift.net/namespaces/vault-prod.yml
  role: admin
