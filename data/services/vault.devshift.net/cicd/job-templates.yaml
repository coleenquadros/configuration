---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vault

name: ci-int-vault-templates

description: ''

app:
  $ref: /services/vault.devshift.net/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: job-templates

config_path: /jenkins/vault.devshift.net/job-templates.yaml
