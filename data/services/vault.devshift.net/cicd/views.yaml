---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vault

name: ci-int-vault-views

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: vault-devshift-net
    description: 'vault-devshift-net view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[vault\.devshift\.net\].*'
