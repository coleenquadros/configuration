---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vault

name: ci-int-vault-jobs

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: vault-devshift-net
    label: vault-devshift-net
    node: app-sre
    gl_group: service
    gl_project: vault-devshift-net
    kube_cluster: app-sre
    kube_namespace: vault-stage
    saasherder_context: vault-devshift-net
    saasherder_services: vault-devshift-net
    saas_git: https://gitlab.cee.redhat.com/service/saas-vault-devshift-net.git
    quay_org: app-sre
    jobs:
    - 'gl-build-master':
        display_name: build master

- project:
    name: vault-devshift-net-cd
    label: vault-devshift-net
    node: app-sre
    gl_group: service
    gl_project: saas-vault-devshift-net
    kube_cluster: app-sre
    kube_namespace: vault-prod
    saasherder_context: vault-devshift-net
    quay_org: app-sre
    jobs:
    - 'saas-deploy':
        display_name: promote-to-prod
    - 'saas-pr-check':
        display_name: promote-to-prod pr check
