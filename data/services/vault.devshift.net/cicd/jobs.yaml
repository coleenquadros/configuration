---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vault

name: ci-int-vault-jobs

description: ''

app:
  $ref: /services/vault.devshift.net/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: vault-devshift-net
    label: vault-devshift-net
    node: app-sre
    gl_group: service
    gl_project: vault-devshift-net
    environment: stage
    jobs:
    - 'gl-test-vault-devshift-net-stage-access':
        display_name: test vault devshift stage access
