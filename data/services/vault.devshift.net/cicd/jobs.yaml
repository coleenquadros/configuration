---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: vault

name: ci-int-vault-jobs

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: vault-devshift-net-cd
    label: vault-devshift-net
    node: app-sre
    gl_group: service
    gl_project: saas-vault-devshift-net
    kube_cluster: app-sre-prod-01
    kube_namespace: vault-prod
    saasherder_context: vault-devshift-net
    quay_org: app-sre
    manifest_bouncer_options: '--disable-burstable --enable-requests --enable-limits'
    jobs:
    - 'saas-deploy':
        display_name: promote-to-prod
    - 'saas-pr-check':
        display_name: promote-to-prod pr check
