---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: control-plane-service-approle-policy
instance:
  $ref: /services/vault.devshift.net/config/prod/devshift-net.yml
rules: |
  path "control-plane-service/*" {
    capabilities = ["read"]
  }
