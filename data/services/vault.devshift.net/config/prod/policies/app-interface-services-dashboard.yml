$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "app-interface-services-dashboard"
instance:
  $ref: /services/vault.devshift.net/config/prod/devshift-net.yml
rules: |
  path "app-interface/metadata/" {
    capabilities = ["list"]
  }
  path "app-interface/metadata/services-dashboard/" {
    capabilities = ["list", "read"]
  }
  path "app-interface/data/services-dashboard/*" {
    capabilities = ["create", "update", "read"]
  }
