---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "insights-qe-replication-policy"
instance:
  $ref: /services/vault.devshift.net/config/ci-ext/ci-ext.yml
rules: |
  path "insights/secrets/qe/*" {
    capabilities = ["read"]
  }
