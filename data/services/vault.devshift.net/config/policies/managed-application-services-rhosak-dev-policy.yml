---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "managed-application-services-rhosak-dev"
rules: |
  path "managed-application-services/RHOSAK/Developer/*" {
    capabilities = ["create", "read", "update", "delete", "list"]
  }

