---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "devtools-osio-ci-admin-policy"
rules: |
  # policy for administrating CICO secrets
  path "devtools-osio-ci/*" {
    capabilities = ["read", "create", "list", "update", "delete"]
  }
