---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "managed-application-services-mk-security-policy"
rules: |
  path "managed-application-services/MK-Security-Team/*" {
    capabilities = ["create", "read", "update", "delete", "list"]
  }

