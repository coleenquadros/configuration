---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "cs-sre-aws-policy"
instance:
  $ref: /services/vault.devshift.net/config/instances/devshift-net.yml
rules: |
  path "cs-sre-aws/*" {
    capabilities = ["create", "read", "update", "delete", "list"]
  }
