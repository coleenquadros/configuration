---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "managed-connectors-services-admin-policy"
rules: |
  path "managed-connectors-services/*" {
    capabilities = ["create", "read", "update", "delete", "list"]
  }
  
