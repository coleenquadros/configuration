---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "managed-services-control-plane-read-policy"
rules: |
  path "managed-application-services/team-control-plane/*" {
    capabilities = ["list", "read"]
  }
