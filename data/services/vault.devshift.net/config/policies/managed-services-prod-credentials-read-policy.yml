---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "managed-services-prod-credentials-read-policy"
rules: |
  path "app-interface/data/managed-service-api/production/*" {
    capabilities = ["read", "list"]
  }
