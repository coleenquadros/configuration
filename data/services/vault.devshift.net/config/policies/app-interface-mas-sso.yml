---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "app-interface-mas-sso"
instance:
  $ref: /services/vault.devshift.net/config/instances/devshift-net.yml
rules: |
  path "app-interface/metadata/" {
    capabilities = ["list"]
  }
  path "app-interface/metadata/mas-sso/" {
    capabilities = ["list"]
  }
  path "app-interface/metadata/mas-sso/stage/*" {
    capabilities = ["list", "read"]
  }
  path "app-interface/data/mas-sso/stage/*" {
    capabilities = ["create", "update", "read"]
  }
