---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "managed-application-services-mk-control-plane-full-policy"
rules: |
  
  path "managed-application-services/MK-Control-Plane-team/*" {
    capabilities = ["create", "read", "update", "delete", "list"]
  }

