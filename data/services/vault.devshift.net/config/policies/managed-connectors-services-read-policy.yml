---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "managed-connectors-services-read-policy"
instance:
  $ref: /services/vault.devshift.net/config/instances/devshift-net.yml
rules: |
  path "managed-connectors-services/*" {
    capabilities = ["read"]
  }
  path "managed-services-ci/*" {
    capabilities = ["read"]
  }
  path "managed-services/*" {
    capabilities = ["read"]
  }
