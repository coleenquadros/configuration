---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "managed-application-services-mk-kafka-instances-policy"
rules: |
  path "managed-application-services/MK-Kafka-Instances-team/*" {
    capabilities = ["create", "read", "update", "delete", "list"]
  }

