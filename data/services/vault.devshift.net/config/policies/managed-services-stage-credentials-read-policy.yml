---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "managed-services-stage-credentials-read-policy"
rules: |
  path "app-interface/data/managed-service-api/stage/*" {
    capabilities = ["read", "list"]
  }
