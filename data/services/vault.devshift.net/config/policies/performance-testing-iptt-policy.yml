---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "performance-testing-policy"
rules: |
  path "managed-services-ci/IPTT/*" {
    capabilities = ["create", "update", "read","delete","list"]
  }

