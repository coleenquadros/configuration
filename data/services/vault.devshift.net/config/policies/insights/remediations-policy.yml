
---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "insights-remediations-policy"
instance:
  $ref: /services/vault.devshift.net/config/instances/devshift-net.yml
rules: |
  path "insights/data/secrets/insights-dev/remediations-ci/*" {
    capabilities = ["create", "read", "update"]
  }
  path "insights/data/secrets/insights-dev/remediations-qa/*" {
    capabilities = ["create", "read", "update"]
  }
  path "insights/data/secrets/insights-prod/remediations-prod/*" {
    capabilities = ["create", "read", "update"]
  }
  path "insights/data/secrets/insights-stage/remediations-stage/*" {
    capabilities = ["create", "read", "update"]
  }
