---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "insights-perf-policy"
rules: |
  path "insights/data/secrets/perf/*" {
    capabilities = ["create", "read", "update"]
  }
