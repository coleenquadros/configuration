---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "insights-policy"
rules: |
  path "insights/data/secrets/*" {
    capabilities = ["create", "read", "update", "delete"]
  }
  path "insights/metadata/secrets/" {
    capabilities = ["list"]
  }
