---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "insights-pes-app-policy"
rules: |
  path "insights/data/secrets/insights-stage/pes-app-stage/*" {
    capabilities = ["create", "read", "update"]
  }
