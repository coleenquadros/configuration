---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "osd-sre-breakglass-policy"
rules: |
  path "app-sre/integrations-output/cluster-deployment-mapper" {
    capabilities = ["read", "list"]
  }
