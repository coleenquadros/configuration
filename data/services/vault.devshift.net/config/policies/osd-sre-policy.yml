---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "osd-sre-policy"
instance:
  $ref: /services/vault.devshift.net/config/instances/devshift-net.yml
rules: |
  path "osd-sre/*" {
    capabilities = ["create", "read", "update", "delete", "list"]
  }
  path "osd-sre-cicd/*" {
    capabilities = ["create", "read", "update", "delete", "list"]
  }
  path "integrations-output/terraform-resources/hivei01ue1/hive/external-dns-route53-creds" {
    capabilities = ["read", "list"]
  }
  path "integrations-output/terraform-resources/hive-stage-01/hive/external-dns-route53-creds" {
    capabilities = ["read", "list"]
  }
  path "integrations-output/terraform-resources/hivep01ue1/hive/external-dns-route53-creds" {
    capabilities = ["read", "list"]
  }
