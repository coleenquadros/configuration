---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "app-sre-interns-policy"
rules: |
  path "app-sre/*" {
    capabilities = ["list"]
  }
  path "app-sre/creds/qontract-reconcile-toml" {
    capabilities = ["read"]
  }
