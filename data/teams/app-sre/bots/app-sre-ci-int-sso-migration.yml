---
$schema: /access/bot-1.yml

labels:
  service: ci-int

name: sso-migration temporary admin bot on ci-int Jenkins
org_username: sso-migration

roles:
- $ref: /dependencies/ci-int/roles/ci-int-admin-access.yml
