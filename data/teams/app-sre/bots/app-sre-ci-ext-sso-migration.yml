---
$schema: /access/bot-1.yml

labels:
  service: ci-ext

name: sso-migration temporary admin bot on ci-ext Jenkins
org_username: sso-migration

roles:
- $ref: /dependencies/ci-ext/roles/ci-ext-admin-access.yml
