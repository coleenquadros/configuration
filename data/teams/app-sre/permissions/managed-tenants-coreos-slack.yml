---
$schema: /access/permission-1.yml

labels: {}

name: managed-tenants-coreos-slack
description: managed-tenants owners (managed via app-interface)

service: slack-usergroup
handle: managed-tenants

workspace:
  $ref: /dependencies/slack/coreos.yml

ownersFromRepos:
  - https://gitlab.cee.redhat.com/service/managed-tenants:main

channels:
- sd-app-sre
