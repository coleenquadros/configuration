---
$schema: /dependencies/jira-board-1.yml

labels: {}

name: ASIC
description: App SRE Interrupt Catcher Jira board
server:
  $ref: /dependencies/jira/issues-redhat-com.yaml

slack:
  workspace:
    $ref: /dependencies/slack/coreos.yml
  channel: sd-app-sre-info
