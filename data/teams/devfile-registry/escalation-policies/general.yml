---
$schema: /app-sre/escalation-policy-1.yml

labels:
  team: devfile-registry

name: devfile-registry

description: |
  In the event that an app-sre engineer cannot resolve the issue in the devfile registry service, 
  as outlined in the SOP, they can escalate to the devfile registry team on CoreOS slack.

  For non production incidents, reach out on the #team-devfile-registry-alert channel.
  For production incidents, reach out on the #team-devfile-registry-alert and tag the @team-devfile-registry slack group.

channels:
  slackUserGroup:
  - $ref: /teams/devfile-registry/permissions/devfile-registry-team-coreos-slack.yml

  email: 
  - team-devfile@redhat.com
