---
$schema: /app-sre/escalation-policy-1.yml

labels: {}

name: cos-fleet-manager-escalations

description: |
  The COS Fleet Manager does not have an oncall support for production incidents.

  App-SRE is expected to triage an incident and try to resolve the issue using the provided SOPs.

  The escalations are summarized as follows:

  For help with production and non production incidents, please reach out to the COS team on slack.

channels:
  slackUserGroup:
  - $ref: /teams/managed-connectors/permissions/managed-connectors-primary-coreos-slack.yml

  email:
  - cos-support@redhat.com

  jiraBoard:
  - $ref: /teams/managed-connectors/jira/mgdctrs.yaml
