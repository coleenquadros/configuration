---
$schema: /app-sre/escalation-policy-1.yml

labels:
  service: api-designer

name: api-designer-escalations

description: |
  The API Designer does not have oncall support for production incidents.

  App-SRE is expected to triage an incident and try to resolve the issue using the provided SOPs.

  The escalations are summarized as follows:

    For help with production and non production incidents, please reach out to the API Designer team on slack.


channels:
  #slackUserGroup:
  #- $ref: /teams/api-designer/permissions/api-designer-coreos-slack.yml

  email:
  - api-designer-dev@redhat.com

  jiraBoard:
  - $ref: /teams/managed-services/jira/mgdstrm.yaml
