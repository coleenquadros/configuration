---
$schema: /access/permission-1.yml

labels: {}

name: cs-sre-primary-coreos-slack
description: CS SRE Primary On Call (managed via app-interface)

service: slack-usergroup
handle: cs-sre-primary

workspace:
  $ref: /dependencies/slack/coreos.yml

pagerduty:
- $ref: /dependencies/pagerduty/cs-sre-primary.yml

channels:
- sd-cssre
