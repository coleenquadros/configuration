---
$schema: /access/permission-1.yml

labels: {}

name: gcp-project-operator-coreos-slack
description: SREP gcp-project-operator owners (managed via app-interface)

service: slack-usergroup
handle: gcp-project-operator

workspace:
  $ref: /dependencies/slack/coreos.yml

ownersFromRepos:
  - https://github.com/openshift/gcp-project-operator

channels:
- sd-sre
- sre-operators
- team-srep-alert
