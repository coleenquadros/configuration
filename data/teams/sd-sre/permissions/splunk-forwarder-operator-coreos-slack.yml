---
$schema: /access/permission-1.yml

labels: {}

name: splunk-forwarder-operator-coreos-slack
description: SREP splunk-forwarder-operator owners (managed via app-interface)

service: slack-usergroup
handle: splunk-forwarder-operator

workspace:
  $ref: /dependencies/slack/redhat-internal.yml

ownersFromRepos:
  - https://github.com/openshift/splunk-forwarder-operator

channels:
- sd-sre-platform
- sre-operators
- team-srep-alert
