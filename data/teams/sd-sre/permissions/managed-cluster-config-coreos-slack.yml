---
$schema: /access/permission-1.yml

labels: {}

name: managed-cluster-config-coreos-slack
description: SREP managed-cluster-config owners (managed via app-interface)

service: slack-usergroup
handle: managed-cluster-config

workspace:
  $ref: /dependencies/slack/redhat-internal.yml

ownersFromRepos:
  - https://github.com/openshift/managed-cluster-config

channels:
- sd-sre-platform
- sre-operators
- team-srep-alert
