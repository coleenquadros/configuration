---
$schema: /app-sre/escalation-policy-1.yml

labels:
  team: sre-platform

name: configuration-anomaly-detection

description: |
  An escalation policy to be used for configuration-anomaly-detection related notifications.
  When you got an alert:
  1. Triage it with the linked SOP
  2. If the alert is not resolved, page SREP oncall with the linked pagerduty policy

channels:
  slackUserGroup:
  - $ref: /teams/sd-sre/permissions/configuration-anomaly-detection-coreos-slack.yml

  email: 
  - sd-sre-platform+cad@redhat.com

  pagerduty:
    $ref: /dependencies/pagerduty/srep-escalation-policy.yml

  jiraBoard:
  - $ref: /teams/sd-sre/jira/osd.yaml
