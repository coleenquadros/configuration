---
$schema: /app-sre/escalation-policy-1.yml

labels:
  team: sre-platform

name: backplane

description: |
  An escalation policy to be used for backplane related notifications.
  When you got an alert:
  1. Triage it with the linked SOP
  2. If the alert is not resolved, page SREP oncall with the linked pagerduty policy

channels:
  slackUserGroup:
  - $ref: /teams/sd-sre/permissions/backplane-team-coreos-slack.yml

  email: 
  - sd-sre-platform@redhat.com

  pagerduty:
    $ref: /dependencies/pagerduty/srep-escalation-policy.yml
