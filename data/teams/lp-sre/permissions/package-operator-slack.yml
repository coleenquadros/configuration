---
$schema: /access/permission-1.yml

labels: {}

name: package-operator-slack
description: SD:LPSRE Package Operator Team. (managed via app-interface)

service: slack-usergroup
handle: package-operator

workspace:
  $ref: /dependencies/slack/redhat-internal.yml

channels:
  - forum-package-operator
