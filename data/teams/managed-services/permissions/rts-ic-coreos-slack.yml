---
$schema: /access/permission-1.yml

labels: {}

name: rts-oncall-coreos-slack
description: RTS Interrupt Catcher (managed via app-interface)

service: slack-usergroup
handle: rts-ic

workspace:
  $ref: /dependencies/slack/coreos.yml

pagerduty:
- $ref: /dependencies/pagerduty/rts-ic.yml

channels:
- forum-managed-kafka
