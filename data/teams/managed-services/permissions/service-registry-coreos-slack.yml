---
$schema: /access/permission-1.yml

labels: {}

name: service-registry-coreos-slack
description: Service-Registry team (managed via app-interface)

service: slack-usergroup

workspace:
  $ref: /dependencies/slack/coreos.yml

channels:
- service-registry-alert
