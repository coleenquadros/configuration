---
$schema: /app-sre/escalation-policy-1.yml

labels:
  service: hacbs

name: hacbs-escalations

description: |
  HACBS does not have oncall support for production incidents.

  App-SRE is expected to triage an incident and try to resolve the issue using the provided SOPs.

  For help with production and non production incidents, please reach out to the HACBS teams on slack.


channels:
  slackUserGroup:
  - $ref: /teams/stonesoup/permissions/hacbs-coreos-slack.yml

  email:
  - hacbs-tech-leads@redhat.com

  jiraBoard:
  - $ref: /teams/stonesoup/jira/hacbs.yml
