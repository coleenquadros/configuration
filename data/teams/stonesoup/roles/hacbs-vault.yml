---
$schema: /access/role-1.yml

labels: {}
# This is old, and deprecated. Eventually, move content to the stonesoup vault.
# All new content should go in the stonesoup vault.
name: hacbs-vault

permissions: []

oidc_permissions:
- $ref: /dependencies/vault/permissions/oidc/prod/hacbs.yml
