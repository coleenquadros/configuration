---
$schema: /app-sre/escalation-policy-1.yml

labels:
  service: status-board

name: status-board

description: |
  This service is in stage only. Escalate only via the slack channel and e-mail.

  Escalate after 1 hour of down-time.
  Do not start manager escalation.

channels:
  slackUserGroup: # required
  - $ref: FIXME # this permission-1.yml file should be of type `service: slack-usergroup`.

  email: # required
  - sd-ops-services-dev@redhat.com

  jiraBoard: # required
  - $ref: /teams/sd-ops-dev/jira/status-board.yml
