---
$schema: /app-sre/escalation-policy-1.yml

labels: {}

name: obo-general

description: |
  The Monitoring team has an oncall rotation for support incidents.

  App-SRE is expected to triage an incident and try to resolve the issue using the SOP's.

  The escalations are summarized as follows:

    - tbd

channels:
  slackUserGroup:
  - $ref: /teams/observability-operator/permissions/obo-oncall-coreos-slack.yml

  email:
  - team-monitoring@redhat.com

  jiraBoard:
  - $ref: /teams/observability-operator/jira/mon.yaml
