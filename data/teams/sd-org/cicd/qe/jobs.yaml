---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: qe

name: ci-int-qe-jobs

description: ''

app:
  $ref: /services/sd-cicd/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: qe-cluster-delivery
    label: qe
    node: qe
    gl_group: service
    gl_project: qe-cluster-delivery
    jobs:
    - 'gl-timed':
        display_name: qe-cluster-delivery installer
        ci_cmd: bringup/installer/run.sh
        cron_expression: 'H H/6 * * *'
