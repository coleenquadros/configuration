---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: qe

name: ci-int-qe-views

description: ''

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: qe
    description: 'qe view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[qe].*'
