---
$schema: /access/permission-1.yml

labels: {}

name: service-registry-coreos-slack
description: Slack user group for Managed Service Registry(managed via app-interface)

service: slack-usergroup
handle: service-registry-team

workspace:
  $ref: /dependencies/slack/redhat-internal.yml

channels:
- service-registry
- team-service-registry-alert
- team-service-registry-alert-stage

