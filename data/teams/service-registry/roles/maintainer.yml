---
$schema: /access/role-1.yml

labels: {}
name: maintainer

permissions:
# jenkins
- $ref: /dependencies/ci-ext/permissions/ci-ext.yml # access to ci-ext
- $ref: /dependencies/ci-int/permissions/ci-int.yml # access to ci-int
# slack
- $ref: /teams/service-registry/permissions/service-registry-coreos-slack.yml # access to service registry slack channel
# quay
- $ref: /dependencies/quay/permissions/quay-membership-app-sre-managed-services.yml
- $ref: /dependencies/quay/permissions/quay-membership-rhoas-managed-services.yml
- $ref: /dependencies/quay/permissions/quay-membership-bf2-managed-services.yml
# observability 
- $ref: /services/observability/permissions/observability-access.yml # Read access to prometheus, alertmanager and grafana
# vault
- $ref: /dependencies/vault/permissions/service-registry.yml

access:
- namespace:
    $ref: /services/service-registry/namespaces/service-registry-stage.yml
  role: view
- namespace:
    $ref: /services/service-registry/namespaces/service-registry-production.yml
  role: view
# CD tekton pipelines access
- namespace:
    $ref: /services/service-registry/namespaces/service-registry-pipelines.appsrep05ue1.yaml
  role: view
- namespace:
    $ref: /services/service-registry/namespaces/service-registry-pipelines.appsrep05ue1.yaml
  role: tekton-trigger-access

