---
$schema: /access/permission-1.yml

labels: {}

name: rts-oncall-coreos-slack
description: RTS Primary (managed via app-interface)

service: slack-usergroup
handle: rts-primary

workspace:
  $ref: /dependencies/slack/coreos.yml

pagerduty:
- $ref: /dependencies/pagerduty/rts-primary.yml

channels:
- forum-managed-kafka
