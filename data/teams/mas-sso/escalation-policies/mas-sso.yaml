---
$schema: /app-sre/escalation-policy-1.yml

labels: {}

name: mas-sso-escalations

description: |
  The MAS Security does not have an oncall support for production incidents.

  App-SRE is expected to triage an incident and try to resolve the issue using the provided SOPs.

  The escalations are summarized as follows:
  
  For help with production and non production incidents, please reach out to the MAS SSO team on slack at the handle `mas-sso-primary`.

  The team is available to support if needed during their regular business hours.

channels:
  slackUserGroup:
  - $ref: /teams/mas-sso/permissions/mas-sso-primary-coreos-slack.yml

  email: 
  - mas-security@redhat.com

  jiraBoard:
  - $ref: /teams/mas-sso/jira/mgdstrm.yaml
