---
$schema: /access/permission-1.yml

labels: {}

name: acscs-eng-oncall
description: Advanced Cluster Security Cloud Service (ACSCS) primary oncall handle

service: slack-usergroup
handle: acscs-eng-oncall

workspace:
  $ref: /dependencies/slack/redhat-internal.yml

pagerduty:
- $ref: /dependencies/pagerduty/acscs-primary.yml

channels:
- forum-acs-cloud-service
- alerts-acs-data-plane
