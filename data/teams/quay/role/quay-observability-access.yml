---
$schema: /access/role-1.yml

labels: {}
name: quayio-observability-access

# This role is for non-developers from Quay who need access to look at Grafana dashboard.

permissions:
- $ref: /dependencies/vault/permissions/quay-dev-team.yml
# observability
- $ref: /services/observability/permissions/observability-access.yml
