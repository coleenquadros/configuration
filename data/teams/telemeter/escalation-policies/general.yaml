---
$schema: /app-sre/escalation-policy-1.yml

labels: {}

name: telemeter-general-escalations

description: |
  The Telemeter team has developer oncall support for production incidents.
  
  App-SRE is expected to triage an incident and try to resolve the issue using the SOP's.
  
  The escalations are summarized as follows:
  
    For non production incidents, reach out to the Observatorium oncall on slack
    For help on production incidents, page the Observatorium oncall on Pagerduty

channels:
  slackUserGroup:
  - $ref: /teams/telemeter/permissions/observatorium-oncall-coreos-slack.yml

  email: 
  - team-observability-platform@redhat.com

  pagerduty:
    $ref: /dependencies/pagerduty/observatorium-oncall.yml

  jiraBoard:
  - $ref: /teams/telemeter/jira/obs.yaml
