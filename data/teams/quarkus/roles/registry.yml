---
$schema: /access/role-1.yml

labels: {}
name: quarkus-registry

permissions:
  - $ref: /services/observability/permissions/observability-access.yml
  - $ref: /dependencies/vault/permissions/app-interface-quarkus.yml

sentry_teams:
  - $ref: /dependencies/sentry/teams/registry-quarkus-redhat-stage.yml

access:
  - namespace:
      $ref: /services/quarkus/registry/namespaces/registry-quarkus-redhat-stage.yml
    role: view

owned_saas_files:
  - $ref: /services/quarkus/registry/cicd/ci-int/saas.yml
