---
$schema: /app-sre/escalation-policy-1.yml

labels: {}

name: image-builder-escalations

description: |
  The escalation policy for all Image Builder apps.

  Currently no pagerduty is in place, so response times are not guaranteed.

channels:
  slackUserGroup:
  - $ref: /teams/image-builder/permissions/image-builder-coreos-slack.yml

  email:
    - osbuilders@redhat.com
