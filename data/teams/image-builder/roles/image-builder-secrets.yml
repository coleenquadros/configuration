---
$schema: /access/role-1.yml

labels:
  service: image-builder

name: image-builder-secrets

permissions:
- $ref: /teams/image-builder/permissions/image-builder-secrets.yml

oidc_permissions:
- $ref: /dependencies/vault/permissions/oidc/prod/image-builder-secrets.yml
