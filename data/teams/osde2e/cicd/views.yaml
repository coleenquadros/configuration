---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osde2e

name: ci-ext-osde2e-views

description: ''

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: views

config:
- view:
    name: osd-e2e
    description: 'osd-e2e view'
    view-type: list
    job-filters:
        regex-job:
            regex-name: DESCRIPTION
            regex: '^\[osd-e2e\].*'
