---
$schema: /access/role-1.yml

labels:
  platform: insights

name: jenkins-view
description: Membership in jenkins-view group on insights dev cluster

permissions: []

access:
- cluster:
    $ref: /openshift/insights/cluster-insights-dev.yml
  group: jenkins-view
