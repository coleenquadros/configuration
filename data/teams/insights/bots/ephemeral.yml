---
$schema: /access/bot-1.yml

labels:
  service: insights-ephemeral

name: ephemeral-bot
description: Service account with access to ephemeral namespaces

openshift_serviceaccount: ephemeral-base/ephemeral-bot

roles:
- $ref: /teams/insights/bots/ephemeral-role.yml
