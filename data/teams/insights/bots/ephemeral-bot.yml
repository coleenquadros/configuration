---
$schema: /access/bot-1.yml

labels:
  service: insights-ephemeral

name: ephemeral-bot
description: Service account with admin access to ephemeral namespaces used in Jenkins jobs

openshift_serviceaccount: ephemeral-base/ephemeral-bot

roles:
- $ref: /teams/insights/roles/ephemeral-admins.yml
