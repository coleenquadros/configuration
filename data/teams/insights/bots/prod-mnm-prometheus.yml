---
$schema: /access/bot-1.yml

labels:
  service: insights-mnm

name: prod-mnm-prometheus-bot
description: |
  Refers to the ServiceAccount used by the PROD Insights MnM Prometheus
  in mnm which needs to view all PROD Insights namespaces

openshift_serviceaccount: mnm/prometheus

roles:
- $ref: /teams/insights/roles/prometheus/prod-prometheus-role.yml
