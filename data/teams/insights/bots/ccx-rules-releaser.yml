---
$schema: /access/bot-1.yml

labels:
  service: insights-ephemeral

name: ccx-rules-releaser
description: Service account with user access to ephemeral namespaces used in the ccx-rules-releaser repo for smoke tests

openshift_serviceaccount: ephemeral-base/ccx-rules-releaser

roles:
- $ref: /teams/insights/roles/ephemeral-users.yml
