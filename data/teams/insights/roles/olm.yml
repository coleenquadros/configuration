---
$schema: /access/role-1.yml

# Gives read-only access to OLM namespace
# Helpful for debugging operator deployment

labels: {}
name: olm

permissions: []

access:
- namespace:
    $ref: /openshift/crc-stg-01/namespaces/openshift-operator-lifecycle-manager.yml
  role: view

- namespace:
    $ref: /openshift/crcp01ue1/namespaces/openshift-operator-lifecycle-manager.yml
  role: view
