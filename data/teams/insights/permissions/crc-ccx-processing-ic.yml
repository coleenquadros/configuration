---
$schema: /access/permission-1.yml

labels: {}

name: ccx-processing-ic-coreos-slack
description: CCX Processing Team interrupt channel (weekly rotation, managed manually by the CCX Processing team). Used by the person in watch duty to receive notifications.

service: slack-usergroup
handle: ccx-processing-ic
skip: true

workspace:
  $ref: /dependencies/slack/coreos.yml

channels:
- ccx-processing-team
- ccx-dev-notifications
- sd-app-sre-alert
