---
$schema: /kafka/cluster-1.yml

labels: {}

name: crcs02ue1k
description: Managed Kafka cluster provisioned via KAS Fleet Manager to be used in the console.redhat.com Stage environment (hosted on cluster crcs02ue1)

ocm:
  $ref: /dependencies/ocm/production.yml

spec:
  provider: aws
  region: us-east-1
  multi_az: true
