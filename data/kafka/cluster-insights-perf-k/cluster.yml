---
$schema: /kafka/cluster-1.yml

labels: {}

name: cluster-insights-perf-k
description: Managed Kafka cluster provisioned via KAS Fleet Manager to be used in the Insights Perf Cluster

ocm:
  $ref: /dependencies/ocm/production.yml

spec:
  provider: aws
  region: us-east-1
  multi_az: true
