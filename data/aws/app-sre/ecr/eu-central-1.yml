---
$schema: /aws/ecr-1.yml

labels: {}

account:
  $ref: /aws/app-sre/account.yml

name: app-sre eu-central-1 ecr
description: |
  ECR repositories are provisioned using the terraform-resources integration.
  This file represents an ECR registry for an aws account, to be consumed by aws-ecr-image-pull-secrets.

region: eu-central-1
