---
$schema: /aws/policy-1.yml

labels: {}

account:
  $ref: /aws/app-sre/account.yml

name: performance-insights-access
description: |
    Allows a user access to RDS performance insights

policy:
  {
    "Version": "2012-10-17",
    "Statement": [
      {
        "Effect": "Allow",
        "Action": "pi:*",
        "Resource": "arn:aws:pi:*:*:metrics/rds/*"
      }
    ]
  }


