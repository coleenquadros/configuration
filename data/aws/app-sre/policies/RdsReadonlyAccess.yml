---
$schema: /aws/policy-1.yml

labels: {}

account:
  $ref: /aws/insights-stage/account.yml

name: ocm-rds-readonly
description: |
    Allows a user access to RDS

policy:
  {
    "Version":"2012-10-17",
    "Statement":[
      {
        "Effect":"Allow",
        "Action":"rds:DescribeDBInstances",
        "Resource":"*"
      },
      {
        "Effect":"Allow",
        "Action":"rds:DescribeDBClusters",
        "Resource":"*"
      }
    ]
  }


