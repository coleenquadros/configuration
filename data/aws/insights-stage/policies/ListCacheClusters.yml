---
$schema: /aws/policy-1.yml

labels: {}

account:
  $ref: /aws/insights-stage/account.yml

name: list-cache-clusters
description: |
    Allows a user to list elatic cache clusters.

policy:
  {
    "Version": "2012-10-17",
    "Statement": [
      {
        "Effect": "Allow",
        "Action": [
          "elasticache:Describe*"
        ],
        "Resource": "*"
      }
    ]
  }


