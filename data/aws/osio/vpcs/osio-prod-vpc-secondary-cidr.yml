---
$schema: /aws/vpc-1.yml

labels: {}

account:
  $ref: /aws/osio/account.yml

name: OSIO Prod VPC secondary CIDR
description: |
  VPC is currently not provisioned by app-interface, it is only here to serve as a data source.
  The CIDR block represents a manual secondary association to avoid overlaps with VPCs in the app-sre AWS account.

vpc_id: vpc-748fe712
cidr_block: 192.168.32.0/24
region: us-east-1
