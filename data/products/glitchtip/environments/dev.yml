---
$schema: /app-sre/environment-1.yml


labels:
  service: glitchtip-dev
  type: dev

name: glitchtip-dev

description: |
  The dev environment for Glitchtip development

product:
  $ref: /products/glitchtip/product.yml

parameters:
  CHANNEL: dev
