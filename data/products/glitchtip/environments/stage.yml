---
$schema: /app-sre/environment-1.yml


labels:
  service: glitchtip-stage
  type: stage

name: glitchtip-stage

description: |
  The stage environment for Glitchtip staging environment

product:
  $ref: /products/glitchtip/product.yml

parameters:
  CHANNEL: staging
