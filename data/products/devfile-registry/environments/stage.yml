
---
$schema: /app-sre/environment-1.yml


labels:
  service: devfile-registry
  type: stage

name: devfile-registry-stage

description: |
  The staging environment for the devfile-registry

product:
  $ref: /products/devfile-registry/product.yml
