
---
$schema: /app-sre/environment-1.yml


labels:
  service: devfile-registry
  type: production

name: devfile-registry-production

description: |
  The production environment for the devfile-registry

product:
  $ref: /products/devfile-registry/product.yml

dependsOn:
  $ref: /products/devfile-registry/environments/stage.yml
