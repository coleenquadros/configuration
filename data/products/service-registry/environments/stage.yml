---
$schema: /app-sre/environment-1.yml

labels:
  service: service-registry
  type: stage

name: service-registry-stage

description: |
    The staging environment for Service Registry

product:
  $ref: /products/service-registry/product.yml
