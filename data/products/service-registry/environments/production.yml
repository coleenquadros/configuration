---
$schema: /app-sre/environment-1.yml

labels:
  service: service-registry
  type: production

name: service-registry-production

description: |
    The production environment for Service Registry

product:
  $ref: /products/service-registry/product.yml
