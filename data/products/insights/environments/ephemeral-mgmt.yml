
---
$schema: /app-sre/environment-1.yml


labels:
  service: insights
  type: ephemeral

name: insights-ephemeral-mgmt

description: |
  Environment settings for ephemeral environment management applications/services

product:
  $ref: /products/insights/product.yml

parameters:
  KAFKA_BOOTSTRAP_HOST: mq-kafka
  KAFKA_BOOTSTRAP_PORT: 29092
