---
$schema: /dependencies/pagerduty-target-1.yml

labels:
  service: pagerduty

name: observatorium-oncall
description: Observatorium (Telemeter) Oncall

instance:
  $ref: /dependencies/pagerduty/instance.yml

scheduleID: P0X05HU
