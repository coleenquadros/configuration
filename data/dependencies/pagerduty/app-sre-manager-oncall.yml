---
$schema: /dependencies/pagerduty-target-1.yml

labels:
  service: pagerduty

name: app-sre-manager-oncall
description: AppSRE manager oncall

instance:
  $ref: /dependencies/pagerduty/instance.yml

scheduleID: P9BYON0
