---
$schema: /dependencies/pagerduty-target-1.yml

labels:
  service: pagerduty

name: quay-oncall
description: Quay Oncall

instance:
  $ref: /dependencies/pagerduty/instance.yml

escalationPolicyID: PUZPPNE
