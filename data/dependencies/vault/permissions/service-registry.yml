---
$schema: /access/permission-1.yml

labels:
  service: managed-services

name: vault-service-registry
description: vault service-registry secret engine access

service: github-org-team
org: app-sre
team: vault-service-registry
