---
$schema: /access/oidc-permission-1.yml

labels:
  service: vault.devshift.net

name: assisted-installer-vault-admin-oidc-vault
description: assisted-installer-vault-admin vault devshift oidc permission

instance:
  $ref: /services/vault.devshift.net/config/prod/devshift-net.yml

service: vault
vault_policies:
- $ref: /services/vault.devshift.net/config/prod/policies/app-interface-assisted-installer.yml
