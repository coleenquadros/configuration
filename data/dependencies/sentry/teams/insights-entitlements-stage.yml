$schema: /dependencies/sentry-team-1.yml

labels:
  service: entitlements
  platform: insights
  type: core

name: insights-entitlements-stage
description: insights-entitlements sentry-stage team
instance:
  $ref: /dependencies/sentry/sentry-stage.yml
