---
$schema: /dependencies/container-image-mirror-1.yml

url: docker.io/caarlos0/domain_exporter

pullCredentials:
  path: app-sre/creds/mirror-images/docker.io-sdappsre
  field: all

tagsExclude:
- "v1.13.0.*"  # the manifest contains no mediaType
- ".*arm64.*"
# broken images, skopeo fails to sync them
- "sha256-.+sig"
