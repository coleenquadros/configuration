---
$schema: /dependencies/quay-org-1.yml

labels:
  service: observability-operator

name: rhobs
description: rhobs official organization

instance:
  $ref: /dependencies/quay/instance.yml

managedRepos: false

managedTeams: []

automationToken:
  path: app-sre/creds/quay-org-rhobs
  field: token
