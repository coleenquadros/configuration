---
$schema: /dependencies/quay-org-1.yml

labels:
  service: quarkus

name: quarkus
description: Quay org for quarkus hosting

instance:
  $ref: /dependencies/quay/instance.yml

managedRepos: true

managedTeams:
- appsre

automationToken:
  path: app-sre/creds/quay-org-quarkus
  field: token
