---
$schema: /dependencies/quay-org-1.yml

labels:
  service: deployment-validation-operator

name: deployment-validation-operator
description: deployment-validation-operator for OperatorHub

instance:
  $ref: /dependencies/quay/instance.yml

managedRepos: true

managedTeams:
- owners

automationToken:
  path: app-sre/creds/quay-org-deployment-validation-operator
  field: token

pushCredentials:
  path: app-sre/quay/deployment-validation-operator-push
  field: all
