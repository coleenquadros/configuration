---
$schema: /dependencies/quay-org-1.yml

labels:
  service: codeready-toolchain

name: codeready-toolchain
description: CodeReady Toolchain official organization

managedTeams:
- owners

automationToken:
  path: app-sre/creds/quay-org-codeready-toolchain
  field: token
