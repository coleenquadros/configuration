---
$schema: /dependencies/quay-org-1.yml

labels:
  service: cs-sre

name: cs-sre
description: CS-SRE official organization

instance:
  $ref: /dependencies/quay/instance.yml

managedRepos: true

managedTeams:
- cssre

automationToken:
  path: cs-sre/creds/cs-sre-quay-bot
  field: token

pushCredentials:
  path: cs-sre/creds/quay/push
  field: all
