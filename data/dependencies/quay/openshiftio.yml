---
$schema: /dependencies/quay-org-1.yml

labels:
  service: openshift.io

name: openshiftio
description: Quay org for openshiftio hosting

instance:
  $ref: /dependencies/quay/instance.yml

managedTeams:
- devs

automationToken:
  path: app-sre/creds/quay-org-openshiftio
  field: token
