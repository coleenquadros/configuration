---
$schema: /dependencies/quay-org-1.yml

labels:
  service: ocm-quay

name: openshift-release-dev
description: OCP mirror

instance:
  $ref: /dependencies/ocm-quay/instance.yml

managedRepos: true
managedTeams: []

automationToken:
  path: app-sre/creds/ocm-quay
  field: openshift-release-dev

pushCredentials:
  path: app-sre/ocm-quay/openshift-release-dev-push
  field: all
