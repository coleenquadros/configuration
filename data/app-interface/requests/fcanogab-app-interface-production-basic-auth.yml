---
$schema: /app-interface/credentials-request-1.yml

labels: {}

name: fcanogab-app-interface-production-basic-auth-20210202

description: |
  I would like to be able to query the GraphQL API in order to obtain information
  about services. We would use that information for obtaining images and repos
  and generate with that data the services manifests. We will also look for
  services metadata in order to check GA date, owner, etc.

user:
  $ref: /teams/prodsec/users/fcanogab.yml

credentials: app-interface-production-dev-access
