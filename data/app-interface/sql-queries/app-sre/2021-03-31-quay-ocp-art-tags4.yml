---
$schema: /app-interface/app-interface-sql-query-1.yml
labels: {}
name: 2021-03-31-quay-ocp-art-tags4
namespace:
  $ref: /services/quayio/namespaces/quayp04ue2.yml
identifier: quayio-production-57-replica
output: stdout
overrides:
  db_name: quay
query: |
  SELECT count(name) FROM tag WHERE
  repository_id = (SELECT id FROM repository
  WHERE name = 'ocp-v4.0-art-dev' LIMIT 1);
