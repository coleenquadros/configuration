---
$schema: /app-interface/app-interface-sql-query-1.yml
labels: {}
name: 2022-06-15-cs-count-clusters-pending-oidc
namespace:
  $ref: /services/ocm/namespaces/uhc-production.yml
identifier: clusters-service-production
output: stdout
queries:
  - SELECT COUNT(clusters.id) FROM clusters WHERE (((clusters.state = 'pending' OR clusters.state = 'waiting') AND clusters.oidc_ready = 'f') AND clusters.aws_sts_oidc_endpoint_url <> '');
