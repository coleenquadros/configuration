---
$schema: /app-interface/app-interface-sql-query-1.yml
labels: {}
name: mas-sso-service-account-count-stage-per-service
namespace:
  $ref: /services/mas-sso/namespaces/mas-sso-stage.yml
identifier:  mas-sso-staging
output: stdout
queries:
  - |
    SELECT COUNT(1) FROM CLIENT WHERE CLIENT_ID LIKE 'connector-%';
  - |
    SELECT COUNT(1) FROM CLIENT WHERE CLIENT_ID LIKE 'srvc-acct-%';
  - |
    SELECT COUNT(1) FROM CLIENT WHERE CLIENT_ID LIKE 'canary-%';
  - |
    SELECT COUNT(1) FROM CLIENT WHERE CLIENT_ID LIKE 'kas-fleetshard-agent-%';
  - |
    SELECT COUNT(1) FROM CLIENT WHERE CLIENT_ID LIKE 'kafka-%';
