---
$schema: /app-interface/app-interface-sql-query-1.yml
labels: {}
name: mas-sso-count-of-clients-per-org-stage-migration
namespace:
  $ref: /services/mas-sso/namespaces/mas-sso-stage.yml
identifier: mas-sso-staging
output: stdout
queries:
  - |
    SELECT value as orgid,COUNT(value)  FROM client_attributes WHERE name = 'rh-org-id' GROUP BY value;
