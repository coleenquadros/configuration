---
$schema: /app-interface/app-interface-sql-query-1.yml
labels: {}
name: mas-sso-client-count-stage
namespace:
  $ref: /services/mas-sso/namespaces/mas-sso-stage.yml
identifier: mas-sso-staging
output: stdout
# Run every day at 12:00 pm (UTC)
schedule:  00 12 * * *
queries:
  - |
    SELECT COUNT(1) FROM CLIENT WHERE REALM_ID='rhoas';
  - |
    SELECT COUNT(1) FROM CLIENT WHERE REALM_ID='rhoas-kafka-sre';
