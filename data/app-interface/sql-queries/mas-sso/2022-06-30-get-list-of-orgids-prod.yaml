---
$schema: /app-interface/app-interface-sql-query-1.yml
labels: {}
name: mas-sso-list-of-orgids-production
namespace:
  $ref: /services/mas-sso/namespaces/mas-sso-production.yml
identifier: mas-sso-production
output: stdout
queries:
  - |
    SELECT name,value from (SELECT DISTINCT(value),name FROM user_attribute) AS t WHERE name = 'rh-org-id';
