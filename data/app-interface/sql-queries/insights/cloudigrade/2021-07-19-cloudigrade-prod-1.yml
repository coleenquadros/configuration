---
$schema: /app-interface/app-interface-sql-query-1.yml
labels: {}
name: 2021-07-19-cloudigrade-prod-1
namespace:
  $ref: /services/insights/cloudigrade/namespaces/cloudigrade-prod.yml
identifier: cloudigrade-prod
output: stdout
queries:
  # get all intervals
  - "select id,every,period from django_celery_beat_intervalschedule;"
  # get all periodic tasks
  - "select id,name,task,args,kwargs,queue,exchange,routing_key,expires,enabled,last_run_at,total_run_count,date_changed,description,crontab_id,interval_id,solar_id,one_off,start_time,priority,headers,clocked_id,expire_seconds from django_celery_beat_periodictask;"
