---
$schema: /app-interface/app-interface-sql-query-1.yml
labels: {}
name: 2022-06-06-prod-get-source-97796
namespace:
  $ref: /services/insights/sources/namespaces/sources-prod.yml
identifier: sources-prod
output: stdout
query: |
  SELECT
    "s"."id", "s"."name", "s"."uid", "s"."created_at", "s"."updated_at", "s"."tenant_id", "s"."source_type_id", "s"."version", "s"."availability_status", "s"."imported", "s"."source_ref", "s"."last_checked_at", "s"."last_available_at", "s"."app_creation_workflow", "s"."paused_at"
  FROM
    "sources" AS "s"
  WHERE
    "s"."id" = 97796;
