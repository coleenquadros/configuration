---
$schema: /app-interface/app-interface-sql-query-1.yml
name: 2021-03-10-check-duplicate-profiles-prod
labels: {}
namespace:
  $ref: /services/insights/compliance/namespaces/compliance-prod.yml
identifier: compliance-prod
output: stdout
query: >-
  SELECT "ref_id", "account_id", "benchmark_id", "policy_id", COUNT("id")
  FROM "profiles"
  GROUP BY "ref_id", "account_id", "benchmark_id", "policy_id"
  HAVING COUNT("id") > 1;
