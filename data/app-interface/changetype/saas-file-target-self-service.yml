---
$schema: /app-interface/change-type-1.yml

labels: {}

name: saas-file-target-self-service
description: |
  Allow updates to saas file targets.

priority: urgent

contextType: datafile
contextSchema: /app-sre/saas-file-target-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - ref
  - parameters
  - secretParameters[*].version
  - upstream
  - image
  - disable
  - delete
  - promotion
