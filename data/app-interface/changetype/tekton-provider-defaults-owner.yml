---
$schema: /app-interface/change-type-1.yml

labels: {}

name: tekton-provider-defaults-owner
description: |
  allows updates on tekton-provider-defaults files

priority: high

contextType: datafile
contextSchema: /app-sre/tekton-provider-defaults-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - $
