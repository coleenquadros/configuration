---
$schema: /app-interface/change-type-1.yml

labels: {}

name: secret-promoter
description: |
  Allow bumping the version of a vault-secret.

priority: high

contextType: datafile
contextSchema: /openshift/namespace-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - openshiftResources[?(@.provider=="vault-secret")].version
  - openshiftResources[?(@.provider=="vault-secret")].labels
  - openshiftResources[?(@.provider=="vault-secret")].annotations
