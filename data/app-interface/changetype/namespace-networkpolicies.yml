---
$schema: /app-interface/change-type-1.yml

labels: {}

name: namespace-networkpolicy
description: |
  Allow updates on networkpolicies of a namespace.

priority: high

contextType: datafile
contextSchema: /openshift/namespace-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - networkPoliciesAllow
