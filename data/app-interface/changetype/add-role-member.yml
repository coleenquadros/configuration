---
$schema: /app-interface/change-type-1.yml

labels: {}

name: change-role-member
description: |
  allow adding users to roles

contextType: datafile
contextSchema: /access/roles-1.yml

changes:
- provider: jsonPath
  changeSchema: /access/user-1.yml
  jsonPathSelectors:
  - roles[*]
  context:
    selector: roles[*].'$ref'
    when: added

