---
$schema: /app-interface/change-type-1.yml

labels: {}

name: saas-file-target-namespace-owner
description: |
  Allow approving additional targets in saas files referencing a namespace.

priority: high

contextType: datafile
contextSchema: /openshift/namespace-1.yml

changes:
- provider: jsonPath
  changeSchema: /app-sre/saas-file-2.yml
  jsonPathSelectors:
  - resourceTemplates[*].targets
  context:
    selector: resourceTemplates[*].targets[*].namespace.'$ref'
    when: added
