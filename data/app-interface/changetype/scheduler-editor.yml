---
$schema: /app-interface/change-type-1.yml

labels: {}

name: scheduler-editor
description: |
  Allow updates on Slack group schedules.

priority: low

contextType: datafile
contextSchema: /app-sre/schedule-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - schedules
