---
$schema: /app-interface/change-type-1.yml

labels: {}

name: cloudflare-dns-record-owner
description: |
  Allow updates on Cloudflare DNS zone records.

priority: high

contextType: datafile
contextSchema: /cloudflare/account-1.yml

changes:
- provider: jsonPath
  changeSchema: /cloudflare/dns-zone-1.yml
  jsonPathSelectors:
  - records 
  context:
    selector: account.'$ref'
