---
$schema: /app-interface/change-type-1.yml

labels: {}

name: hypershift-operator-overriding-data-maintainer
description: |
  Allow updates to the HYPERSHIFT_OPERATOR_OVERRIDING_DATA parameter in saas file targets.

priority: urgent

contextType: datafile
contextSchema: /app-sre/saas-file-2.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - resourceTemplates[*].parameters.HYPERSHIFT_OPERATOR_OVERRIDING_DATA
  - resourceTemplates[*].targets[*].parameters.HYPERSHIFT_OPERATOR_OVERRIDING_DATA
