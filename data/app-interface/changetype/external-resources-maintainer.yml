---
$schema: /app-interface/change-type-1.yml

labels: {}

name: external-resources-maintainer
description: |
  Allow changes of externalResources in namespaces

priority: high

contextType: datafile
contextSchema: /openshift/namespace-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - externalResources
