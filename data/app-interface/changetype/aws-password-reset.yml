---
$schema: /app-interface/change-type-1.yml

labels: {}

name: aws-password-reset
description: |
  Allow reset password in an aws account

priority: low

contextType: datafile
contextSchema: /aws/account-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - resetPasswords

implicitOwnership:
- provider: jsonPath
  jsonPathSelector: $.resetPasswords[*].user.'$ref'
