---
$schema: /app-interface/change-type-1.yml

labels: {}

name: aws-privatelink-region-enabler
description: |
  allows enabling regions for privatelink aws accounts

priority: medium

contextType: datafile
contextSchema: /aws/account-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - supportedDeploymentRegions
