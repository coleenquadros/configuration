---
$schema: /app-interface/change-type-1.yml

labels: {}

name: shared-resources-secret-promoter
description: |
  Allow bumping the version of a vault-secret in a shared resources file.

priority: high

contextType: datafile
contextSchema: /openshift/shared-resources-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - openshiftResources[?(@.provider=="vault-secret")].version
