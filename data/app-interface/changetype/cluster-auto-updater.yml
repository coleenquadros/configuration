---
$schema: /app-interface/change-type-1.yml

labels: {}

name: cluster-auto-updater
description: |
  Allows updates on a cluster file.

priority: low

contextType: datafile
contextSchema: /openshift/cluster-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - consoleUrl
  - prometheusUrl
  - alertmanagerUrl
  - spec.id
  - spec.external_id
  - spec.version
  - spec.provision_shard_id
