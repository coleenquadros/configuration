---
$schema: /app-interface/change-type-1.yml

labels: {}

name: dns-zone-self-service
description: |
  allows updates on dns zones

priority: high

contextType: datafile
contextSchema: /dependencies/dns-zone-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - records
  - unmanaged_record_names
