---
$schema: /app-interface/change-type-1.yml

labels: {}

name: glitchtip-project
description: |
  Allow self-service on glitchtip projects.

priority: low

contextType: datafile
contextSchema: /dependencies/glitchtip-project-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - $
