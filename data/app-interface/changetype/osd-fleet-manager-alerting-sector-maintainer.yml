---
$schema: /app-interface/change-type-1.yml

labels: {}

name: osd-fleet-manager-alerting-sector-maintainer
description: |
  Allow updates to sectors in OSD Fleet Manager.

priority: urgent

contextType: datafile
contextSchema: /openshift/namespace-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - openshiftResources[?(@.path=="/observability/prometheusrules/osd-fleet-manager-nonprod.prometheusrules.yaml.j2")].variables.sectors
