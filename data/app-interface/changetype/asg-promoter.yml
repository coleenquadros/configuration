---
$schema: /app-interface/change-type-1.yml

labels: {}

name: asg-promoter
description: |
  Allow updating the image of an AutoScalingGroup (asg) provider in a namespace external resources

priority: high

contextType: datafile
contextSchema: /openshift/namespace-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - externalResources[?(@.provider=="aws")].resources[?(@.provider=="asg")].image
