---
$schema: /app-interface/change-type-1.yml

labels: {}

name: gabi-signoff-manager
description: |
  allows a manager to approve requests related to a gabi instance

contextType: datafile
contextSchema: /app-sre/gabi-instance-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - users
  - expirationDate
