---
$schema: /app-interface/change-type-1.yml

labels: {}

name: cloudflare-worker-promoter
description: |
  Allow updating the ref of a cloudflare worker_script provider in a namespace external resources

priority: high

contextType: datafile
contextSchema: /openshift/namespace-1.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - externalResources[?(@.provider=="cloudflare")].resources[?(@.provider=="worker_script")].ref
