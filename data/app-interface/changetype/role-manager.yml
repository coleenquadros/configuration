---
$schema: /app-interface/change-type-1.yml

labels: {}

name: role-manager
description: |
  Higher level change permission on roles.

priority: high

contextType: datafile
contextSchema: /access/role-1.yml

inherit:
- $ref: /app-interface/changetype/add-role-member.yml

changes:
- provider: jsonPath
  jsonPathSelectors:
  - glitchtip_teams
