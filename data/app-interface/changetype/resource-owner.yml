---
$schema: /app-interface/change-type-1.yml

labels: {}

name: resource-owner
description: |
  Allows general updates on app-interface resources.

priority: high

contextType: resourcefile

changes:
- provider: jsonPath
  jsonPathSelectors:
  - $
