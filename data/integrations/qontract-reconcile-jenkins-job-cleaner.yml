$schema: /app-sre/integration-1.yml

labels: {}

name: jenkins-job-cleaner

description: Delete Jenkins jobs in multiple tenant instances

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  disabled: true
  cmd: jenkins-job-cleaner
  run_for_valid_saas_file_changes: false

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage-int.yml
  spec:
    resources:
      requests:
        memory: 200Mi
        cpu: 100m
      limits:
        memory: 400Mi
        cpu: 200m
    logs:
      slack: true
    cache: true
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production-int.yml
  spec:
    resources:
      requests:
        memory: 200Mi
        cpu: 100m
      limits:
        memory: 400Mi
        cpu: 200m
    logs:
      slack: true
    cache: true

schemas:
- /app-sre/integration-1.yml
- /app-interface/app-interface-settings-1.yml # for secret reader
- /dependencies/jenkins-instance-1.yml # for jobs
- /dependencies/jenkins-config-1.yml # for jobs
