$schema: /app-sre/integration-1.yml

labels: {}

name: openshift-saas-deploy-trigger-cleaner

description: Cleanup trigger resources (Tekton PipelineRun)

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  disabled: true
  cmd: openshift-saas-deploy-trigger-cleaner

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage-int.yml
  spec:
    resources:
      requests:
        memory: 800Mi
        cpu: 400m
      limits:
        memory: 1000Mi
        cpu: 600m
    extraArgs: --no-use-jump-host
    logs:
      slack: true
    trigger: true
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production-int.yml
  spec:
    resources:
      requests:
        memory: 800Mi
        cpu: 400m
      limits:
        memory: 1000Mi
        cpu: 600m
    extraArgs: --no-use-jump-host
    logs:
      slack: true
    trigger: true

schemas:
- /app-interface/app-interface-settings-1.yml
- /app-sre/integration-1.yml
- /app-sre/pipelines-provider-1.yml
- /app-sre/tekton-provider-defaults-1.yml
- /dependencies/dependency-1.yml
- /openshift/cluster-1.yml
- /openshift/namespace-1.yml
