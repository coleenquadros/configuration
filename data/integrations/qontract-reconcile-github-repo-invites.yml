$schema: /app-sre/integration-1.yml

labels: {}

name: qontract-reconcile github-repo-invites

shortDescription: Accept GitHub repository invitations for known repositories

description: |
  tbd

upstream: https://github.com/app-sre/qontract-reconcile

usage: |
  Usage: qontract-reconcile [BASE_OPTIONS] github-repo-invites

  Base Options:
    --config TEXT                   Path to configuration file in toml format.
                                    [required]
    --dry-run / --no-dry-run        If `true`, it will only print the planned
                                    actions that would be performed, without
                                    executing them.
    --log-level [DEBUG|INFO|WARNING|ERROR|CRITICAL]
                                    log-level of the command. Defaults to INFO.

pr_check:
  cmd: github-repo-invites

schemas:
- /app-sre/app-1.yml
- /dependencies/jenkins-config-1.yml
