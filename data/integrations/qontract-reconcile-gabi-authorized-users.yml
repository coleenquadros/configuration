$schema: /app-sre/integration-1.yml

labels: {}

name: gabi-authorized-users

description: Manage Gabi authorized users

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  cmd: gabi-authorized-users

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage.yml
  spec:
    resources:
      requests:
        memory: 600Mi
        cpu: 200m
      limits:
        memory: 800Mi
        cpu: 300m
    extraArgs: --external --no-use-jump-host
    logs:
      slack: true
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production.yml
  spec:
    resources:
      requests:
        memory: 600Mi
        cpu: 200m
      limits:
        memory: 800Mi
        cpu: 300m
    extraArgs: --external --no-use-jump-host
    logs:
      slack: true

schemas:
- /app-interface/app-interface-settings-1.yml
- /app-sre/integration-1.yml
- /app-sre/gabi-instance-1.yml
- /access/user-1.yml
- /openshift/namespace-1.yml
- /openshift/cluster-1.yml
- /dependencies/dependency-1.yml
- /aws/account-1.yml
