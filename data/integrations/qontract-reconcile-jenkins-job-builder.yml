$schema: /app-sre/integration-1.yml

labels: {}

name: qontract-reconcile jenkins-job-builder

shortDescription: Manage Jenkins jobs configurations using jenkins-jobs

description: |
  tbd

upstream: https://github.com/app-sre/qontract-reconcile

usage: |
  Usage: qontract-reconcile [BASE_OPTIONS] jenkins-roles [OPTIONS]

  Options:
    --io-dir TEXT             directory of input/output files.
    --compare / --no-compare  compare between current and desired state.

  Base Options:
    --config TEXT                   Path to configuration file in toml format.
                                    [required]
    --dry-run / --no-dry-run        If `true`, it will only print the planned
                                    actions that would be performed, without
                                    executing them.
    --log-level [DEBUG|INFO|WARNING|ERROR|CRITICAL]
                                    log-level of the command. Defaults to INFO.
    --help                          Show this message and exit.

pr_check:
  cmd: jenkins-job-builder

schemas:
- /dependencies/jenkins-instance-1.yml
- /dependencies/jenkins-config-1.yml
- /app-sre/saas-file-1.yml
- /app-sre/environment-1.yml
- /access/user-1.yml
- /access/external-user-1.yml
