$schema: /app-sre/integration-1.yml

labels: {}

name: jenkins-worker-fleets

description: Manage web hooks to Jenkins jobs

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  cmd: jenkins-worker-fleets
  early_exit: true

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage-int.yml
  spec:
    sleepDurationSecs: "600"
    resources:
      requests:
        memory: 100Mi
        cpu: 100m
      limits:
        memory: 200Mi
        cpu: 200m
    logs:
      slack: true
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production-int.yml
  spec:
    sleepDurationSecs: "600"
    resources:
      requests:
        memory: 100Mi
        cpu: 100m
      limits:
        memory: 200Mi
        cpu: 200m
    logs:
      slack: true

schemas:
- /app-sre/integration-1.yml
- /app-interface/app-interface-settings-1.yml
- /dependencies/jenkins-instance-1.yml
- /openshift/namespace-1.yml
- /aws/account-1.yml
- /dependencies/dependency-1.yml
