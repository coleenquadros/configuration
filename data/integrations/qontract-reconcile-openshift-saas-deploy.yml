$schema: /app-sre/integration-1.yml

labels: {}

name: qontract-reconcile openshift-saas-deploy

shortDescription: Manage OpenShift resources defined in Saas files (SaasHerder).

description: |
  tbd

upstream: https://github.com/app-sre/qontract-reconcile

usage: |
  Usage: qontract-reconcile [BASE_OPTIONS] openshift-saas-deploy [OPTIONS]

  Options:
    --thread-pool-size INTEGER      number of threads to run in parallel.
    --internal / --external         manage resources in internal or external
                                    clusters only.

    --use-jump-host / --no-use-jump-host
                                    use jump host if defined.
    --help                          Show this message and exit.

  Base Options:
    --config TEXT                   Path to configuration file in toml format.
                                    [required]
    --dry-run / --no-dry-run        If `true`, it will only print the planned
                                    actions that would be performed, without
                                    executing them.
    --log-level [DEBUG|INFO|WARNING|ERROR|CRITICAL]
                                    log-level of the command. Defaults to INFO.

pr_check:
  cmd: openshift-saas-deploy

schemas:
  - /access/user-1.yml
