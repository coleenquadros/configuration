$schema: /app-sre/integration-1.yml

labels: {}

name: qontract-reconcile openshift-saas-deploy

description: Manage OpenShift resources defined in Saas files (SaasHerder).

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  # hard coded in manual_reconcile.sh so it runs on when the MR title contains "saas-deploy-full"
  disabled: true
  cmd: openshift-saas-deploy

schemas:
- /app-sre/environment-1.yml
- /app-sre/product-1.yml
