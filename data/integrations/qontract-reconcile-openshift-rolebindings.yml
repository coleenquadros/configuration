$schema: /app-sre/integration-1.yml

labels: {}

name: qontract-reconcile openshift-rolebindings

shortDescription: Configures Rolebindings in OpenShift clusters

description: |
  tbd

upstream: https://github.com/app-sre/qontract-reconcile

usage: |
  Usage: qontract-reconcile [BASE_OPTIONS] openshift-rolebindings [OPTIONS]

  Options:
    --thread-pool-size INTEGER  number of threads to run in parallel

  Base Options:
    --config TEXT                   Path to configuration file in toml format.
                                    [required]
    --dry-run / --no-dry-run        If `true`, it will only print the planned
                                    actions that would be performed, without
                                    executing them.
    --log-level [DEBUG|INFO|WARNING|ERROR|CRITICAL]
                                    log-level of the command. Defaults to INFO.

pr_check:
  cmd: openshift-rolebindings

schemas:
- /access/user-1.yml
- /access/bot-1.yml
- /access/role-1.yml
- /openshift/cluster-1.yml
- /openshift/namespace-1.yml
