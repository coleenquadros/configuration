$schema: /app-sre/integration-1.yml

labels: {}

name: qontract-reconcile saas-file-owners

shortDescription: Adds an `approved` label on merge requests based on approver schema for saas files.

description: tbd

upstream: https://github.com/app-sre/qontract-reconcile

usage: |
  Usage: qontract-reconcile [BASE_OPTIONS] saas-file-owners

  Base Options:
    --config TEXT                   Path to configuration file in toml format.
                                    [required]
    --dry-run / --no-dry-run        If `true`, it will only print the planned
                                    actions that would be performed, without
                                    executing them.
    --log-level [DEBUG|INFO|WARNING|ERROR|CRITICAL]
                                    log-level of the command. Defaults to INFO.

pr_check:
  cmd: saas-file-owners $gitlabMergeRequestTargetProjectId $gitlabMergeRequestIid

schemas:
  - /openshift/performance-parameters-1.yml
