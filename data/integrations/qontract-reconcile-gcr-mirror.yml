$schema: /app-sre/integration-1.yml

labels: {}

name: gcr-mirror

description: Mirrors external images into Google Container Registry.

upstream: https://github.com/app-sre/qontract-reconcile

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage.yml
  spec:
    resources:
      requests:
        memory: 150Mi
        cpu: 200m
      limits:
        memory: 200Mi
        cpu: 300m
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production.yml
  spec:
    resources:
      requests:
        memory: 150Mi
        cpu: 200m
      limits:
        memory: 200Mi
        cpu: 300m

schemas:
- /app-interface/app-interface-settings-1.yml
- /app-sre/integration-1.yml
- /dependencies/dependency-1.yml
- /gcp/project-1.yml
- /app-sre/app-1.yml
- /dependencies/container-image-mirror-1.yml
