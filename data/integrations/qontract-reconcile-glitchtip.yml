$schema: /app-sre/integration-1.yml

labels: {}

name: glitchtip

description: Manages GlitchTip organisations, teams, projects, and users

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  cmd: glitchtip
  early_exit: true

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage-int.yml
  spec:
    resources:
      requests:
        memory: 200Mi
        cpu: 200m
      limits:
        memory: 200Mi
        cpu: 200m
    logs:
      slack: true
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production-int.yml
  spec:
    resources:
      requests:
        memory: 200Mi
        cpu: 200m
      limits:
        memory: 200Mi
        cpu: 200m
    logs:
      slack: true

schemas:
- /access/role-1.yml
- /access/user-1.yml
- /app-interface/app-interface-settings-1.yml
- /app-sre/app-1.yml
- /app-sre/integration-1.yml
- /dependencies/github-org-1.yml
- /dependencies/glitchtip-instance-1.yml
- /dependencies/glitchtip-organization-1.yml
- /dependencies/glitchtip-team-1.yml
- /dependencies/glitchtip-project-1.yml
