$schema: /app-sre/integration-1.yml

labels: {}

name: qontract-reconcile github-repo-permissions-validator

shortDescription: tbd

description: tbd

upstream: https://github.com/app-sre/qontract-reconcile

usage: |
  Usage: qontract-reconcile [BASE_OPTIONS] github-repo-permissions-validator [OPTIONS] INSTANCE_NAME BOT_TOKEN_ORG_NAME

  Base Options:
    --config TEXT                   Path to configuration file in toml format.
                                    [required]
    --dry-run / --no-dry-run        If `true`, it will only print the planned
                                    actions that would be performed, without
                                    executing them.
    --log-level [DEBUG|INFO|WARNING|ERROR|CRITICAL]
                                    log-level of the command. Defaults to INFO.

pr_check:
  cmd: github-repo-permissions-validator ci-ext app-sre

schemas:
- /dependencies/jenkins-config-1.yml
