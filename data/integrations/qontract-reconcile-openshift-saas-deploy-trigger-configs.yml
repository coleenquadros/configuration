$schema: /app-sre/integration-1.yml

labels: {}

name: qontract-reconcile openshift-saas-deploy-trigger-configs

shortDescription: Trigger jobs in Jenkins when configuration changed.

description: |
  tbd

upstream: https://github.com/app-sre/qontract-reconcile

usage: |
  Usage: qontract-reconcile [BASE_OPTIONS] openshift-saas-deploy-trigger-configs


  Base Options:
    --config TEXT                   Path to configuration file in toml format.
                                    [required]
    --dry-run / --no-dry-run        If `true`, it will only print the planned
                                    actions that would be performed, without
                                    executing them.
    --log-level [DEBUG|INFO|WARNING|ERROR|CRITICAL]
                                    log-level of the command. Defaults to INFO.

pr_check:
  cmd: openshift-saas-deploy-trigger-configs
  state: true

schemas:
- /openshift/cluster-1.yml
- /openshift/namespace-1.yml
- /app-sre/app-1.yml
- /app-sre/environment-1.yml
- /app-sre/saas-file-1.yml
