$schema: /app-sre/integration-1.yml

labels: {}

name: github-owners

description: Adds an `approved` label on gitlab merge requests based on OWNERS files schema.

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  cmd: github-owners

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage.yml
  spec:
    resources:
      requests:
        memory: 100Mi
        cpu: 100m
      limits:
        memory: 160Mi
        cpu: 200m
    logs:
      slack: true

schemas:
- /access/user-1.yml
- /access/bot-1.yml
- /access/role-1.yml
- /access/permission-1.yml
- /dependencies/github-org-1.yml
- /app-interface/app-interface-settings-1.yml
- /app-sre/integration-1.yml
- /dependencies/dependency-1.yml
