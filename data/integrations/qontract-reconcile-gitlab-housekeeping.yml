$schema: /app-sre/integration-1.yml

labels: {}

name: qontract-reconcile gitlab-housekeeping

shortDescription: Manage issues and merge requests on GitLab projects

description: |
  tbd

upstream: https://github.com/app-sre/qontract-reconcile

usage: |
  Usage: qontract-reconcile [BASE_OPTIONS] gitlab-housekeeping [OPTIONS] GITLAB_PROJECT_ID

  Options:
    --days-interval INTEGER         interval of days between actions.
    --enable-deletion / --no-enable-deletion
                                    enable destroy/replace action.

  Base Options:
    --config TEXT                   Path to configuration file in toml format.
                                    [required]
    --dry-run / --no-dry-run        If `true`, it will only print the planned
                                    actions that would be performed, without
                                    executing them.
    --log-level [DEBUG|INFO|WARNING|ERROR|CRITICAL]
                                    log-level of the command. Defaults to INFO.

schemas:
- /app-sre/app-1.yml
- /dependencies/gitlab-instance-1.yml
