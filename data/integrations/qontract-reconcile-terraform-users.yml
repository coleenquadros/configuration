$schema: /app-sre/integration-1.yml

labels: {}

name: qontract-reconcile terraform-users

shortDescription: Manage AWS users using Terraform

description: |
  tbd

upstream: https://github.com/app-sre/qontract-reconcile

usage: |
  Usage: qontract-reconcile [BASE_OPTIONS] terraform-users [OPTIONS]

  Options:
    --print-only / --no-print-only  only print the terraform config file.
    --io-dir TEXT                   directory of input/output files.
    --thread-pool-size INTEGER      number of threads to run in parallel.
    --enable-deletion / --no-enable-deletion
                                    enable destroy/replace action.
    --send-mails / --no-send-mails  send email notification to users.

  Base Options:
    --config TEXT                   Path to configuration file in toml format.
                                    [required]
    --dry-run / --no-dry-run        If `true`, it will only print the planned
                                    actions that would be performed, without
                                    executing them.
    --log-level [DEBUG|INFO|WARNING|ERROR|CRITICAL]
                                    log-level of the command. Defaults to INFO.
    --help                          Show this message and exit.

pr_check:
  cmd: terraform-users

schemas:
  - /access/user-1.yml
