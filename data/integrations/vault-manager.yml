$schema: /app-sre/integration-1.yml

labels: {}

name: vault-manager

shortDescription: Manage HashiCorp Vault configurations

description: |
  tbd

upstream: https://github.com/app-sre/vault-manager

usage: |
  Options:
    -dry-run
        If true, will only print planned actions
    -thread-pool-size int
        Some operations are running in parallel to achieve the best performance, so -thread-pool-size determine how many threads can be utilized (default 10)

schemas:
- /vault-config/audit-1.yml
- /vault-config/auth-1.yml
- /vault-config/policy-1.yml
- /vault-config/role-1.yml
- /vault-config/secret-engine-1.yml
