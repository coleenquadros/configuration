$schema: /app-sre/integration-1.yml

labels: {}

name: quay-membership

description: Configures the teams and members in Quay

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  cmd: quay-membership

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage.yml
  spec:
    resources:
      requests:
        memory: 50Mi
        cpu: 100m
      limits:
        memory: 150Mi
        cpu: 200m
    logs:
      slack: true

schemas:
- /access/user-1.yml
- /access/role-1.yml
- /access/permission-1.yml
- /dependencies/quay-org-1.yml
- /dependencies/quay-instance-1.yml
- /app-interface/app-interface-settings-1.yml
- /app-sre/integration-1.yml
- /dependencies/dependency-1.yml
- /access/bot-1.yml
