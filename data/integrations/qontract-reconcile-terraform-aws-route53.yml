$schema: /app-sre/integration-1.yml

labels: {}

name: terraform-aws-route53

description: Provisions AWS Route53 DNS zones and records

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  cmd: terraform-aws-route53

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage.yml
  spec:
    resources:
      requests:
        memory: 500Mi
        cpu: 100m
      limits:
        memory: 1000Mi
        cpu: 500m
    logs:
      slack: true
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production.yml
  spec:
    resources:
      requests:
        memory: 500Mi
        cpu: 100m
      limits:
        memory: 1000Mi
        cpu: 500m
    logs:
      slack: true
    shardingStrategy: per-aws-account

schemas:
- /app-interface/app-interface-settings-1.yml
- /app-sre/integration-1.yml
- /dependencies/dependency-1.yml
- /dependencies/dns-zone-1.yml
- /aws/account-1.yml
- /aws/vpc-1.yml
- /aws/ecr-1.yml
- /openshift/cluster-1.yml
- /dependencies/github-org-1.yml
- /openshift/namespace-1.yml
