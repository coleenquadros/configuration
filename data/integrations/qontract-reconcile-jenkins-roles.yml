$schema: /app-sre/integration-1.yml

labels: {}

name: jenkins-roles

description: Manage Jenkins roles association via REST API

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  cmd: jenkins-roles

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage-int.yml
  spec:
    resources:
      requests:
        memory: 100Mi
        cpu: 100m
      limits:
        memory: 200Mi
        cpu: 200m
    logs:
      slack: true
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production-int.yml
  spec:
    resources:
      requests:
        memory: 100Mi
        cpu: 100m
      limits:
        memory: 200Mi
        cpu: 200m
    logs:
      slack: true

schemas:
- /app-sre/integration-1.yml
- /app-interface/app-interface-settings-1.yml # for secret reader
- /access/role-1.yml # fetching roles
- /access/user-1.yml # ... and the users
- /access/bot-1.yml # ... and bots
- /access/permission-1.yml # ... that have permissions
- /dependencies/jenkins-instance-1.yml # ... on jenkins instances
