$schema: /app-sre/integration-1.yml

labels: {}

name: gitlab-labeler

description: Guesses and adds labels to merge requests according to changed paths.

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  # hard coded in manual_reconcile.sh so it runs
  disabled: true
  cmd: gitlab-labeler $gitlabMergeRequestTargetProjectId $gitlabMergeRequestIid

schemas:
- /dependencies/gitlab-instance-1.yml
- /app-interface/app-interface-settings-1.yml
- /app-sre/integration-1.yml
- /dependencies/dependency-1.yml
