$schema: /app-sre/integration-1.yml

labels: {}

name: dyn-traffic-director

description: Configure Dyn Traffic Director service

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  cmd: dyn-traffic-director

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage.yml
  spec:
    resources:
      requests:
        memory: 100Mi
        cpu: 100m
      limits:
        memory: 150Mi
        cpu: 200m
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production.yml
  spec:
    resources:
      requests:
        memory: 100Mi
        cpu: 100m
      limits:
        memory: 150Mi
        cpu: 200m

schemas:
- /access/user-1.yml
- /app-interface/app-interface-settings-1.yml
- /app-sre/integration-1.yml
- /dependencies/dependency-1.yml
- /dependencies/dyn-traffic-director-1.yml
- /openshift/cluster-1.yml
