kind: Service
apiVersion: v1
metadata:
  name: {{ resource.namespace.cluster.name }}
  annotations:
    skupper.io/proxy: http
    skupper.io/target: "{{ resource.namespace.cluster.name }}.skupper-fake-api-server.svc.cluster.local"
spec:
  ports:
  - port: 8080
