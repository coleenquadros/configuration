apiVersion: v1
kind: Service
metadata:
  name: {{ resource.namespace.cluster.name }}
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: http-stub
  type: ClusterIP

