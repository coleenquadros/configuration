acl: private
server_side_encryption_configuration:
  rule:
    apply_server_side_encryption_by_default:
      sse_algorithm: "AES256"
lifecycle_rules:
  - id: archive-expiration
    enabled: true
    prefix: "bundle-archive/"
    noncurrent_version_expiration:
      days: 3
    expiration:
      days: 60
