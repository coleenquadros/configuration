replication_group_description: elasticache HA 1
engine: redis
engine_version: 5.0.4
auto_minor_version_upgrade: false
node_type: cache.m5.large
automatic_failover_enabled: true
at_rest_encryption_enabled: true
transit_encryption_enabled: true
port: 6379
# the following variables are not overridable
# create a new defaults file if required
cluster_mode.num_node_groups: 3
cluster_mode.replicas_per_node_group: 2
# the following subjects should be created beforehand
# these values are just examples
security_group_ids: ['sg-31337']
subnet_group_name: 'subnet-group-name'
