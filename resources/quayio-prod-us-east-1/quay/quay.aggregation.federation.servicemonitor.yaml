---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: app-sre
  name: quay-aggregation-federation-service-monitor
spec:
  endpoints:
  - interval: 30s
    path: /federate
    port: web
    params:
      'match[]':
      - '{__name__=~"aggregation:.*"}'
  namespaceSelector:
    matchNames:
    - openshift-customer-monitoring
  selector:
    matchLabels:
      prometheus: quay-aggregation
