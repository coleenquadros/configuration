apiVersion: v1
kind: ConfigMap
metadata:
  annotations:
    grafana-folder: /grafana-dashboard-definitions/Insights
  labels:
    grafana_dashboard: "true"
  name: grafana-dashboard-ephemeral-cluster
data:
  ephemeral-cluster-grafana.json: |-
    {}
