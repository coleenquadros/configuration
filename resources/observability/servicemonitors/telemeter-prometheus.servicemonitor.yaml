apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: app-sre
  name: telemeter-prometheus-{{environment}}
spec:
  endpoints:
  - interval: 60s
    port: https
    scheme: https
    tlsConfig:
      insecureSkipVerify: true
  namespaceSelector:
    matchNames:
    - {{namespace}}
  selector:
    matchLabels:
      prometheus: telemeter
