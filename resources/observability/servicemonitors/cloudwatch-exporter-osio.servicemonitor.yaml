apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cloudwatch-exporter-osio
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - port: prom
    interval: 30s
  namespaceSelector:
    matchNames:
    - {{namespace}}
  selector:
    matchLabels:
      app: cloudwatch-exporter-osio
