apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: app-sre
  name: servicemonitor-dashdotdb-{{environment}}
spec:
  endpoints:
  - interval: 60s
    path: /api/v1/metrics
    port: dashdotdb
    scheme: http
  namespaceSelector:
    matchNames:
    - {{namespace}}
  selector:
    matchLabels:
      app: dashdotdb
