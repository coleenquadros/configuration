apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: che-dev-dogfooding-exporter
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 30s
    port: 8087-tcp
  namespaceSelector:
    matchNames:
    - dogfooding
  selector:
    matchLabels:
      component: che
