apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ name | default('signalfx-prometheus-exporter') }}
  labels:
    prometheus: app-sre
spec:
  selector:
    matchLabels:
      app: {{ name | default('signalfx-prometheus-exporter') }}
  endpoints:
  - port: observability
    interval: 30s
  namespaceSelector:
    matchNames:
    - {{namespace}}
