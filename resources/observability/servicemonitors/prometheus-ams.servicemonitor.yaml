apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: prometheus-ams
  labels:
    prometheus: app-sre
spec:
  selector:
    matchLabels:
      app: prometheus-ams
      prometheus: ams
  endpoints:
  - port: web
    interval: 30s
