apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: app-sre
  name: servicemonitor-dashdotdb-cso-{{environment}}
spec:
  endpoints:
  - interval: 6h
    path: /api/v1/imagemanifestvuln/metrics
    port: dashdotdb
    scheme: http
  namespaceSelector:
    matchNames:
    - {{namespace}}
  selector:
    matchLabels:
      app: dashdotdb
