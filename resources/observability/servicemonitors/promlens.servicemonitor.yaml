apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: app-sre
  name: promlens
spec:
  endpoints:
  - interval: 60s
    port: promlens
    path: /metrics
    scheme: http
  selector:
    matchLabels:
      app: promlens
  namespaceSelector:
    matchNames:
    - {{namespace}}
