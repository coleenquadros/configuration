apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ name | default('aws-resource-exporter') }}
  labels:
    prometheus: app-sre
spec:
  selector:
    matchLabels:
      app: {{ name | default('aws-resource-exporter') }}
  endpoints:
  - port: http
    interval: 30s
  namespaceSelector:
    matchNames:
    - {{namespace}}
