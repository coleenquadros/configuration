apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: aws-resource-exporter
  labels:
    prometheus: app-sre
spec:
  selector:
    matchLabels:
      app: aws-resource-exporter
  endpoints:
  - port: http
    interval: 30s
  namespaceSelector:
    matchNames:
    - {{namespace}}
