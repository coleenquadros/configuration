apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: aws-resource-exporter-osio
  labels:
    prometheus: app-sre
spec:
  selector:
    matchLabels:
      app: aws-resource-exporter-osio
  endpoints:
  - port: http
    interval: 30s
  namespaceSelector:
    matchNames:
    - {{namespace}}
