apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: app-sre
  name: servicemonitor-srs-tenant-manager-stage
spec:
  endpoints:
  - interval: 30s
    path: /q/metrics
    port: http
    scheme: http
  namespaceSelector:
    matchNames:
    - {{namespace}}
  selector:
    matchLabels:
      app: tenant-manager
      template: multitenant-apicurio-registry
      component: tenant-manager
