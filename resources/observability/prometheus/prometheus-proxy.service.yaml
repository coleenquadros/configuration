apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus-proxy
  name: prometheus-proxy
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: http
  selector:
    app: prometheus
  type: ClusterIP
