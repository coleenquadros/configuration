apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-exporter-api-token
  annotations:
    qontract.ignore_reconcile_time: "true"
data:
    api_token: {{{ vault('app-sre/creds/cloudflare/app-sre', 'api_token') }}}
