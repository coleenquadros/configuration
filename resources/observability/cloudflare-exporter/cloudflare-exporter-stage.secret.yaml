apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-exporter-api-token
  annotations:
    qontract.recycle: "true"
data:
    api_token: "{{% b64encode %}}{{{ vault('app-sre/creds/cloudflare/app-interface-read-metrics', 'api_token') }}}{{%- endb64encode %}}"
