apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: app-interface
  name: prometheus-app-sre{{environment}}
spec:
  host: {{host}}
  port:
    targetPort: {{ target_port | default('http') }}
  to:
    kind: Service
    name: {{ service_name | default('prometheus-proxy') }}
    weight: 100
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
