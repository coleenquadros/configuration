---
$schema: /openshift/prometheus-rule-1.yml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  labels:
    prometheus: app-sre
    role: alert-rules
  name: token-refresher-production
spec:
  groups:
  - name: token-refresher-production
    rules:
    - alert: KasFleetManagerTokenRefresherDown - production
      annotations:
        message: "No targets found for Token Refresher in namespace {{ $labels.namespace }}. Token Refresher is down."
        runbook: "https://gitlab.cee.redhat.com/service/app-interface/tree/master/docs/managed-services/sop/token-refresher-down.md"
      expr: |
        absent(up{job="token-refresher",namespace="managed-services-production"} == 1)
      for: 15m
      labels:
        service: kas-fleet-manager
        severity: critical
