apiVersion: v1
kind: Service
metadata:
  labels:
    app: alertmanager-proxy
  name: alertmanager-proxy
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: http
  selector:
    app: alertmanager
  sessionAffinity: None
  type: ClusterIP
