apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    service: sentry
  name: sentry
spec:
  host: sentry.stage.openshift.com
  port:
    targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: sentry
    weight: 100
  wildcardPolicy: None
