---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pulp-api
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: pulp-api
  namespaceSelector:
    matchNames:
    - automation-hub-prod
  selector:
    matchLabels:
      app: pulp
