---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ccx-notification-service
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 5s
    path: /metrics
    port: ccx-notification-service-metrics-port
  namespaceSelector:
    matchNames:
    - ccx-data-pipeline-prod
  selector:
    matchLabels:
      app: ccx-notification-service
