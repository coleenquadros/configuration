---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: image-builder
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: image-builder
  namespaceSelector:
    matchNames:
    - image-builder-prod
  selector:
    matchLabels:
      app: image-builder
