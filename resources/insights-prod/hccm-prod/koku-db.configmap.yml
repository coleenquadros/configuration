apiVersion: v1
data:
  database-engine: postgresql
  # The commented out host still in the koku-db in v3 prod cluster.
  # The hccm dev should remove it if not needed.
  # database-host: cost-management-prod.cchll3oigdid.us-east-1.rds.amazonaws.com
  database-host: cost-management-prod.ckxxru2ayexw.us-east-1.rds.amazonaws.com
  database-name: cost-management-db
  database-port: "5432"
  database-service-name: POSTGRES_SQL
kind: ConfigMap
metadata:
  labels:
    app: koku
    template: koku-configmap
  name: koku-db
