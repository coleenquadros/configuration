---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: remediations
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: public
  namespaceSelector:
    matchNames:
    - remediations-prod
  selector:
    matchLabels:
      app: remediations
