---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ros-backend-engine-result-processor
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: metrics
  namespaceSelector:
    matchNames:
    - ros-prod
  selector:
    matchLabels:
      app: ros-backend
      pod: ros-backend-engine-result-processor
