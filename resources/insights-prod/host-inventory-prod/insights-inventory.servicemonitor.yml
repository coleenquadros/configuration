---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: insights-inventory
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: port-8080
  namespaceSelector:
    matchNames:
    - host-inventory-prod
  selector:
    matchLabels:
      app: insights-inventory
