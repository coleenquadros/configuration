---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: playbook-dispatcher
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: metrics
  namespaceSelector:
    matchNames:
    - playbook-dispatcher-prod
  selector:
    matchLabels:
      app: playbook-dispatcher
