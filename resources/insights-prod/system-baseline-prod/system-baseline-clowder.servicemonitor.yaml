---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: system-baseline-backend-service
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /mgmt/v0/metrics
    port: public
  namespaceSelector:
    matchNames:
    - system-baseline-prod
  selector:
    matchLabels:
      pod: system-baseline-backend-service
