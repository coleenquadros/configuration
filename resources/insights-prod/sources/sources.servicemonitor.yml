---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sources
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: '9394'
  namespaceSelector:
    matchNames:
    - sources-prod
  selector:
    matchLabels:
      app: sources
