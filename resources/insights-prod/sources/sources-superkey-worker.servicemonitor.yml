---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sources-superkey-worker
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: metrics
  namespaceSelector:
    matchNames:
    - sources-prod
  selector:
    matchLabels:
      app: sources-superkey-worker
