---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: insights-advisor-service
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /
    port: 8000-tcp
  namespaceSelector:
    matchNames:
    - advisor-prod
  selector:
    matchLabels:
      app: insights-advisor-service
