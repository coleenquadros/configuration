# this secret is needed by the marketplace deployment configuration
apiVersion: v1
data:
  database-engine: postgresql
  database-service-name: postgres_sql
  postgres-sql-service-host: marketplace-prod.ckxxru2ayexw.us-east-1.rds.amazonaws.com
  postgres-sql-service-port: '5432'
kind: ConfigMap
metadata:
  labels:
    app: marketplace
    name: marketplace
  name: marketplace-db
