---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: rbac
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: public
  namespaceSelector:
    matchNames:
    - rbac-stage
  selector:
    matchLabels:
      app: rbac
