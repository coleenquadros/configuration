---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kibana
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: 8443-tcp
  namespaceSelector:
    matchNames:
    - ingress-stage
  selector:
    matchLabels:
      app: kibana
