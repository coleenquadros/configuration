---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: platform-mq-prod
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: 9208-tcp
  namespaceSelector:
    matchNames:
    - platform-mq-stage
  selector:
    matchLabels:
      app: platform-mq-prod
