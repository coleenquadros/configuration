apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: app-sre
  name: servicemonitor-rhsm-system-profile-bridge
spec:
  endpoints:
    - interval: 10s
      path: /q/metrics
      port: public
  namespaceSelector:
    matchNames:
      - rhsm-api-proxy-stage
  selector:
    matchLabels:
      name: rhsm-system-profile-bridge

