apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: prometheus
  name: prometheus
spec:
  host: prometheus-mnm-sre.apps.crc-stage-01.p5t7.p1.openshiftapps.com
  port:
    targetPort: proxy-port
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: reencrypt
  to:
    kind: Service
    name: prometheus
    weight: 100
  wildcardPolicy: None
