apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: grafana
  name: metrics
spec:
  host: metrics-mnm-sre.apps.crc-stg-01.o4v9.p1.openshiftapps.com
  port:
    targetPort: proxy-port
  tls:
    termination: reencrypt
  to:
    kind: Service
    name: grafana
    weight: 100
  wildcardPolicy: None

