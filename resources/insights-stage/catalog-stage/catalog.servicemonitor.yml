---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: catalog
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: '9394'
  namespaceSelector:
    matchNames:
    - catalog-stage
  selector:
    matchLabels:
      app: catalog
