---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sources-api
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: metrics
  namespaceSelector:
    matchNames:
    - sources-stage
  selector:
    matchLabels:
      app: sources-api
