apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: gateway
  name: gateway-stage
spec:
  host: {{ host }}
  port:
    targetPort: 9092
  tls:
    termination: reencrypt
  to:
    kind: Service
    name: gateway
    weight: 100
  wildcardPolicy: None
