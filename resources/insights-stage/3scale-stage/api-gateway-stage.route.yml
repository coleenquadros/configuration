apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: 3scale-gateway
  name: api-gateway-stage
spec:
  host: {{ host }}
  port:
    targetPort: 9092
  tls:
    termination: reencrypt
  to:
    kind: Service
    name: apicast
    weight: 100
  wildcardPolicy: None
