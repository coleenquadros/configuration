---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: apicast
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: metrics
  namespaceSelector:
    matchNames:
    - 3scale-stage
  selector:
    matchLabels:
      app: apicast
