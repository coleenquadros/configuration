---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: policies
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: policies-ui-backend
  - interval: 15s
    path: /q/metrics
    port: policies-engine
  namespaceSelector:
    matchNames:
    - policies-stage
  selector:
    matchLabels:
      app: policies
