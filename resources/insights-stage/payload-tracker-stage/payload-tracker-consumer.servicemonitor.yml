---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: payload-tracker-consumer
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: metrics 
  namespaceSelector:
    matchNames:
    - payload-tracker-stage
  selector:
    matchLabels:
      pod: payload-tracker-consumer
