---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: system-baseline-backend-service
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /mgmt/v0/metrics
    port: metrics
  namespaceSelector:
    matchNames:
    - system-baseline-stage
  selector:
    matchLabels:
      pod: system-baseline-backend-service
