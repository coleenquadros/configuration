---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: receptor-switch
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: metrics
  namespaceSelector:
    matchNames:
    - receptor-stage
  selector:
    matchLabels:
      app: receptor-switch
