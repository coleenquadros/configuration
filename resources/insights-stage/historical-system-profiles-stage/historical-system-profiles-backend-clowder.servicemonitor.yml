---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: historical-system-profiles-backend-service
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /mgmt/v0/metrics
    port: public
  namespaceSelector:
    matchNames:
    - historical-system-profiles-stage
  selector:
    matchLabels:
      pod: historical-system-profiles-backend-service
