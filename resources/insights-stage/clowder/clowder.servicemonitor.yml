---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: clowder
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: "metrics"
  namespaceSelector:
    matchNames:
    - clowder-system
  selector:
    matchLabels:
      operator-name: clowder
