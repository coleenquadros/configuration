---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hooks-prometheus-exporter
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: metrics
  namespaceSelector:
    matchNames:
    - hooks-stage
  selector:
    matchLabels:
      app: hooks-prometheus-exporter
