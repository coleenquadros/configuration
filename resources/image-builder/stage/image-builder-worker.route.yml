apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: image-builder-worker
  name: image-builder-worker-route
spec:
  host: 4fx6t9iy0x8ja3q.api.stage.openshift.com
  port:
    targetPort: worker-api
  to:
    kind: Service
    name: image-builder-worker
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
