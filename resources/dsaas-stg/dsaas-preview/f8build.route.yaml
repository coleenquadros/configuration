apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    service: f8build
  name: f8build
spec:
  host: build.prod-preview.openshift.io
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: f8build
    weight: 100
  wildcardPolicy: None
