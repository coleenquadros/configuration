apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    service: f8cluster
  name: f8cluster
spec:
  host: cluster.prod-preview.openshift.io
  port:
    targetPort: "8087"
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: f8cluster
    weight: 100
  wildcardPolicy: None
