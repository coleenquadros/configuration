apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    service: apicast
  name: crda-api
spec:
  host: gw.api.prod-preview.openshift.io
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: apicast
    weight: 100
  wildcardPolicy: None
