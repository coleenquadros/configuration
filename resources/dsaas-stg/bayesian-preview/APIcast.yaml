apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    openshift.io/host.generated: "true"
  labels:
    service: apicast
    template: apicast
  name: crda-api
spec:
  port:
    targetPort: proxy
  to:
    kind: Service
    name: apicast
    weight: 100
  wildcardPolicy: None
