---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: clusters-service-allow-port-forward
rules:
- apiGroups: [""]
  resources: ["pods", "pods/portforward"]
  verbs: ["create"]
  resourceNames: [
    "*",
    ]
