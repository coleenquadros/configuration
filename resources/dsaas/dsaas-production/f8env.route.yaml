apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    service: f8env
  name: f8env
spec:
  host: env.openshift.io
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: f8env
    weight: 100
  wildcardPolicy: None
