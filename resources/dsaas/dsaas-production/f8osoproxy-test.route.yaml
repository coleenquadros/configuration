apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    service: f8osoproxy
  name: f8osoproxy-test
spec:
  host: osoproxy.openshift.io
  port:
    targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: f8osoproxy
    weight: 100
  wildcardPolicy: None
