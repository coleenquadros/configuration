apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    openshift.io/host.generated: "true"
  name: f8osoproxy-test
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: f8osoproxy
    weight: 100
  wildcardPolicy: None
