apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    service: f8notification
  name: notify-api
spec:
  host: notify.api.openshift.io
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: f8notification
    weight: 100
  wildcardPolicy: None
