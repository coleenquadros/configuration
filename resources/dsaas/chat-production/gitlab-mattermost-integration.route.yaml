apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: gitlab-mattermost-integration
spec:
  host: gitlab-chat.openshift.io
  port:
    targetPort: 5000-tcp
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: mattermost-gitlab-integration
    weight: 100
  wildcardPolicy: None
