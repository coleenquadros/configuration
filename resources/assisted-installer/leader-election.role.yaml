kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: leader-election
rules:
  - verbs:
      - create
    apiGroups:
      - ''
    resources:
      - configmaps
  - verbs:
      - get
      - update
      - delete
    apiGroups:
      - ''
    resources:
      - configmaps
    resourceNames: ["assisted-service-leader-election-helper"]
