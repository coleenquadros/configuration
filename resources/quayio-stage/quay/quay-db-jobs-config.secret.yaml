apiVersion: v1
kind: Secret
metadata:
  name: {{{ name }}}
  annotations:
    qontract.ignore_reconcile_time: "true"
    qontract.recycle: "true"
data:
  NEW_DB_USER: {{% b64encode %}}{{{ vault('app-interface/quayio-stage/quayio-stage/quay-db-jobs', 'new_db_user', '{{ vault_secret_version }}') }}}{{% endb64encode %}}
  NEW_DB_USER_PASSWORD: {{% b64encode %}}{{{ vault('app-interface/quayio-stage/quayio-stage/quay-db-jobs', 'new_db_password', '{{ vault_secret_version }}') }}}{{% endb64encode %}}

