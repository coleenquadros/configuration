apiVersion: v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: quayio-service-tool
  name: quayio-service-tool-route
spec:
  host: servicetool.stage.quay.io
  port:
    targetPort: http
  to:
    kind: Service
    name: quayio-service-tool
  tls:
    termination: edge
