apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: web-rca-ui
  name: web-rca-ui-production
spec:
  host: web-rca.devshift.net
  to:
    kind: Service
    name: web-rca-ui
    weight: 100
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
