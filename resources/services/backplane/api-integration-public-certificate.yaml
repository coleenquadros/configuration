apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-public-integration-backplane-devshift-net
spec:
  secretName: public-cert

  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 2048

  usages:
    - server auth

  dnsNames:
    - api-public.integration.backplane.devshift.net

  issuerRef:
    name: letsencrypt-devshiftnet-dns
    kind: ClusterIssuer
    group: cert-manager.io
