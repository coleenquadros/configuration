apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: cos-fleet-manager
  name: cos-fleet-manager-stage
spec:
  host: cos-fleet-manager-stage.api.stage.openshift.com
  to:
    kind: Service
    name: cos-fleet-manager-envoy
    weight: 100
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: reencrypt
