apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: hypershift-management-clusters
spec:
  predicates:
  - requiredClusterSelector:
      labelSelector:
        matchLabels:
          api.openshift.com/hypershift: "true"
