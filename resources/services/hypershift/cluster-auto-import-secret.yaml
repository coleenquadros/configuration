apiVersion: v1
kind: Secret
metadata:
  name: auto-import-secret
stringData:
  autoImportRetry: "5"
  token: {{{ vault('app-sre/creds/kube-configs/{{cluster}}', 'token') }}}
  server: {{{ vault('app-sre/creds/kube-configs/{{cluster}}', 'server') }}}
type: Opaque
