apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: srs-fleet-manager
  name: srs-fleet-manager
spec:
  # TODO request new subdomain
  # host: fixn5jemlt2d50b.api.stage.openshift.com
  to:
    kind: Service
    name: srs-fleet-manager-envoy
    weight: 100
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
