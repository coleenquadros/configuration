apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: apicurio-registry
  name: service-registry-bu98-stage
spec:
  host: bu98.serviceregistry-stage.rhcloud.com
  to:
    kind: Service
    name: apicurio-registry-envoy
    weight: 100
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
