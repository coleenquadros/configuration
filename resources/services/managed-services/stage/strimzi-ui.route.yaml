apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: strimzi-ui
  name: strimzi-ui
spec:
  host: somethingrandom.api.stage.openshift.com
  to:
    kind: Service
    name: strimzi-ui
    weight: 100
  tls:
    termination: edge
