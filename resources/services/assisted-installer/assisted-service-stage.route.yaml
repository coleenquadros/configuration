apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: assisted-service
  name: assisted-service
spec:
  host: "wllyvs40ee788l7.api.stage.openshift.com"
  to:
    kind: Service
    name: assisted-service
    weight: 100
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
