apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: assisted-service
  name: assisted-image-service
spec:
  host: "zoscaru4s08w1mz.api.openshift.com"
  path: "/images"
  to:
    kind: Service
    name: assisted-image-service
    weight: 100
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
