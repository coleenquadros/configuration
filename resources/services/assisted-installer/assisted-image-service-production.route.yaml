apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    # Cert-manager annotation set in assisted-service-production.route.yaml
    # All routes using the same host share the same certificate
  labels:
    service: assisted-service
  name: assisted-image-service
spec:
  host: "zoscaru4s08w1mz.api.openshift.com"
  path: "/images"
  to:
    kind: Service
    name: assisted-image-service
    weight: 100
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
