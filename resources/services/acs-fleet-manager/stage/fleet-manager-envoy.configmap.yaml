---
apiVersion: v1
kind: ConfigMap
metadata:
  name: fleet-manager-envoy-config
  annotations:
    qontract.recycle: "true"
data:
  main.yaml: |
    # Empty for now (bootstraping phase).
    # See resources/services/managed-services/stage/kas-fleet-manager-envoy.configmap.yaml
    # for an inspiration.
