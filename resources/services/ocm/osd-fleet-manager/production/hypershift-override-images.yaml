apiVersion: v1
kind: ConfigMap
metadata:
  name: hypershift-override-images
data:
  acm-hypershift-operator-overriding.yaml: |+
    apiVersion: template.openshift.io/v1
    kind: Template
    metadata:
      name: hypershift-operator-override
    objects:

    - apiVersion: v1
      kind: ConfigMap
      metadata:
        name: hypershift-override-images
        namespace: ${CLUSTER_NAME}
      data:

    parameters:
    - name: CLUSTER_NAME
      required: true


