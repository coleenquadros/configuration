apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: ocm-api-tests
  labels:
    app: ocm-api-tests
spec:
  host: api.integration.openshift.com
  path: "/api/ocm-api-tests/"
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: reencrypt
  to:
    kind: Service
    name: ocm-api-tests