apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    # Cert-manager annotation set in gateway-server.route.yaml
    # All routes using the same host share the same certificate
    haproxy.router.openshift.io/timeout: 5m
    haproxy.router.openshift.io/balance: roundrobin
    haproxy.router.openshift.io/disable_cookies: "true"
  name: osd-fleet-manager
spec:
  host: mizg8z5wj7s6cpn.api.openshift.com
  path: "/api/osd_fleet_mgmt"
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: reencrypt
  to:
    kind: Service
    name: osd-fleet-mgmt-envoy-eu-west-1
    weight: 100
