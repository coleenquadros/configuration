- job-template:
    id: 'gl-test-vault-devshift-net-stage-access'
    name: 'gl-test-vault-devshift-net-stage-access'
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - vault-secrets:
        <<: *vault_stage
        secrets:
        - *vault_stage_test_secret
