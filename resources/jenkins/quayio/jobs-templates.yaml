- job-template:
    id: 'gh-build-master-quayio'
    name: '{gh_org}-{gh_repo}-gh-build-master'
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *quayio_backup_ecr
    <<: *gh_build_master
