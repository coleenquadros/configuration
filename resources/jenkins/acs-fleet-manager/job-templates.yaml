- job-template:
    id: acs-fleet-manager-e2e
    name: "{gh_repo}-e2e-{branch}"
    wrappers:
      - timeout_wrapper:
          timeout: "{timeout}"
      - vault-secrets:
          <<: *vault_defaults
          secrets:
          - *acs-fleet-manager-e2e-test
    <<: *gh_build_master
