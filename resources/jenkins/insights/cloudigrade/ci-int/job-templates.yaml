- job-template:
    id: 'insights-cloudigrade-gh-pr-check'
    name: '{gh_org}-{gh_repo}-pr-check'
    branch: "master"
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - ansicolor
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *insights_ephemeral_oc_login
        - *app_interface_creds
        - *insights_rh_registry_pull_secret
        - *quay_secret_push
        - *quay_api_token
        - *insightsdroid_github
        - *insights_cloudigrade_sonarqube_token
        - *insights_cloudigrade_rh_it_root_ca_cert_url
    <<: *insights_gh_pr_check

- job-template:
    id: 'insights-cloudigrade-gh-build-master'
    name: '{gh_org}-{gh_repo}-gh-build-{branch}'
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - ansicolor
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *insights_rh_registry_pull_secret
        - *quay_secret_push
        - *quay_api_token
        - *insights_cloudigrade_sonarqube_token
        - *insights_cloudigrade_rh_it_root_ca_cert_url
    <<: *gh_build_master
