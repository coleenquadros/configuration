- base_templates:
    name: base-templates

    feature_defaults: &feature_defaults
        ssh_ci_ext: ''
        disable_saasherder_validate: ''
        disable_saasherder_delete: ''

    build_deploy: &build_deploy
        shell: |
            {build_deploy_script_path}

    pr_check: &pr_check
        shell: |
            {pr_check_script_path}

    stack_analysis: &stack_analysis
        shell: |
            if [ -z "{run_stack_analysis_path}" ]; then exit; fi
            tmp_dir=$(mktemp -d -t sa-XXXXXXXXXX)
            cp {run_stack_analysis_path} $tmp_dir
            docker run --rm -v $tmp_dir:/coreapi/repo -e API_GATEWAY_URL=$API_GATEWAY_URL -e API_GATEWAY_USER_KEY=$API_GATEWAY_USER_KEY quay.io/openshiftio/fabric8-analytics-stack-analyzer-image:latest
            cat $tmp_dir/response.json > response.json
            cat response.json

    saasherder_apply: &saasherder_apply
        shell: |
            #!/bin/bash
            set -e

            if [[ ! -z "{kube_cluster}" && ! "{kube_cluster}" == "none" && ! -z "{saasherder_context}" && \
                ! -z "{saas_git}" && ! -z "{saasherder_services}" ]]; then

                SAAS_GIT="$(echo {saas_git} | sed "s|//|//$APP_SRE_BOT_USER:$APP_SRE_BOT_PUSH_TOKEN@|g")"
                git clone --depth 1 $SAAS_GIT saas.git

                if [ -n "{ssh_ci_ext}" ]; then SSH_CMD="{ssh_ci_ext_cmd}"; else SSH_CMD=""; fi

                #Deploy preview
                cd saas.git
                for service in {saasherder_services}; do
                    {saasherder} update hash ${{service}} $GIT_COMMIT
                    {saasherder} pull ${{service}}
                    {saasherder} template --filter {saasherder_object_blacklist} --output-dir {saasherder_context}-processed --local --ignore-unknown-parameters tag ${{service}}
                    {saasherder} label --output-dir {saasherder_context}-processed --saas-repo-url {saas_git} ${{service}} > label_selector

                    cat {saasherder_context}-processed/${{service}}.yaml | {oc} apply -f - --dry-run --validate
                    cat {saasherder_context}-processed/${{service}}.yaml | {oc} apply -f -

                    label_selector=$(cat label_selector)

                    # validate that the number of replicas matches the number of ready replicas
                    if [ -z "{disable_saasherder_validate}" ]; then
                        current_label_selector=$(echo $label_selector | sed 's/!=/==/g')
                        resources_to_validate=$({oc} get {saasherder_object_validate} -o name -l "$current_label_selector")

                        STATUS=invalid
                        ATTEMPT=0
                        MAX_ATTEMPTS={saasherder_validate_max_attempts} # total sleep time is (( MAX_ATTEMPTS * (MAX_ATTEMPTS+1) ) / 2)
                        until [[ "$STATUS" == "valid" || $ATTEMPT -ge $MAX_ATTEMPTS ]]; do
                            STATUS=valid
                            sleep $(( ATTEMPT++ )) # first sleep is 0
                            for r in $resources_to_validate; do
                                echo "validating $r"
                                replicas_status=$({oc} get $r -o jsonpath="{{['status.replicas', 'status.updatedReplicas', 'status.readyReplicas']}}")

                                replicas=$(echo $replicas_status | cut -d' ' -f1)
                                updated_replicas=$(echo $replicas_status | cut -d' ' -f2)
                                ready_replicas=$(echo $replicas_status | cut -d' ' -f3)

                                if [ "$replicas" == "0" ]; then
                                    continue
                                fi

                                if [[ "$replicas" != "$ready_replicas" || "$replicas" != "$updated_replicas" ]]; then
                                    echo "new replicas not ready, status is invalid"
                                    STATUS=invalid
                                    continue
                                fi
                            done
                        done

                        if [ "$STATUS" == "invalid" ]; then exit 1; fi
                    fi

                    # delete resources from previous deployments that are no longer deployed
                    if [ -z "{disable_saasherder_delete}" ]; then
                        resources_to_delete=$({oc} get {saasherder_object_whitelist} -o name -l "$label_selector")

                        for r in $resources_to_delete; do
                            {oc} delete $r
                        done
                    fi
                done
                cd ..
            fi

    post_apply: &post_apply
        shell: |
            {post_apply_script_path}

    saasherder_promote: &saasherder_promote
        shell: |
            #!/bin/bash
            set -e

            if [ -z "{auto_promote}" ]; then exit; fi

            BRANCH="promote-$GIT_COMMIT"
            if [ "$(git ls-remote --heads {saas_git} $BRANCH | wc -l)" != "0" ]; then
                echo remote branch promote-$GIT_COMMIT exists, exiting.
                exit
            fi

            cd saas.git
            git config --global user.email "$APP_SRE_BOT_EMAIL"
            git config --global user.name "$APP_SRE_BOT_USER"
            git checkout -b $BRANCH
            git add {saasherder_context}-services
            git commit -m "promote $GIT_COMMIT"
            git remote add auth $(echo {saas_git} | sed "s|//|//$APP_SRE_BOT_USER:$APP_SRE_BOT_PUSH_TOKEN@|g")
            git push auth $BRANCH
            cd ..

    build_master: &build_master_template
        <<: *feature_defaults
        disabled: '{disable}'
        kube_cluster: 'none' # https://vault.devshift.net/ui/vault/secrets/app-sre/show/creds/kube-configs/none
        backup_ecr_vault_path: 'none' # https://vault.devshift.net/ui/vault/secrets/app-sre/show/integrations-output/terraform-resources/none
        saasherder_services: ''
        build_deploy_script_path: './build_deploy.sh'
        post_apply_script_path: 'true'
        auto_promote: ''
        saas_git: ''
        saas_env: 'staging'
        wrappers:
        - timestamps
        - timeout_wrapper:
            timeout: '{timeout}'
        builders:
        - *build_deploy
        - *saasherder_apply
        - *post_apply
        - *saasherder_promote
        - *saasherder_create_pr

    pr_check_template: &pr_check_template
        disabled: '{disable}'
        node: '{node}'
        concurrent: '{concurrent_build}'
        pr_check_script_path: "./pr_check.sh"
        run_stack_analysis_path: ''
        wrappers:
        - timestamps
        - timeout_wrapper:
            timeout: '{timeout}'
        builders:
        - *pr_check
        - *stack_analysis

    timed_template: &timed_template
        disabled: '{disable}'
        properties:
        - build-discarder:
            days-to-keep: '{days_to_keep}'
            num-to-keep: '{num_to_keep}'
        ci_cmd: ./run.sh
        unstable_return: 80
        wrappers:
        - timestamps
        - timeout_wrapper:
            timeout: '{timeout}'
        builders:
        - shell:
            command: "{ci_cmd}"
            unstable-return: "{unstable_return}"
        triggers:
        - timed: |
            {cron_expression}

    build_tag_template: &build_tag_template
        disabled: '{disable}'
        build_tag_script_path: './build_tag.sh'
        wrappers:
        - timestamps
        - timeout_wrapper:
            timeout: '{timeout}'
        builders:
        - shell: |
            {build_tag_script_path}

    # Defines the saasherder shell build script that implements CD
    #
    # Arguments:
    #
    # saasherder_context:
    #    The name of the context in the saasherder repo, as defined in the
    #    config.yaml file.
    # dry_run:
    #   If `yes` => it will not perform the deployment.
    # image_pattern:
    #   Enforce that the images reference by the deployment files
    #   match this regexp. Example: '^quay\.io/app-sre/'. If empty, any image url
    #   will be valid.
    # kube_namespace:
    #   Namespace to deploy to.
    #
    # Additionally, any job or job-template that includes this definition must
    # define secrets which export:
    # - KUBE_SERVER
    # - KUBE_TOKEN
    saas_deploy: &saas_deploy
        <<: *feature_defaults
        saas_env: 'production'
        repo_url: ''
        wrappers:
        - timestamps
        - timeout_wrapper:
            timeout: '{timeout}'
        - ansicolor
        - workspace-cleanup
        - vault-secrets:
            <<: *vault_defaults
            secrets:
            - *kube_config
            - *quay_secret_pull
        builders:
        - shell: |
            #!/bin/bash
            set -e

            # Ensure $KUBE_SERVER and $KUBE_TOKEN are defined
            if [[ -z "$KUBE_SERVER" || -z "$KUBE_TOKEN" ]]; then
                echo 'Missing secret that defines $KUBE_SERVER and $KUBE_TOKEN'
                exit 1
            fi

            if [[ -z "{saasherder_context}" ]]; then
                echo '{{saasherder_context}} not defined'
                exit 1
            fi

            PROCESSED_DIR="{saasherder_context}-processed"

            {saasherder} pull
            {saasherder} template --filter {saasherder_object_blacklist} --output-dir "$PROCESSED_DIR" --local --ignore-unknown-parameters tag
            {saasherder} label --output-dir "$PROCESSED_DIR" --saas-repo-url {repo_url} > label_selectors

            if [ -n "{ssh_ci_ext}" ]; then SSH_CMD="{ssh_ci_ext_cmd}"; else SSH_CMD=""; fi

            find $PROCESSED_DIR -type f | while read f; do
                cat $f | {oc} apply -f - --dry-run --validate

                {saasherder_check_image} $f '{image_pattern}'
                cat $f | {manifest_bouncer_cmd}

                if [ "{dry_run}" != "yes" ]; then
                    cat $f | {oc} apply -f -
                fi
            done

            if [ "{dry_run}" == "yes" ]; then exit; fi

            # validate that the number of replicas matches the number of ready replicas
            if [ -z "{disable_saasherder_validate}" ]; then
                STATUS=invalid
                ATTEMPT=0
                MAX_ATTEMPTS={saasherder_validate_max_attempts} # total sleep time is (( MAX_ATTEMPTS * (MAX_ATTEMPTS+1) ) / 2)
                until [[ "$STATUS" == "valid" || $ATTEMPT -ge $MAX_ATTEMPTS ]]; do
                    STATUS=valid
                    sleep $(( ATTEMPT++ )) # first sleep is 0
                    while IFS= read -r label_selector; do
                        current_label_selector=$(echo $label_selector | sed 's/!=/==/g')
                        resources_to_validate=$({oc} get {saasherder_object_validate} -o name -l "$current_label_selector")

                        for r in $resources_to_validate; do
                            echo "validating $r"
                            replicas_status=$({oc} get $r -o jsonpath="{{['status.replicas', 'status.updatedReplicas', 'status.readyReplicas']}}")

                            replicas=$(echo $replicas_status | cut -d' ' -f1)
                            updated_replicas=$(echo $replicas_status | cut -d' ' -f2)
                            ready_replicas=$(echo $replicas_status | cut -d' ' -f3)

                            if [ "$replicas" == "0" ]; then continue; fi

                            if [[ "$replicas" != "$ready_replicas" || "$replicas" != "$updated_replicas" ]]; then
                                echo "replicas=$replicas , updated_replicas=$updated_replicas, ready_replicas=$ready_replicas"
                                echo "new replicas not ready, status is invalid"
                                STATUS=invalid
                                continue
                            fi
                        done
                    done < label_selectors
                done

                if [ "$STATUS" == "invalid" ]; then exit 1; fi
            fi

            # delete resources from previous deployments that are no longer deployed
            if [ -z "{disable_saasherder_delete}" ]; then
                while IFS= read -r label_selector; do
                    resources_to_delete=$({oc} get {saasherder_object_whitelist} -o name -l "$label_selector")

                    for r in $resources_to_delete; do
                        {oc} delete $r
                    done
                done < label_selectors
            fi

    saas_notify: &saas_notify
        publishers:
        - slack:
            room: '{slack_channel_notification}'
            notify-start: '{slack_notify_start}'
            notify-success: True
            notify-failure: True
            notify-repeated-failure: True

- base_templates:
    name: github-base-templates

    gh_properties: &gh_properties
        properties:
        - github:
            url: 'https://github.com/{gh_org}/{gh_repo}/'
        - build-discarder:
            days-to-keep: '{days_to_keep}'
            num-to-keep: '{num_to_keep}'

    gh_checkout: &gh_checkout
        branch: "master"
        node: '{node}'
        <<: *gh_properties
        scm:
        - git:
            url: 'git@github.com:{gh_org}/{gh_repo}.git'
            credentials-id: '{github_ssh_credentials}'
            branches:
            - '{branch}'
            included-regions:
            - '{include_path}'

    gh_build_master: &gh_build_master
        <<: *gh_triggers # these are different on ci.int and ci.ext
        <<: *gh_checkout
        <<: *build_master_template

    gh_pr_check: &gh_pr_check
        <<: *gh_properties
        status_context: "ci.ext.devshift.net PR build"
        scm:
        - git:
            url: 'git@github.com:{gh_org}/{gh_repo}.git'
            credentials-id: '{github_ssh_credentials}'
            skip-tag: true
            refspec: '+refs/pull/*:refs/remotes/origin/pr/*'
            branches:
            - '${{ghprbActualCommit}}'
            merge:
                remote: origin
                branch: master
            git-config-name: "AppSRE"
            git-config-email: "sd-app-sre@redhat.com"
            included-regions:
            - '{include_path}'
        triggers:
          - github-pull-request:
              cron: '* * * * *'
              github-hooks: true
              permit-all: false
              trigger-phrase: '(?ms).*^(\[test\]|\/retest|\/lgtm|\/hold)\s*$.*'
              org-list:
              - app-sre
              - openshift
              allow-whitelist-orgs-as-admins: true
              status-context: '{status_context}'
              white-list-target-branches:
              - '{white_list_target_branches}'
        <<: *pr_check_template

    gh_timed: &gh_timed
        <<: *timed_template
        <<: *gh_checkout

    gh_build_tag: &gh_build_tag
        branch: "refs/tags/*"
        <<: *gh_triggers # these are different on ci.int and ci.ext
        <<: *gh_checkout
        <<: *build_tag_template

- base_templates:
    name: gitlab-base-templates

    gl_properties: &gl_properties
        properties:
        - github:
            url: 'https://gitlab.cee.redhat.com/{gl_group}/{gl_project}.git'
        - gitlab:
            connection: 'gitlab cee'
        - build-discarder:
            days-to-keep: '{days_to_keep}'
            num-to-keep: '{num_to_keep}'

    gl_checkout: &gl_checkout
        branch: "master"
        node: '{node}'
        <<: *gl_properties
        scm:
        - git:
            url: 'git@gitlab.cee.redhat.com:{gl_group}/{gl_project}.git'
            credentials-id: '{gitlab_ssh_credentials}'
            shallow_clone: true
            branches:
            - '{branch}'
            included-regions:
            - '{include_path}'

    gl_build_master: &gl_build_master
        triggers:
        - gitlab:
            trigger-push: true
            trigger-merge-request: false
            trigger-open-merge-request-push: never
            add-note-merge-request: false
            add-ci-message: false
            set-build-description: true
            add-vote-merge-request: false
            trigger-note: false
            ci-skip: false
            wip-skip: false
            branch-filter-type: "NameBasedFilter"
            include-branches:
            - '{branch}'
        <<: *gl_checkout
        <<: *build_master_template

    gl_pr_check: &gl_pr_check
        <<: *gl_properties
        scm:
        - git:
            remotes:
            - 'origin':
                url: 'git@gitlab.cee.redhat.com:{gl_group}/{gl_project}.git'
                credentials-id: '{gitlab_ssh_credentials}'
                refspec: '+refs/heads/*:refs/remotes/origin/* +refs/merge-requests/${{gitlabMergeRequestIid}}/head:refs/remotes/origin/merge-requests/${{gitlabMergeRequestIid}}'
            branches:
            - 'merge-requests/${{gitlabMergeRequestIid}}'
            merge:
                remote: 'origin'
                branch: '${{gitlabTargetBranch}}'
            included-regions:
            - '{include_path}'
        triggers:
        - gitlab:
            trigger-push: true
            trigger-merge-request: true
            trigger-open-merge-request-push: source
            note-regex: '(?ms).*^(\[test\]|\/retest|\/lgtm|\/hold)\s*$.*'
            set-build-description: true
            add-note-merge-request: true
            add-vote-merge-request: true
            add-ci-message: true
            allow-all-branches: true
            ci-skip: true
            wip-skip: true
        publishers:
        - gitlab_message:
            <<: *gitlab_message_defaults
        - gitlab-notifier
        <<: *pr_check_template

    gl_timed: &gl_timed
        <<: *timed_template
        <<: *gl_checkout

    # This template is intended to create jobs that run the pipeline described
    # in the 'Jenkinsfile' of the project when changes are merged to the master
    # branch.
    gl_build_pipeline_template: &gl_build_pipeline_template
        project-type: pipeline
        pipeline-scm:
          scm:
          - git:
              url: 'git@gitlab.cee.redhat.com/{gl_group}/{gl_project}.git'
              credentials-id: '{gitlab_ssh_credentials}'
              shallow_clone: true
              branches:
              - '{branch}'
        triggers:
        - pollscm:
            cron: '* * * * *'
        wrappers:
        - timestamps
        - timeout_wrapper:
            timeout: '{timeout}'
        <<: *gl_checkout


    # This template is intended to dynamically create jobs that run the openshift-saas-deploy integration
    # parameters to be passed in job generation
    # saas_file_name: name of saas file
    # env_name: name opf environment
    # app_name: name of application
    # slack_channel: slack channel to send notifications to
    openshift_saas_deploy_template: &openshift_saas_deploy_template
        saas_file_name: empty
        env_name: empty
        app_name: empty
        slack_channel: empty
        label: '{app_name}'
        wrappers:
        - timeout_wrapper:
            timeout: '{timeout}'
        - vault-secrets:
            <<: *vault_defaults
            secrets:
            - *reconcile_toml
            - *github_api
        builders:
        - shell: |
            #!/bin/bash

            # Write config.toml for reconcile tools
            mkdir -p config
            echo "$CONFIG_TOML" | base64 -d > config/config.toml
            WORK_DIR=`pwd`

            set -x
            docker run --rm \
                -v $WORK_DIR/config:/config:z \
                -v /etc/pki:/etc/pki:z \
                -e REQUESTS_CA_BUNDLE=/etc/pki/tls/cert.pem \
                -e GITHUB_API=$GITHUB_API \
                {openshift_saas_deploy_image} \
                qontract-reconcile --config /config/config.toml openshift-saas-deploy \
                --saas-file-name {saas_file_name} \
                --env-name {env_name}
        publishers:
        - slack:
            room: '{slack_channel}'
            notify-start: False
            notify-success: True
            notify-failure: True
            notify-repeated-failure: True
