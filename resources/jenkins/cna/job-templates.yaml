- job-template:
    id: 'gl-timed-cna'
    name: '{gl_group}-{gl_project}-timed'
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *cna_credentials
    <<: *gl_timed
