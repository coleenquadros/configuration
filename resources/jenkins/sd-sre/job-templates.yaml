<<<<<<< HEAD
- job-template:
    id: 'operator-curator'
    name: '{gh_org}-{gh_repo}-operator-curator'
    ci_cmd: docker run --rm quay.io/openshift-sre/operator-curator --app-token "$BASIC_QUAY" --oauth-token "$CURATOR_OAUTH"
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    builders:
    - shell: |
        {ci_cmd}
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - ansicolor
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *curator_oauth
    triggers:
    - timed: |
        {cron_expression}
    <<: *gh_checkout
=======
    operator_curator: &operator_curator
        ci_cmd: ./curator.py --app-token {{BASIC_QUAY}} --oauth-token {{CURATOR_OAUTH}}
        wrappers:
        - timeout_wrapper:
            timeout: '{timeout}'
        builders:
        - shell: |
            {ci_cmd}
        wrappers:
          - timeout_wrapper:
              timeout: '{timeout}'
          - ansicolor
          - workspace-cleanup
          - vault-secrets:
              <<: *vault_defaults
              secrets:
              - *curator-oauth
              - *basic_quay
        triggers:
        - timed: |
            '{cron_expression}' #runs every day at midnight EST
        <<: *gh_checkout
>>>>>>> Initial effort to get into app-interface
