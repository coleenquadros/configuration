- job-template:
    id: 'gl-build-master-vuln-reporter-fetcher'
    name: '{gl_group}-{gl_project}-fetcher-gl-build-{branch}'
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - ansicolor
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *quay_secret_push
    <<: *gl_build_master

- job-template:
    id: 'gl-build-master-vuln-reporter-reporter'
    name: '{gl_group}-{gl_project}-reporter-gl-build-{branch}'
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - ansicolor
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *quay_secret_push
    <<: *gl_build_master

- job-template:
    id: 'gl-build-master-vuln-reporter-publisher'
    name: '{gl_group}-{gl_project}-publisher-gl-build-{branch}'
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - ansicolor
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *quay_secret_push
    <<: *gl_build_master

- job-template:
    id: 'gl-build-master-vuln-reporter-notifier'
    name: '{gl_group}-{gl_project}-notifier-gl-build-{branch}'
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - ansicolor
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *quay_secret_push
    <<: *gl_build_master
