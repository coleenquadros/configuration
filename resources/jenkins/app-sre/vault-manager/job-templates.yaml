- job-template:
    id: 'gh-pr-check-vault-manager'
    name: '{gh_org}-{gh_repo}-gh-pr-check'
    environment: production
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - ansicolor
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *app_interface_basic_auth
        - *quay_secret_push
    <<: *gh_pr_check

- job-template:
    id: 'gh-build-master-vault-manager'
    name: '{gh_org}-{gh_repo}-gh-build-{branch}'
    environment: production
    wrappers:
    - timeout_wrapper:
        timeout: '{timeout}'
    - ansicolor
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *kube_config
        - *quay_secret_push
        - *app_interface_basic_auth
    <<: *gh_build_master
