- job-template:
    id: 'gh-run-tests-osd-e2e-{environment}-{version}'
    name: '{gh_org}-{gh_repo}-run-tests-{environment}-{version}'
    wrappers:
    - vault-secrets:
        <<: *vault_defaults
        secrets:
        - *uhc_staging
        - *testgrid
    <<: *gh_timed
