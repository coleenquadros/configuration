apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: el-cad-event-listener
  name: configuration-anomaly-detection
spec:
  host: configuration-anomaly-detection.devshift.net
  to:
    kind: Service
    name: el-cad-event-listener
    weight: 100
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
