apiVersion: v1
kind: LimitRange
metadata:
  name: app-sre-defaults
spec:
  limits: []
  # this is disabled until we do
  # capacity planning
  # - default:
  #     cpu: "500m"
  #     memory: 512Mi
  #   defaultRequest:
  #     cpu: 250m
  #     memory: 128Mi
  #   type: Container
