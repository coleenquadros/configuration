apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: netobserv-operator
spec:
  channel: alpha
  installPlanApproval: Automatic
  name: netobserv-operator
  source: community-operators
  sourceNamespace: openshift-marketplace
  startingCSV: netobserv-operator.v0.1.4
  config:
    resources:
      requests:
        memory: 256Mi
        cpu: 250m
      limits:
        memory: 512Mi
        cpu: 500m
