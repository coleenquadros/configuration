{
  clusters_v1
  (
    name: "{{ name }}"
  )
  {
    name
    namespaces {
      name
      app {
        name
        escalationPolicy {
          channels {
            jiraBoard {
              name
            }
          }
        }
      }
    }
  }
}
