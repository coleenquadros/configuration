{
  clusters_v1
  (
    name: "{{ name }}"
  )
  {
    name
    namespaces {
      name
      app {
        name
        labels
        escalationPolicy {
          channels {
            jiraBoard {
              name
            }
          }
        }
      }
    }
  }
}
