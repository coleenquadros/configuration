apiVersion: v1
kind: Secret
metadata:
  name: glitchtip-metrics
  annotations:
    qontract.ignore_reconcile_time: "true"
data:
  auth_token: {{{ vault('cs-sre/creds/glitchtip/stage', 'METRICS_SCRAPE_TOKEN', 2) }}}
