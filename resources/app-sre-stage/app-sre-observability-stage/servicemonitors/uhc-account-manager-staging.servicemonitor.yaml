apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: app-sre
  name: uhc-account-manager-stage
spec:
  endpoints:
  - interval: 30s
    path: /metrics
    port: metrics
    scheme: http
  namespaceSelector:
    matchNames:
    - uhc-stage
  selector:
    matchLabels:
      app: uhc-acct-mngr
      port: metrics
  # jobLabel: "prometheusJob"
