apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: app-sre
  name: telemeter-cache-stage
spec:
  endpoints:
  - interval: 60s
    port: https
    scheme: https
    tlsConfig:
      insecureSkipVerify: true
  namespaceSelector:
    matchNames:
    - telemeter-cache-stage
  selector:
    matchLabels:
      prometheus: telemeter-cache
