apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: jaeger
  name: observatorium-jaeger
spec:
  host: observatorium-jaeger-observatorium.api.openshift.com
  # TODO(prekumar): Uncomment after migration.
  #host: observatorium-jaeger.api.openshift.com
  port:
    targetPort: https
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: reencrypt
  to:
    kind: Service
    name: jaeger-query
    weight: 100
  wildcardPolicy: None
