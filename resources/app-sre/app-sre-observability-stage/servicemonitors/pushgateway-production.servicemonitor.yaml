apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pushgateway-app-sre-production
  labels:
    prometheus: app-sre
spec:
  selector:
    matchLabels:
      app: pushgateway
  namespaceSelector:
    matchNames:
    - app-sre-observability-production
  endpoints:
  - port: scrape
  interval: 30s