apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: app-sre
  name: uhc-account-manager-production
spec:
  endpoints:
  - interval: 30s
    path: /metrics
    port: metrics
    scheme: http
  namespaceSelector:
    matchNames:
    - uhc-production
  selector:
    matchLabels:
      app: uhc-acct-mngr
      port: metrics
  # jobLabel: "prometheusJob"
