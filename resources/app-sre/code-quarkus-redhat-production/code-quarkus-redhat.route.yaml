apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: code-quarkus
  name: code-quarkus-redhat
spec:
  host: code.quarkus.redhat.com
  port:
    targetPort: "8080"
  to:
    kind: Service
    name: code-quarkus
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
