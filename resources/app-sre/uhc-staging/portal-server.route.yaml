apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: portal-server
  namespace: uhc-staging
spec:
  host: cloud.stage.openshift.com
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: portal-server
    weight: 100
