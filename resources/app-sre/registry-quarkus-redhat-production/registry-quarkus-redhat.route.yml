apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    kubernetes.io/tls-acme: "true"
  labels:
    service: registry-quarkus
  name: registry-quarkus-redhat
spec:
  host: registry.quarkus.redhat.com
  port:
    targetPort: "8080"
  to:
    kind: Service
    name: registry-quarkus
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
