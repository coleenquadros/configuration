apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: app-sre
  name: uhc-portal-server-staging
spec:
  endpoints:
  - interval: 30s
    path: /metrics
    port: metrics
  namespaceSelector:
    matchNames:
    - uhc-stage
  selector:
    matchLabels:
      app: portal-server
      port: metrics
  # jobLabel: "prometheusJob"
