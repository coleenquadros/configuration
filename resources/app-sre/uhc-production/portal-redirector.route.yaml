apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: portal-redirector
  namespace: uhc-production
spec:
  host: cloud.openshift.com
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: portal-redirector
    weight: 100
