---
"$schema": http://json-schema.org/schema#
version: 0.0.1
type: object
definitions:
  identifier:
    type: string
    pattern: "^[a-z][a-z-_]{0,30}[a-z]$"

properties:
  "$schema":
    type: string
  labels:
    "$ref": "common.json#/definitions/labels"

